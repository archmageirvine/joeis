package irvine.oeis.a048;

import java.util.TreeSet;

import irvine.factor.factor.PrimeDivision;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A048689 Number of classes generated by function A001222 when applied to binomial coefficients.
 * @author Sean A. Irvine
 */
public class A048689 implements Sequence {

  private final PrimeDivision mFactor = new PrimeDivision();
  private long mN = 0;

  @Override
  public Z next() {
    ++mN;
    final TreeSet<Long> seen = new TreeSet<>();
    for (long k = 0; k <= mN / 2; ++k) {
      seen.add(mFactor.factorize(Binomial.binomial(mN, k)).bigOmega());
    }
    return Z.valueOf(seen.size());
  }
}

