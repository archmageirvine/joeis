package irvine.oeis.a052;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A052876 Expansion of e.g.f. (exp(x)-1)^2/(-2+exp(x))^2.
 * E.g.f.: (exp(x)-1)^2/(-2+exp(x))^2
 * @author Georg Fischer
 */
public class A052876 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 2, mN), RING.pow(RING.add(RING.negate(RING.monomial(Q.TWO, 0)), RING.exp(RING.x(), mN)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
