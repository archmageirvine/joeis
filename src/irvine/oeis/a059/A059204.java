package irvine.oeis.a059;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A059204 Number of non-unimodal permutations of n items (i.e., those which do not simply go up for the first part and then down for the rest, but at some point go down then up).
 * E.g.f.: (1+x)/(2*(1-x))-exp(2*x)/2
 * @author Georg Fischer
 */
public class A059204 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.onePlusXToTheN(1), RING.multiply(RING.monomial(Q.TWO, 0), RING.oneMinusXToTheN(1), mN), mN), RING.series(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.monomial(Q.TWO, 0), mN)).coeff(mN).multiply(mF).toZ();
  }
}
