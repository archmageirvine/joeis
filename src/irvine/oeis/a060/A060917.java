package irvine.oeis.a060;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A060917 Expansion of e.g.f.: exp((-1)^k/k*LambertW(-x)^k)/(k-1)!, k=3.
 * E.g.f.: 0
 * @author Georg Fischer
 */
public class A060917 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A060917() {
    super(3);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.exp(RING.multiply(RING.series(RING.pow(RING.negate(RING.one()), 3, n), RING.monomial(Q.THREE, 0), n), RING.pow(RING.lambertW(RING.negate(RING.x()), n), 3, n), n), n), RING.monomial(Q.TWO, 0), n);
  }
}
