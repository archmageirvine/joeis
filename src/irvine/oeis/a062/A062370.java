package irvine.oeis.a062;
// Generated by gen_seq4.pl mult at 2022-08-01 11:56

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.oeis.MultiplicativeSequence;

/**
 * A062370 a(n) = Sum_{i|n,j|n} sigma(i)*sigma(j)/sigma(gcd(i,j)), where sigma(n) = sum of divisors of n.
 * @author Georg Fischer
 */
public class A062370 extends MultiplicativeSequence {

  /** Construct the sequence. */
  public A062370() {
    super(1, (p, e) -> Integers.SINGLETON.sum(1, e, k -> Jaguar.factor(p.pow(k)).sigma().multiply(2 * k + 1)).add(1));
  }
}
