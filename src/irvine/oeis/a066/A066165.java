package irvine.oeis.a066;
// Generated by gen_seq4.pl egfu at 2021-12-04 17:50

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A066165 Variant of Stanley's children's game. Class of n (named) children forms into rings of at least two with exactly one child inside each ring. a(n) gives number of possibilities, including clockwise order (or which hand is held), in each ring.
 * E.g.f.: exp(-x*log(1-x)-x^2)-1
 * @author Georg Fischer
 */
public class A066165 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A066165() {
    super(3);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.subtract(RING.exp(RING.subtract(RING.subtract(RING.zero(), RING.multiply(RING.x(), RING.log(RING.oneMinusXToTheN(1), mN), mN)), RING.pow(RING.x(), 2, mN)), mN), RING.one());
  }
}
