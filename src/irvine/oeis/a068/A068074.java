package irvine.oeis.a068;
// Generated by gen_seq4.pl sumdiv2/sumdiv at 2023-04-10 10:08

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A068074 a(n) = Sum_{d|n} (-1)^d*2^omega(n/d) where omega(x) is the number of distinct prime factors in the factorization of x.
 * @author Georg Fischer
 */
public class A068074 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A068074() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> Z.NEG_ONE.pow(d).multiply(Z.ONE.shiftLeft(Jaguar.factor(mN / d).omega())));
  }
}
