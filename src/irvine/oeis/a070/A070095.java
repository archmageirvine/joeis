package irvine.oeis.a070;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A070095 Number of acute integer triangles with perimeter n and prime side lengths.
 * a(n)=sum(k=1, floor(n/3), sum(i=k, floor((n-k)/2), (primepi(i)-primepi(i-1))*(primepi(k)-primepi(k-1))*(primepi(n-i-k)-primepi(n-i-k-1))*(1-sign(floor((n-i-k)^2/(i^2+k^2))))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A070095 extends LambdaSequence {

  /** Construct the sequence. */
  public A070095() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Puma.primePiZ(i).subtract(Puma.primePiZ(i - 1)).multiply(Puma.primePiZ(k).subtract(Puma.primePiZ(k - 1))).multiply(Puma.primePiZ(n - i - k).subtract(Puma.primePiZ(n - i - k - 1))).multiply(Z.ONE.subtract(Z.valueOf(n - i - k).square().divide(Z.valueOf(i * i + k * k)).signum())).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
