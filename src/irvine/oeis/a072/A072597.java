package irvine.oeis.a072;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A072597 Expansion of 1/(exp(-x)-x) as exponential generating function.
 * E.g.f.: 1 / (exp(-x) - x)
 * @author Georg Fischer
 */
public class A072597 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.exp(RING.negate(RING.x()), mN), RING.x()), mN).coeff(mN).multiply(mF).toZ();
  }
}
