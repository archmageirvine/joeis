package irvine.oeis.a072;
// Generated by gen_seq4.pl simbinom/simjagfa at 2023-03-17 21:33

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Euler;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A072780 a(n) = sigma_2(n) + phi(n) * sigma(n) - 2*n^2, which is A072779(n) - 2*n^2.
 * @author Georg Fischer
 */
public class A072780 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A072780() {
    super(1);
  }

  @Override
  public Z next() {
    final FactorSequence fs = Jaguar.factor(++mN);
    return fs.sigma(2).add(Euler.phi(Z.valueOf(mN)).multiply(fs.sigma()).subtract(2 * mN * mN));
  }
}
