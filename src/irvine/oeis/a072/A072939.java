package irvine.oeis.a072;
// Generated by gen_seq4.pl simgcd at 2023-08-12 22:14

import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.a036.A036554;

/**
 * A072939 Define a sequence c depending on n as follows: c(1)=1 and c(2)=n; c(k+2) = (c(k+1) + c(k))/2 if c(k+1) and c(k) have the same parity; otherwise c(k+2) = abs(c(k+1) - 2*c(k)); sequence gives values of n such that lim_{k-&gt;oo} c(k) = infinity.
 * @author Georg Fischer
 */
public class A072939 extends AbstractSequence {

  private final AbstractSequence mSeq = new A036554();

  /** Construct the sequence. */
  public A072939() {
    super(1);
  }

  @Override
  public Z next() {
    return mSeq.next().add(1);
  }
}
