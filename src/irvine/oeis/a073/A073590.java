package irvine.oeis.a073;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A073590 Expansion of e.g.f. exp(x) * log(1+x)/(1-x).
 * E.g.f.: exp(x) * log(1+x)/(1-x)
 * @author Georg Fischer
 */
public class A073590 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A073590() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.multiply(RING.exp(RING.x(), mN), RING.log(RING.onePlusXToTheN(1), mN), mN), RING.oneMinusXToTheN(1), mN);
  }
}
