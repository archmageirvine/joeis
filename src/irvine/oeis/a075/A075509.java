package irvine.oeis.a075;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A075509 Shifts one place left under 10th-order binomial transform.
 * E.g.f.: exp((exp(10*x)-1)/10)
 * @author Georg Fischer
 */
public class A075509 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.subtract(RING.exp(RING.monomial(Q.TEN, 1), mN), RING.one()), RING.monomial(Q.TEN, 0), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
