package irvine.oeis.a078;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A078468 Distinct compositions of the complete graph with one edge removed (K^-_n).
 * E.g.f.: (-1+exp(x)+exp(2*x))*exp(exp(x)-1)
 * @author Georg Fischer
 */
public class A078468 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.add(RING.add(RING.negate(RING.one()), RING.exp(RING.x(), mN)), RING.exp(RING.monomial(Q.TWO, 1), mN)), RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
