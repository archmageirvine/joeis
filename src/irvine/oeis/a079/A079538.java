package irvine.oeis.a079;
// Generated by gen_seq4.pl simbinom/simjagfa at 2023-03-17 21:33

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Euler;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A079538 a(n) = sigma[k](n) - phi(n)^k - d(n)^k for k=2.
 * @author Georg Fischer
 */
public class A079538 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A079538() {
    super(1);
  }

  @Override
  public Z next() {
    final FactorSequence fs = Jaguar.factor(++mN);
    return fs.sigma(2).subtract(Euler.phi(Z.valueOf(mN)).square()).subtract(fs.tau().square());
  }
}
