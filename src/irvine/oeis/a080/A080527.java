package irvine.oeis.a080;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-02 19:59

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A080527 Expansion of e.g.f. exp(3*cosh(x))/e^3 (even powers only).
 * E.g.f.: exp(3*cosh(x)-3), even powers
 * @author Georg Fischer
 */
public class A080527 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.multiply(RING.monomial(Q.THREE, 0), RING.cosh(RING.x(), mN), mN), RING.monomial(Q.THREE, 0)), mN).coeff(mN).multiply(mF).toZ();
  }
}
