package irvine.oeis.a081;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A081096 Taylor coefficients at x=0 of exp(exp(x^3/3+x^2/2)-1).
 * E.g.f.: exp(exp(x^3/3+x^2/2)-1)
 * @author Georg Fischer
 */
public class A081096 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.exp(RING.add(RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(Q.THREE, 0), mN), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN)), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
