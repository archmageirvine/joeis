package irvine.oeis.a083;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A083008 a(n) = Sum_{k=0..n-1} 4^k*B(k)*C(n,k) where B(k) is the k-th Bernoulli number and C(n,k) = binomial(n,k).
 * E.g.f.: 4*x/(1+exp(x)+exp(2*x)+exp(3*x))
 * @author Georg Fischer
 */
public class A083008 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.monomial(Q.FOUR, 1), RING.add(RING.add(RING.add(RING.one(), RING.exp(RING.x(), mN)), RING.exp(RING.monomial(Q.TWO, 1), mN)), RING.exp(RING.monomial(Q.THREE, 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
