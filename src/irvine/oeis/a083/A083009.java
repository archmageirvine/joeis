package irvine.oeis.a083;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A083009 a(n) = Sum_{k=0,n-1} 5^k*B(k)*binomial(n,k) where B(k) is the k-th Bernoulli number.
 * E.g.f.: 5*x/(1+exp(x)+exp(2*x)+exp(3*x)+exp(4*x))
 * @author Georg Fischer
 */
public class A083009 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.monomial(new Q(5), 1), RING.add(RING.add(RING.add(RING.add(RING.one(), RING.exp(RING.x(), mN)), RING.exp(RING.monomial(new Q(2), 1), mN)), RING.exp(RING.monomial(new Q(3), 1), mN)), RING.exp(RING.monomial(new Q(4), 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
