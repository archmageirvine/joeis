package irvine.oeis.a083;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A083384 a(n) = n*Sum(((k-1)/2)*k!*Stirling_2(n,k),k=1..n).
 * E.g.f.: x*(exp(x)-1)*exp(x)/(2-exp(x))^3
 * @author Georg Fischer
 */
public class A083384 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.multiply(RING.x(), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.exp(RING.x(), mN), mN), RING.pow(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), 3, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
