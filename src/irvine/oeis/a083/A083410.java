package irvine.oeis.a083;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A083410 a(n) = A083385(n)/n.
 * E.g.f.: (1/(2-exp(x))^2-1)/2
 * @author Georg Fischer
 */
public class A083410 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.series(RING.one(), RING.pow(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), 2, mN), mN), RING.one()), RING.monomial(Q.TWO, 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
