package irvine.oeis.a083;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A083411 a(n) = Sum(((k-1)/2)*k!*Stirling_2(n,k),k=1..n).
 * E.g.f.: (1/2)*( (1-exp(x))/(2-exp(x)) )^2
 * @author Georg Fischer
 */
public class A083411 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.series(RING.one(), RING.monomial(Q.TWO, 0), mN), RING.pow(RING.series(RING.subtract(RING.one(), RING.exp(RING.x(), mN)), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), mN), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
