package irvine.oeis.a083;
// Generated by gen_seq4.pl simbinom at 2023-03-17 21:33

import irvine.factor.factor.Jaguar;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A083538 a(n) = sigma(n)*sigma(n+1)/gcd(sigma(n+1), sigma(n))^2.
 * @author Georg Fischer
 */
public class A083538 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A083538() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Jaguar.factor(mN).sigma().multiply(Jaguar.factor(mN + 1).sigma()).divide(Jaguar.factor(mN + 1).sigma().gcd(Jaguar.factor(mN).sigma()).square());
  }
}
