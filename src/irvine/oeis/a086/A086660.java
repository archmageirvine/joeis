package irvine.oeis.a086;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A086660 Stirling transform of Hermite numbers: Sum_{k=0..n} Stirling2(n,k) * HermiteH(k,0).
 * E.g.f.: exp(-(exp(x)-1)^2)
 * @author Georg Fischer
 */
public class A086660 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.zero(), RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 2, mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
