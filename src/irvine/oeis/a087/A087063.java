package irvine.oeis.a087;
// Generated by gen_seq4.pl 2026-08-30.ack/lambdan at 2026-08-30 22: 03

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A087063 Sum{Floor(n*(r^k)): r=sqrt(2)-1, k&gt;=0}.
 * @author Georg Fischer
 */
public class A087063 extends LambdaSequence {

  /** Construct the sequence. */
  public A087063() {
    super(0, n -> {
      final CR r = CR.SQRT2.subtract(1);
      long k = 0;
      Z sum = Z.ZERO;
      Z a = r.pow(k).multiply(n).floor();
      while (!a.isZero()) {
        sum = sum.add(a);
        a = r.pow(++k).multiply(n).floor();
      }
      return sum.add(a);
    });
  }
}
