package irvine.oeis.a087;
// Generated by gen_seq4.pl 2026-08-30.ack/lambdan at 2026-08-30 22: 03

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A087065 Sum{Floor(n*r^k): r=sqrt(3)-1, k&gt;=0}.
 * @author Georg Fischer
 */
public class A087065 extends LambdaSequence {

  /** Construct the sequence. */
  public A087065() {
    super(0, n -> {
      final CR r = CR.THREE.sqrt().subtract(1);
      long k = 0;
      Z sum = Z.ZERO;
      Z a = r.pow(k).multiply(n).floor();
      while (!a.isZero()) {
        sum = sum.add(a);
        a = r.pow(++k).multiply(n).floor();
      }
      return sum.add(a);
    });
  }
}
