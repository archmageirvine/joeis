package irvine.oeis.a087;
// Generated by gen_seq4.pl 2026-08-30.ack/lambdan at 2026-08-30 22: 03

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A087066 a(n) = Sum_{k&gt;=0} floor(n*(r^k)), where r = sqrt(5)-2.
 * @author Georg Fischer
 */
public class A087066 extends LambdaSequence {

  /** Construct the sequence. */
  public A087066() {
    super(0, n -> {
      final CR r = CR.FIVE.sqrt().subtract(2);
      long k = 0;
      Z sum = Z.ZERO;
      Z a = r.pow(k).multiply(n).floor();
      while (!a.isZero()) {
        sum = sum.add(a);
        a = r.pow(++k).multiply(n).floor();
      }
      return sum.add(a);
    });
  }
}
