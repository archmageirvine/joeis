package irvine.oeis.a087;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A087649 a(n) = (1/2)*(Bell(n+2)-Bell(n+1)+Bell(n)).
 * E.g.f.: exp(exp(x) - 1) * (exp(2*x) + 1) / 2
 * @author Georg Fischer
 */
public class A087649 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.add(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.one()), mN), RING.monomial(Q.TWO, 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
