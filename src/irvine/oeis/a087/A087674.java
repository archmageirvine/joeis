package irvine.oeis.a087;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A087674 Value of the n-th Eulerian polynomial (cf. A008292) evaluated at x=-2.
 * E.g.f.: 3*exp(x)/(2*exp(x)+exp(-2*x))
 * @author Georg Fischer
 */
public class A087674 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.monomial(new Q(3), 0), RING.exp(RING.x(), mN), mN), RING.add(RING.multiply(RING.monomial(new Q(2), 0), RING.exp(RING.x(), mN), mN), RING.exp(RING.subtract(RING.zero(), RING.monomial(new Q(2), 1)), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
