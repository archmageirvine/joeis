package irvine.oeis.a087;
// Generated by gen_seq4.pl mult3man/mult at 2023-10-07 22:14

import irvine.math.z.Z;
import irvine.oeis.MultiplicativeSequence;

/**
 * A087692 Number of cubes in multiplicative group modulo n.
 * Multiplicative with: Multiplicative with a(3) = 2, a(3^k) = 2*3^(k-2) otherwise; a(p^k) = (p-1)*p^(k-1)/3 if prime p == 1 mod 6; a(p^k) = (p-1)*p^(k-1) for all other primes p.
 * @author Georg Fischer
 */
public class A087692 extends MultiplicativeSequence {

  /** Construct the sequence. */
  public A087692() {
    super(1, (p, e) -> is3(p) ? ((e == 1) ? Z.TWO : Z.THREE.pow(e - 2).multiply2()) : (isMod(6, p, 1) ? p.subtract(1).multiply(p.pow(e - 1)).divide(3) : p.subtract(1).multiply(p.pow(e - 1))));
  }
}
