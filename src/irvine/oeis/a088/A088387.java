package irvine.oeis.a088;
// Generated by gen_seq4.pl 2026-07-18.ack/lambdin at 2026-07-18 13:34

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A088387 Prime corresponding to largest prime power factor of n, a(1)=1.
 * @author Georg Fischer
 */
public class A088387 extends LambdaSequence {

  /** Construct the sequence. */
  public A088387() {
    super(1, n -> {
      final FactorSequence fs = Jaguar.factor(n);
      final Z peMax = FactorUtils.iterate(fs, Z.ZERO, (x, p, e) -> p.pow(e).compareTo(x) > 0 ? p.pow(e) : x);
      return FactorUtils.iterate(fs, Z.ZERO, (x, p, e) -> p.pow(e).equals(peMax) ? p : x);
    }, 1);
  }
}
