package irvine.oeis.a088;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A088815 Expansion of e.g.f. (1-x)^(-1/(1+log(1-x))).
 * E.g.f.: (1-x)^(-1/(1+log(1-x)))
 * @author Georg Fischer
 */
public class A088815 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.negate(RING.series(RING.one(), RING.add(RING.one(), RING.log(RING.oneMinusXToTheN(1), mN)), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
