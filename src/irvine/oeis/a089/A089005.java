package irvine.oeis.a089;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A089005 Number of partitions of n-set with at least one even block.
 * E.g.f.: exp(sinh(x))*(exp(cosh(x)-1)-1)
 * @author Georg Fischer
 */
public class A089005 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.exp(RING.sinh(RING.x(), mN), mN), RING.subtract(RING.exp(RING.subtract(RING.cosh(RING.x(), mN), RING.one()), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
