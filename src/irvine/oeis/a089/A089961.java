package irvine.oeis.a089;
// Generated by gen_seq4.pl floor at 2021-08-30 23:16

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;

/**
 * A089961 a(n) = floor( 1/( {n/phi}-{n/phi}^2 ) )-1, where {} is the fractional part and phi the golden ratio.
 * @author Georg Fischer
 */
public class A089961 extends FloorSequence {
  /** Construct the sequence. */
  public A089961() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return CR.ONE.divide(CR.ONE.divide(CR.PHI).multiply(CR.valueOf(n)).frac().subtract(CR.ONE.divide(CR.PHI).multiply(CR.valueOf(n)).frac().pow(2))).floor().subtract(Z.ONE);
  }

}
