package irvine.oeis.a090;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A090145 Even-indexed terms of the binomial transform equal 1 and the odd-indexed terms of the second binomial transform equal 1.
 * E.g.f.: 2*((1 + sinh(x))*exp(x) - sinh(x)) / (exp(x)*(1 + exp(2*x)))
 * @author Georg Fischer
 */
public class A090145 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.monomial(Q.TWO, 0), RING.subtract(RING.multiply(RING.add(RING.one(), RING.sinh(RING.x(), mN)), RING.exp(RING.x(), mN), mN), RING.sinh(RING.x(), mN)), mN), RING.multiply(RING.exp(RING.x(), mN), RING.add(RING.one(), RING.exp(RING.monomial(Q.TWO, 1), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
