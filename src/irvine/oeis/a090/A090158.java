package irvine.oeis.a090;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A090158 Odd-indexed terms of the binomial transform equals 1 and the even-indexed terms of the second binomial transform equals 1.
 * E.g.f.: 2*(1 + sinh(x) + exp(x)*sinh(x)) / (exp(x)*(1 + exp(2*x)))
 * @author Georg Fischer
 */
public class A090158 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.monomial(new Q(2), 0), RING.add(RING.add(RING.one(), RING.sinh(RING.x(), mN)), RING.multiply(RING.exp(RING.x(), mN), RING.sinh(RING.x(), mN), mN)), mN), RING.multiply(RING.exp(RING.x(), mN), RING.add(RING.one(), RING.exp(RING.monomial(new Q(2), 1), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
