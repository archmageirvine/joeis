package irvine.oeis.a090;
// Generated by gen_seq4.pl mult3man/mult at 2023-10-07 22:14

import irvine.factor.prime.Puma;
import irvine.math.z.Z;
import irvine.oeis.MultiplicativeSequence;

/**
 * A090884 a(n) is the derivative of n via transport of structure from polynomials. Completely multiplicative with a(2) = 1, a(prime(i+1)) = prime(i)^i for i &gt; 0.
 * Multiplicative with: multiplicative with a(2) = 1, a(prime(i+1)) = prime(i)^i for i > 0.
 * @author Georg Fischer
 */
public class A090884 extends MultiplicativeSequence {

  /** Construct the sequence. */
  public A090884() {
    super(1, (p, e) -> {
      if (is2(p)) {
        return Z.ONE;
      } else {
        final long i = Puma.primePi(p);
        return Puma.primeZ(i - 1).pow(e * (i - 1));
      }
    });
  }
}
