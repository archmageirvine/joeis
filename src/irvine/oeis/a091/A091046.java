package irvine.oeis.a091;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A091046 Stirling transform of first differences of Bell numbers (A005493), if offset zero: a(n) = Sum_{k=1..n} A008277(n,k)*A005493(k).
 * E.g.f.: (exp(exp(x)-1)-1)*exp(exp(exp(x)-1)-1)
 * @author Georg Fischer
 */
public class A091046 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A091046() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.multiply(RING.subtract(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.one()), RING.exp(RING.subtract(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.one()), mN), mN);
  }
}
