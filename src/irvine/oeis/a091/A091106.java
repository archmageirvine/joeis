package irvine.oeis.a091;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A091106 a(0)=a(1)=-1. For n&gt;1: a(n)=Sum(i!i^2 Stirling2[n-1,i],i=2,..,n-1).
 * E.g.f.: (exp(x)-1)^2/(2*(exp(x)-2)^2)-exp(x)
 * @author Georg Fischer
 */
public class A091106 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 2, mN), RING.multiply(RING.monomial(new Q(2), 0), RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.monomial(new Q(2), 0)), 2, mN), mN), mN), RING.exp(RING.x(), mN)).coeff(mN).multiply(mF).toZ();
  }
}
