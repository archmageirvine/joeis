package irvine.oeis.a091;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A091345 Exponential convolution of A069321(n) with itself, where we set A069321(0)=0.
 * E.g.f.: (exp(x)-1)^2 / (2-exp(x))^4
 * @author Georg Fischer
 */
public class A091345 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 2, mN), RING.pow(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), 4, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
