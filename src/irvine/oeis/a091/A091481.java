package irvine.oeis.a091;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A091481 Number of labeled rooted 2,3 cacti (triangular cacti with bridges).
 * E.g.f.: reverse(x/exp(x*(2+x)/2))
 * @author Georg Fischer
 */
public class A091481 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A091481() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.series(RING.x(), RING.exp(RING.series(RING.multiply(RING.x(), RING.add(RING.monomial(new Q(2), 0), RING.x()), n), RING.monomial(new Q(2), 0), n), n), n), n);
  }
}
