package irvine.oeis.a091;
// Generated by gen_seq4.pl 2026-08-16.ack/mtrifun

import irvine.math.MemoryFunctionInt3;
import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A091613 Triangle: T(n,k) = number of compositions (ordered partitions) of n such that some part is repeated consecutively k times and no part is repeated consecutively more than k times.
 * @author Georg Fischer
 */
public class A091613 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A091613() {
    super(1, 1, 1);
  }

  final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int l, final int k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(1, n, i ->
          i == l ? Z.ZERO : Integers.SINGLETON.sum(1, Functions.MIN.i(k, n / i), j -> get(n - i * j, i, k)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return mB.get(n, 0, k).subtract(mB.get(n, 0, k - 1));
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
