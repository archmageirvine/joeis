package irvine.oeis.a096;
// Generated by gen_seq4.pl egfu/egfuo at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A096619 Number of partitions of a 2*n-element set with exactly two odd blocks.
 * E.g.f.: 1/2*exp(cosh(x)-1)*(sinh(x))^2
 * @author Georg Fischer
 */
public class A096619 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A096619() {
    super(1, 1, 2);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.multiply(RING.multiply(RING.series(RING.one(), RING.monomial(Q.TWO, 0), n), RING.exp(RING.subtract(RING.cosh(RING.x(), n), RING.one()), n), n), RING.pow(RING.sinh(RING.x(), n), 2, n), n);
  }
}
