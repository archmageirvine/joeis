package irvine.oeis.a097;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A097236 Number of hierarchical orderings ("societies") with at least 2 elements ("individuals") on each level for n labeled elements.
 * E.g.f.: exp(-(-exp(x)+1+x)/(2-exp(x)+x))
 * @author Georg Fischer
 */
public class A097236 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.zero(), RING.series(RING.add(RING.add(RING.subtract(RING.zero(), RING.exp(RING.x(), mN)), RING.one()), RING.x()), RING.add(RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.x(), mN)), RING.x()), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
