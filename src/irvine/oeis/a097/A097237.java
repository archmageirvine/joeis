package irvine.oeis.a097;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A097237 Number of hierarchical orderings ("societies") of n labeled elements ("individuals") with at least two occupied levels.
 * E.g.f.: exp(-(exp(x)^2-2*exp(x)+1)/(-2+exp(x)))
 * @author Georg Fischer
 */
public class A097237 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.negate(RING.series(RING.add(RING.subtract(RING.pow(RING.exp(RING.x(), mN), 2, mN), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN), mN)), RING.one()), RING.add(RING.negate(RING.monomial(Q.TWO, 0)), RING.exp(RING.x(), mN)), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
