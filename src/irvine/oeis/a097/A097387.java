package irvine.oeis.a097;
// Generated by gen_seq4.pl 2026-08-04.ack/filnum at 2026-08-04 19:40

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.oeis.FilterNumberSequence;

/**
 * A097387 Let f(x)=(largest digit of x)^(smallest digit of x) + x (A097385). Sequence gives numbers n such that f(n) and f(n+1) are both prime.
 * @author Georg Fischer
 */
public class A097387 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A097387() {
    super(1, 0, k -> Predicates.PRIME.is(Functions.DIGIT_SORT_ASCENDING.z(k).modZ(10).pow(Functions.DIGIT_SORT_DESCENDING.z(k).mod(10)).add(k)) && Predicates.PRIME.is(Functions.DIGIT_SORT_ASCENDING.z(k + 1).modZ(10).pow(Functions.DIGIT_SORT_DESCENDING.z(k + 1).mod(10)).add(k + 1)));
  }
}
