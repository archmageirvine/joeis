package irvine.oeis.a097;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A097679 E.g.f.: (1/(1-x^4))*exp( 4*Sum_{i&gt;=0} x^(4*i+1)/(4*i+1) ) for an order-4 linear recurrence with varying coefficients.
 * E.g.f.: (1+x)/(1-x^4)/(1-x)*exp(2*atan(x))
 * @author Georg Fischer
 */
public class A097679 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.series(RING.series(RING.onePlusXToTheN(1), RING.oneMinusXToTheN(4), mN), RING.oneMinusXToTheN(1), mN), RING.exp(RING.multiply(RING.monomial(new Q(2), 0), RING.atan(RING.x(), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
