package irvine.oeis.a097;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A097953 Boustrophedon transform of ((-2)^n+0^n)/2.
 * E.g.f.: (1+exp(-2*x))*(sec(x)+tan(x))/2
 * @author Georg Fischer
 */
public class A097953 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.add(RING.one(), RING.exp(RING.subtract(RING.zero(), RING.monomial(Q.TWO, 1)), mN)), RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), mN), RING.monomial(Q.TWO, 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
