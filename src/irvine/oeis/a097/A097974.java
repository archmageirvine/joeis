package irvine.oeis.a097;
// Generated by gen_seq4.pl sumdiv at 2023-04-05 22:33

import irvine.math.LongUtils;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A097974 Sum of distinct prime divisors of n which are &lt;= sqrt(n).
 * @author Georg Fischer
 */
public class A097974 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A097974() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> Z.valueOf((long) (Z.valueOf(d).isProbablePrime() ? d : 0) * (d <= LongUtils.sqrt(mN) ? 1 : 0)));
  }
}
