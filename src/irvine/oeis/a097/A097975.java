package irvine.oeis.a097;
// Generated by gen_seq4.pl sumdiv at 2023-04-05 22:33

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A097975 a(n) is the prime divisor of n which is &gt;= sqrt(n), or 0 if there is no such prime divisor.
 * @author Georg Fischer
 */
public class A097975 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A097975() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> Z.valueOf((Z.valueOf(d).isProbablePrime() && (d * d >= mN)) ? d : 0));
  }
}
