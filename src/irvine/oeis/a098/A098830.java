package irvine.oeis.a098;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A098830 3*Sum_{k&gt;=0} k^n/binomial(2*k, k) = Pi*sqrt(3)*q(n) + a(n) for some rational sequence (q(n)).
 * a(n)=sum(k=0,n,sum(j=0,n-k,(j+1)^k*sum(i=0,j,(-1)^(n-k+j-i)*binom(j,i)*(j-i)^(n-k))))
 * @author Georg Fischer
 */
public class A098830 extends LambdaSequence {

  /** Construct the sequence. */
  public A098830() {
    super(-1, n -> Integers.SINGLETON.sum(0, n, k -> Integers.SINGLETON.sum(0, n - k, j -> Z.valueOf(j + 1).pow(k).multiply(Integers.SINGLETON.sum(0, j, i -> Z.NEG_ONE.pow(n - k + j - i).multiply(Binomial.binomial(j, i)).multiply(Z.valueOf(j - i).pow(n - k)))))));
  }
}
