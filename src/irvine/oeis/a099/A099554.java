package irvine.oeis.a099;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A099554 Decimal expansion of the constant x that satisfies x = exp(1/sqrt(x)).
 * Equation; x-(exp(1/sqrt(x)))
 * @author Georg Fischer
 */
public class A099554 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A099554() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.subtract(CR.ONE.divide(x.sqrt()).exp());
      }
    }.inverseMonotone(CR.valueOf(1.980), CR.valueOf(2.060)).execute(CR.ZERO));
  }
}
