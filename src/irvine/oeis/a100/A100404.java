package irvine.oeis.a100;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A100404 a(n) = L(n) * n! where L(n) are the Lucas numbers.
 * E.g.f.: (2 - x) / (1 - x - x^2)
 * @author Georg Fischer
 */
public class A100404 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.monomial(new Q(2), 0), RING.x()), RING.subtract(RING.oneMinusXToTheN(1), RING.pow(RING.x(), 2, mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
