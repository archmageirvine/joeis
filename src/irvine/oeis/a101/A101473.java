package irvine.oeis.a101;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A101473 Boustrophedon transform of the Jacobsthal numbers.
 * E.g.f.: (sec(x) + tan(x))*(exp(2*x) - exp(-x))/3
 * @author Georg Fischer
 */
public class A101473 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), RING.subtract(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.exp(RING.negate(RING.x()), mN)), mN), RING.monomial(Q.THREE, 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
