package irvine.oeis.a101;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A101474 Boustrophedon transform of the signed Jacobsthal numbers.
 * E.g.f.: (sec(x)+tan(x))*(exp(x)-exp(-2*x))/3
 * @author Georg Fischer
 */
public class A101474 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), RING.subtract(RING.exp(RING.x(), mN), RING.exp(RING.subtract(RING.zero(), RING.monomial(new Q(2), 1)), mN)), mN), RING.monomial(new Q(3), 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
