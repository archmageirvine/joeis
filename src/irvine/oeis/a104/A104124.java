package irvine.oeis.a104;
// Generated by gen_seq4.pl sumdiv2/sumdiv at 2023-04-10 10:08

import irvine.math.IntegerUtils;
import irvine.math.predicate.Predicates;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A104124 a(n) = number of ways to write n = (2m-1)^2 * k, m &gt;= k, k and m = positive integers.
 * @author Georg Fischer
 */
public class A104124 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A104124() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> ((d & 1) > 0 && Predicates.SQUARE.is((long) d) && (IntegerUtils.sqrt(d) + 1) / 2 >= mN / d) ? Z.ONE : Z.ZERO);
  }
}
