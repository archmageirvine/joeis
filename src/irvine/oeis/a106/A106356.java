package irvine.oeis.a106;
// Generated by gen_seq4.pl 2026-08-17.ack/mtrifun

import irvine.math.MemoryFunctionInt3;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A106356 Triangle T(n,k) 0&lt;=k&lt;n : Number of compositions of n with k adjacent equal parts.
 * @author Georg Fischer
 */
public class A106356 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A106356() {
    super(1, 1, 0);
  }

  final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int h, final int t) {
      return
        n == 0 ? Z.valueOf(t == 0 ? 1 : 0) : (t < 0 ? Z.ZERO : Integers.SINGLETON.sum(1, n, j -> get(n - j, j, j == h ? t - 1 : t)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return mB.get(n, -1, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
