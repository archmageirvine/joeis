package irvine.oeis.a108;
// Generated by gen_seq4.pl 2026-06-21.ack/filnum at 2026-06-21 22:51

import irvine.factor.util.FactorUtils;
import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A108405 Numbers n such that a^u + b^u + c^u + ... is prime, where a*b*c* ... is the prime factorization of n and u is the largest prime dividing n.
 * @author Georg Fischer
 */
public class A108405 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A108405() {
    super(1, 1, k -> {
      final Z u = Functions.GPF.z(k);
      return Predicates.PRIME.is(FactorUtils.iterate(k, Z.ZERO, (x, p, e) -> x.add(Integers.SINGLETON.sum(1, e, i -> p.pow(u)))));
    });
  }
}
