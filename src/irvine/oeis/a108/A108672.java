package irvine.oeis.a108;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-25 11:10

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A108672 a(n) = Sum{k=1 to n} sigma_{n-k+1}(k), where sigma_m(k) = sum{j|k} j^m.
 * a(n) = sum(k=1,n,sigma(k,n-k+1));
 * @author Georg Fischer
 */
public class A108672 extends LambdaSequence {

  /** Construct the sequence. */
  public A108672() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Jaguar.factor(k).sigma(n - k + 1)));
  }
}
