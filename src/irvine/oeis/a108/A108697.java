package irvine.oeis.a108;
// Generated by gen_seq4.pl 2026-06-21.ack/filnum at 2026-06-21 22:51

import irvine.factor.util.FactorUtils;
import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A108697 Numbers n such that a^r + b^r + c^r + ... is prime, where a*b*c* ... is the prime factorization of n and r is the product of the nonzero digits of n.
 * @author Georg Fischer
 */
public class A108697 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A108697() {
    super(1, 1, k -> {
      final Z u = Functions.DIGIT_NZ_PRODUCT.z(k);
      return Predicates.PRIME.is(FactorUtils.iterate(k, Z.ZERO, (x, p, e) -> x.add(Integers.SINGLETON.sum(1, e, i -> p.pow(u)))));
    });
  }
}
