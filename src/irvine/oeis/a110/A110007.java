package irvine.oeis.a110;
// Generated by gen_seq4.pl floor at 2021-08-30 23:16

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;

/**
 * A110007 a(n)=n-floor(phi*floor(phi^-1*floor(phi*floor(phi^-1*floor(phi*floor(phi^-1*n)))))) where phi=(1+sqrt(5))/2.
 * @author Georg Fischer
 */
public class A110007 extends FloorSequence {
  /** Construct the sequence. */
  public A110007() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).subtract(CR.PHI.multiply(CR.ONE.divide(CR.PHI).multiply(CR.PHI.multiply(CR.ONE.divide(CR.PHI).multiply(CR.PHI.multiply(CR.ONE.divide(CR.PHI).multiply(CR.valueOf(n)).floor()).floor()).floor()).floor()).floor()).floor());
  }

}
