package irvine.oeis.a110;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-13 11:40

import irvine.math.factorial.MemoryFactorial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A110131 Determinant of n X n matrix M_{i,j} = 2^i*P_i(j), where P_i(j) is the Legendre polynomial of order i at j and i and j are 0-based.
 * a(n) = 2^n * prod(k=1,n,(2*k-1)!/(k-1)!)
 * @author Georg Fischer
 */
public class A110131 extends LambdaSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A110131() {
    super(1, n -> Z.ONE.shiftLeft(n).multiply(Integers.SINGLETON.product(1, n, k ->
      MemoryFactorial.SINGLETON.factorial(2 * k - 1).divide(MemoryFactorial.SINGLETON.factorial(k - 1)))));
  }

  @Override
  public Z next() {
    return (++mN == 1) ? Z.ONE : super.next();
  }
}
