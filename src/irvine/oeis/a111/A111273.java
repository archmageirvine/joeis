package irvine.oeis.a111;
// Generated by gen_seq4.pl 2026-09-17.ack/mex

import irvine.factor.util.FactorUtils;
import irvine.math.function.Functions;
import irvine.math.z.Z;
import irvine.oeis.recur.MexSequence;

/**
 * A111273 a(n) is the smallest divisor of triangular number T(n) := n(n+1)/2 not already in the sequence.
 * @author Georg Fischer
 */
public class A111273 extends MexSequence {

  /** Construct the sequence. */
  public A111273() {
    super(1, (self, n) -> FactorUtils.iterateSorted(Functions.TRIANGULAR.z(n), Z.ZERO, (x, d) -> !self.contains(d) && x.isZero() ? d : x), "1");
  }
}
