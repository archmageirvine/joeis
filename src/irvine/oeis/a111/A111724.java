package irvine.oeis.a111;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A111724 Number of partitions of an n-set with an even number of blocks of size 1.
 * E.g.f.: cosh(x)*exp(exp(x)-1-x)
 * @author Georg Fischer
 */
public class A111724 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.cosh(RING.x(), mN), RING.exp(RING.subtract(RING.subtract(RING.exp(RING.x(), mN), RING.one()), RING.x()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
