package irvine.oeis.a116;
// Generated by gen_seq4.pl sumdiv3/sumdiv at 2023-04-11 14:46

import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A116512 a(n) = number of positive integers each of which is &lt;= n and is divisible by exactly one prime dividing n (but is coprime to every other prime dividing n). a(1) = 0.
 * @author Georg Fischer
 */
public class A116512 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A116512() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> Functions.PHI.z(mN / d).multiply((Functions.OMEGA.i(d) == 1) ? Z.ONE : Z.ZERO));
  }
}
