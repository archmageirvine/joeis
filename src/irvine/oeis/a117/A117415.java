package irvine.oeis.a117;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A117415 E.g.f. (x*tan(x)-x^2)/8 (even powers only).
 * E.g.f.: (x*tan(x)-x^2)/8, even powers
 * @author Georg Fischer
 */
public class A117415 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.multiply(RING.x(), RING.tan(RING.x(), mN), mN), RING.pow(RING.x(), 2, mN)), RING.monomial(new Q(8), 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
