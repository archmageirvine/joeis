package irvine.oeis.a120;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A120368 a(n) = number of sequences (a_1, a_2, ..., a_n) in {1,2,...,n} such that the range {a_1, a_2, ..., a_n} is an interval.
 * E.g.f.: (3-2*exp(x)+x*exp(x))/(exp(x)-2)^2
 * @author Georg Fischer
 */
public class A120368 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.subtract(RING.monomial(Q.THREE, 0), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN), mN)), RING.multiply(RING.x(), RING.exp(RING.x(), mN), mN)), RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.monomial(Q.TWO, 0)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
