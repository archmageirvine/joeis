package irvine.oeis.a122;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A122105 Sum of the bottom levels of all columns in all deco polyominoes of height n. A deco polyomino is a directed column-convex polyomino in which the height, measured along the diagonal, is attained only in the last column.
 * E.g.f.: (2 * x + (1 - x) * log(1 - x) * (2 - log(1 - x))) / (2 * (1 - x)^2)
 * @author Georg Fischer
 */
public class A122105 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.monomial(Q.TWO, 1), RING.multiply(RING.multiply(RING.oneMinusXToTheN(1), RING.log(RING.oneMinusXToTheN(1), mN), mN), RING.subtract(RING.monomial(Q.TWO, 0), RING.log(RING.oneMinusXToTheN(1), mN)), mN)), RING.multiply(RING.monomial(Q.TWO, 0), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
