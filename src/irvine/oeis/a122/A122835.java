package irvine.oeis.a122;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A122835 Number of topologies on n labeled elements in which no element belongs to any pair of noncomparable members of the topology.
 * E.g.f.: (3/4) / (1 - exp(x)/2) - exp(x)/2
 * @author Georg Fischer
 */
public class A122835 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.series(RING.monomial(Q.THREE, 0), RING.monomial(Q.FOUR, 0), mN), RING.subtract(RING.one(), RING.series(RING.exp(RING.x(), mN), RING.monomial(Q.TWO, 0), mN)), mN), RING.series(RING.exp(RING.x(), mN), RING.monomial(Q.TWO, 0), mN)).coeff(mN).multiply(mF).toZ();
  }
}
