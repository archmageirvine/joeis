package irvine.oeis.a123;
// Generated by gen_seq4.pl 2023-11-24/trilambn at 2023-11-24 23:32

import irvine.math.factorial.MemoryFactorial;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;

/**
 * A123516 Triangle read by rows: T(n,k) = (-1)^k * n! * 2^(n-2*k) * binomial(n,k) * binomial(2*k,k) (0&lt;=k&lt;=n).
 * @author Georg Fischer
 */
public class A123516 extends LambdaTriangle {

  /** Construct the sequence. */
  public A123516() {
    super(0, 0, 0, (n, k) -> {
      final Z t = Binomial.binomial(n, k).multiply(Binomial.binomial(2 * k, k)).multiply(MemoryFactorial.SINGLETON.factorial(n)).multiply(Z.NEG_ONE.pow(k));
      return n >= 2 * k ? t.multiply(Z.TWO.pow(n - 2 * k)) : t.divide(Z.TWO.pow(2 * k - n));
    });
  }
}
