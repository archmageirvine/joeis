package irvine.oeis.a124;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A124215 E.g.f.: exp(exp(x)/(2-exp(3*x))^(1/3)-1).
 * E.g.f.: exp(exp(x)/(2-exp(3*x))^(1/3)-1)
 * @author Georg Fischer
 */
public class A124215 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.series(RING.exp(RING.x(), mN), RING.exp(RING.multiply(RING.log(RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.monomial(new Q(3), 1), mN)), mN), RING.series(RING.one(), RING.monomial(new Q(3), 0), mN), mN), mN), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
