package irvine.oeis.a126;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence2;

/**
 * A126673 Third diagonal of A126671.
 * E.g.f.: (2*x - 3*x^2 + 2*(1-x)^2 * log(1-x)) / (2*(-1+x)^3)
 * @author Georg Fischer
 */
public class A126673 extends Sequence2 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.subtract(RING.monomial(Q.TWO, 1), RING.monomial(Q.THREE, 2)), RING.multiply(RING.multiply(RING.monomial(Q.TWO, 0), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN), RING.log(RING.oneMinusXToTheN(1), mN), mN)), RING.multiply(RING.monomial(Q.TWO, 0), RING.pow(RING.add(RING.negate(RING.one()), RING.x()), 3, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
