package irvine.oeis.a127;
// Generated by gen_seq4.pl 2026-08-19.ack/mlamfun

import irvine.math.MemoryFunctionInt2;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A127070 Define an array by d(m, 0) = 1, d(m, 1) = m; d(m, k) = (m - k + 1) d(m+1, k-1) - (k-1) (m+1) d(m+2, k-2). Sequence gives d(4,n).
 * @author Georg Fischer
 */
public class A127070 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A127070() {
    this(0, 0);
  }

  /**
   * Generic constructor with parameters.
   * @param offset first index
   * @param colNo column number
   */
  public A127070(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt2<Z> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Z compute(final int n, final int k) {
      return
        (k == 0) ? Z.ONE : (k == 1 ? Z.valueOf(n) : (get(n + 1, k - 1).multiply(n - k + 1).subtract(get(n + 2, k - 2).multiply(k - 1).multiply(n + 1))));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(4, n);
  }
}
