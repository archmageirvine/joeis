package irvine.oeis.a128;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A128103 Number of permutations of [n] with an even number of rises.
 * E.g.f.: 1 + 1/2 (x/(1-x) + tanh(x) )
 * @author Georg Fischer
 */
public class A128103 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.one(), RING.multiply(RING.series(RING.one(), RING.monomial(new Q(2), 0), mN), RING.add(RING.series(RING.x(), RING.oneMinusXToTheN(1), mN), RING.tanh(RING.x(), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
