package irvine.oeis.a129;
// Generated by gen_seq4.pl 2026-02-13.ack/filnum at 2026-02-13 16:26

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A129310 Numbers k such that 6*p(k)*p(k+1)*p(k+2)*p(k+3)*p(k+4)-1 and 6*p(k)*p(k+1)*p(k+2)*p(k+3)*p(k+4)+1 are twin primes with p(h) = h-th prime.
 * @author Georg Fischer
 */
public class A129310 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A129310() {
    super(1, 1, k -> {
      final Z f = Functions.PRIME.z(k).multiply(Functions.PRIME.z(k + 1)).multiply(Functions.PRIME.z(k + 2)).multiply(Functions.PRIME.z(k + 3)).multiply(Functions.PRIME.z(k + 4)).multiply(6);
      return Predicates.PRIME.is(f.subtract(1)) && Predicates.PRIME.is(f.add(1));
    });
  }
}
