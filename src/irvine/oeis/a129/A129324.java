package irvine.oeis.a129;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A129324 Third column of PE^2.
 * E.g.f.: (x^2/2) * exp(2 * (exp(x) - 1))
 * @author Georg Fischer
 */
public class A129324 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(new Q(2), 0), mN), RING.exp(RING.multiply(RING.monomial(new Q(2), 0), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
