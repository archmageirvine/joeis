package irvine.oeis.a132;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A132060 Row sums of triangle A132056 (S2(8), Stirling2 generalization).
 * E.g.f.: exp(-1+(1-7*x)^(-1/7)) - 1
 * @author Georg Fischer
 */
public class A132060 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.exp(RING.add(RING.negate(RING.one()), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.monomial(Q.SEVEN, 1)), mN), RING.negate(RING.series(RING.one(), RING.monomial(Q.SEVEN, 0), mN)), mN), mN)), mN), RING.one()).coeff(mN).multiply(mF).toZ();
  }
}
