package irvine.oeis.a133;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A133189 Number of simple directed graphs on n labeled nodes consisting only of some cycle graphs C_2 and nodes not part of a cycle having directed edges to both nodes in exactly one cycle.
 * E.g.f.: exp(exp(x)*x^2/2)
 * @author Georg Fischer
 */
public class A133189 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A133189() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.series(RING.multiply(RING.exp(RING.x(), n), RING.pow(RING.x(), 2, n), n), RING.monomial(new Q(2), 0), n), n);
  }
}
