package irvine.oeis.a134;
// Generated by gen_seq4.pl 2026-07-03.ack/filnum at 2026-07-03 22:27

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A134610 Composite numbers such that the cube root of the sum of cubes of their prime factors is a prime.
 * @author Georg Fischer
 */
public class A134610 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A134610() {
    super(1, 4, k -> {
      final FactorSequence fs = Jaguar.factor(k);
      if (fs.bigOmega() == 1) {
        return false;
      }
      final Z sum = FactorUtils.iterate(fs, Z.ZERO, (x, p, e) -> x.add(p.pow(3).multiply(e)));
      return Predicates.CUBE.is(sum) && Predicates.PRIME.is(Functions.CBRT.z(sum));
    });
  }
}
