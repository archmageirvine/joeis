package irvine.oeis.a134;
// Generated by gen_seq4.pl 2026-07-03.ack/filnum at 2026-07-03 22:27

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.predicate.Predicates;
import irvine.math.q.Q;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A134621 Numbers such that the arithmetic mean of the 4th power of their prime factors (taken with multiplicity) is a prime.
 * @author Georg Fischer
 */
public class A134621 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A134621() {
    super(1, 2, k -> {
      final FactorSequence fs = Jaguar.factor(k);
      final Q mean = new Q(FactorUtils.iterate(fs, Z.ZERO, (x, p, e) -> x.add(p.pow(4).multiply(e))), fs.bigOmega());
      return mean.isInteger() && Predicates.PRIME.is(mean.num());
    });
  }
}
