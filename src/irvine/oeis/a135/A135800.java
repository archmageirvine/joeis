package irvine.oeis.a135;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A135800 Decimal expansion of root of (2-x)^(1/(2+x)) + (3-x)^(1/(2+x)) = Pi.
 * Equation; (2-x)^(1/(2+x))+(3-x)^(1/(2+x))-(Pi)
 * @author Georg Fischer
 */
public class A135800 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A135800() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.subtract(x).pow(CR.ONE.divide(CR.TWO.add(x))).add(CR.THREE.subtract(x).pow(CR.ONE.divide(CR.TWO.add(x)))).subtract(CR.PI);
      }
    }.inverseMonotone(CR.valueOf(0.003), CR.valueOf(0.004)).execute(CR.ZERO));
  }
}
