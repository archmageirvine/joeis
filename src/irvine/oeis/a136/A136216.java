package irvine.oeis.a136;
// Generated by gen_seq4.pl trecpas

import irvine.math.z.Z;
import irvine.oeis.triangle.Triangle;

/**
 * A136216 Triangle T, read by rows, where T(n,k) = A008544(n-k)*C(n,k) where A008544 equals the triple factorials in column 0.
 * @author Georg Fischer
 */
public class A136216 extends Triangle {

  /** Construct the sequence. */
  public A136216() {
    hasRAM(false);
  }

  @Override
  protected Z compute(final int n, final int k) {
    return n == 0 ? Z.ONE : get(n - 1, k - 1).add(get(n - 1, k).multiply(3 * n - 3 * k - 1));
  }
}
