package irvine.oeis.a136;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A136554 G.f.: A(x) = Sum_{n&gt;=0} log( (1 + x)*(1 + 2^n*x) )^n / n!.
 * a(n)=sum(k=0,n,binom(2^k,k)*binom(2^k,n-k))
 * @author Georg Fischer
 */
public class A136554 extends LambdaSequence {

  /** Construct the sequence. */
  public A136554() {
    super(0, n -> Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(Z.ONE.shiftLeft(k), k).multiply(Binomial.binomial(Z.ONE.shiftLeft(k), n - k))));
  }
}
