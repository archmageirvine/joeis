package irvine.oeis.a138;
// Generated by gen_seq4.pl 2026-02-13.ack/filnum at 2026-02-13 16:26

import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A138303 Numbers k such that k*(k+1)-1 and k*(k+1)+1 are twin primes and k*(k+3)-1 and k*(k+3)+1 are also twin primes.
 * @author Georg Fischer
 */
public class A138303 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A138303() {
    super(1, 1, k -> {
      final Z f1 = Z.valueOf(k).multiply(k + 1);
      ;
      final Z f3 = Z.valueOf(k).multiply(k + 3);
      return Predicates.PRIME.is(f1.subtract(1)) && Predicates.PRIME.is(f1.add(1)) && Predicates.PRIME.is(f3.subtract(1)) && Predicates.PRIME.is(f3.add(1));
    });
  }
}
