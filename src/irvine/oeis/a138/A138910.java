package irvine.oeis.a138;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A138910 Inverse binomial transform of A138909.
 * E.g.f.: (x+1)/(exp(x)-x*exp(2*x))
 * @author Georg Fischer
 */
public class A138910 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.x(), RING.one()), RING.subtract(RING.exp(RING.x(), mN), RING.multiply(RING.x(), RING.exp(RING.monomial(Q.TWO, 1), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
