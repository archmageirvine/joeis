package irvine.oeis.a139;
// Generated by gen_seq4.pl 2026-04-13.ack/lambdan at 2026-04-13 15:20

import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A139104 Numbers whose binary representation shows the distribution of prime numbers up to the n-th prime, using &quot;0&quot; for primes and &quot;1&quot; for nonprime numbers.
 * @author Georg Fischer
 */
public class A139104 extends LambdaSequence {

  /** Construct the sequence. */
  public A139104() {
    super(1, n -> {
      final int pn = Functions.PRIME.i(n);
      return Z.TWO.pow(pn).subtract(1).subtract(Integers.SINGLETON.sum(1, pn, i -> Z.TWO.pow(pn - i).multiply(Functions.PRIME_PI.z(i))).add(n).divide(2));
    });
  }
}
