package irvine.oeis.a140;
// Generated by gen_seq4.pl 2026-06-23.ack/filnum at 2026-06-23 23:37

import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A140831 Numbers in whose canonical prime factorization the powers of the primes do not form an increasing sequence.
 * @author Georg Fischer
 */
public class A140831 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A140831() {
    super(1, 2, k -> {
      final Z high = Z.valueOf(19470629L);
      return FactorUtils.iterate(k, Z.ZERO, (x, p, e) -> x.isZero() ? p.pow(e) : (p.pow(e).compareTo(x) >= 0 ? p.pow(e) : high)).equals(high);
    });
  }
}
