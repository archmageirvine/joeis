package irvine.oeis.a141;
// Generated by gen_seq4.pl trisumm/trisimple at 2023-06-09 19:35

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;

/**
 * A141901 Triangle T(n, k) = Sum_{j=0..n-k-1} binomial(n, j+k+1) - 2^(n-k) with T(n, 0) = 1, read by rows.
 * @author Georg Fischer
 */
public class A141901 extends BaseTriangle {

  /** Construct the sequence. */
  public A141901() {
    super(0, 0, 0);
    hasRAM(true);
  }

  @Override
  public Z triangleElement(final int n, final int k) {
    return (k == 0) ? Z.ONE : Integers.SINGLETON.sum(0, n - k - 1, j -> Binomial.binomial(n, j + k + 1)).subtract(Z.ONE.shiftLeft(n - k));
  }
}
