package irvine.oeis.a143;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A143405 Number of forests of labeled rooted trees of height at most 1, with n labels, where any root may contain &gt;= 1 labels, also row sums of A143395, A143396 and A143397.
 * E.g.f.: exp(exp(x)*(exp(x)-1))
 * @author Georg Fischer
 */
public class A143405 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
