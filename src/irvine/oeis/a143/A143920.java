package irvine.oeis.a143;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A143920 E.g.f. satisfies: A(x) = 1 + x*exp(2*Integral A(x) dx).
 * E.g.f.: (1+exp(2*x)) / (1+exp(2*x)*(1-2*x))
 * @author Georg Fischer
 */
public class A143920 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.one(), RING.exp(RING.monomial(Q.TWO, 1), mN)), RING.add(RING.one(), RING.multiply(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.subtract(RING.one(), RING.monomial(Q.TWO, 1)), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
