package irvine.oeis.a144;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A144211 Decimal expansion of solution to (x+1)^(x+1) = x^(x+2).
 * Equation; (x+1)^(x+1)-(x^(x+2))
 * @author Georg Fischer
 */
public class A144211 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A144211() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.add(CR.ONE).pow(x.add(CR.ONE)).subtract(x.pow(x.add(CR.TWO)));
      }
    }.inverseMonotone(CR.valueOf(3.078), CR.valueOf(3.204)).execute(CR.ZERO));
  }
}
