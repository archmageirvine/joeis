package irvine.oeis.a144;
// Generated by gen_seq4.pl 2026-08-17.ack/marrfun

import irvine.math.MemoryFunctionInt2;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A144287 Square array A(n,k), n&gt;=1, k&gt;=1, read by antidiagonals: A(n,k) = Fibonacci rabbit sequence number n coded in base k.
 * @author Georg Fischer
 */
public class A144287 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A144287() {
    super(1, 1, 1, -1);
  }

  final MemoryFunctionInt2<Z[]> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Z[] compute(final int n, final int b) {
      return
        n < 2
          ? new Z[] {Z.valueOf(n), Z.valueOf(n)}
          : new Z[] {get(n - 1, b)[0].multiply(Z.valueOf(b).pow(get(n - 1, b)[1])).add(get(n - 2, b)[0]), get(n - 1, b)[1].add(get(n - 2, b)[1])};
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return mB.get(n, k)[0];
  }

  @Override
  public Z a(final long n, final long k) {
    return matrixElement((int) n, (int) k);
  }
}
