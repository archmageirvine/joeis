package irvine.oeis.a145;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A145222 a(n) is the number of odd permutations (of an n-set) with exactly 1 fixed point.
 * E.g.f.: ((x^3)*exp(-x))/(2*(1-x))
 * @author Georg Fischer
 */
public class A145222 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.pow(RING.x(), 3, mN), RING.exp(RING.negate(RING.x()), mN), mN), RING.multiply(RING.monomial(Q.TWO, 0), RING.oneMinusXToTheN(1), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
