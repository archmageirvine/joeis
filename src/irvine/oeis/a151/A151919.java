package irvine.oeis.a151;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A151919 a(n) = (-2)^n*A_{n,3}(1/2) where A_{n,k}(x) are the generalized Eulerian polynomials.
 * E.g.f.: exp(-x)/(2 - exp(-3*x))
 * @author Georg Fischer
 */
public class A151919 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A151919() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.exp(RING.negate(RING.x()), n), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.negate(RING.monomial(Q.THREE, 1)), n)), n);
  }
}
