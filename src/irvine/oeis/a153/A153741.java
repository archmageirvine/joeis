package irvine.oeis.a153;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A153741 Number of elements in wreath product C_2 \u2240 S_n that alternate up/not-up with respect to a weak product ordering.
 * E.g.f.: (1 + sin(x) + x*cos(x))/(cos(x) - x*sin(x))
 * @author Georg Fischer
 */
public class A153741 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.add(RING.one(), RING.sin(RING.x(), mN)), RING.multiply(RING.x(), RING.cos(RING.x(), mN), mN)), RING.subtract(RING.cos(RING.x(), mN), RING.multiply(RING.x(), RING.sin(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
