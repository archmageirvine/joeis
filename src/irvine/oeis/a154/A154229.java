package irvine.oeis.a154;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A154229 0..50
 * @author Georg Fischer
 */
public class A154229 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A154229() {
    super(0, 0, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        k == 0 || k == n ? Z.ONE : get(n - 1, k).add(get(n - 1, k - 1)).add(get(n - 2, k - 1).multiply(Z.valueOf(n + 1).multiply(n + 2).divide(2).square()));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
