package irvine.oeis.a156;
// Generated by gen_seq4.pl 2026-03-14.ack/trink1 at 2026-03-14 23:12

import java.util.function.BiFunction;

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;


/**
 * A156722 Triangle T(n, k, m) = t(n,m)/( t(k,m) * t(n-k,m) ) with T(n, 0, m) = T(n, n, m) = 1, where t(n, m) = Product_{j=1..n} Product_{i=1..j-1} ( 1 - (m+1)*(3*i-2) ) and m = 1, read by rows.
 * @author Georg Fischer
 */
public class A156722 extends LambdaTriangle {

  private static final BiFunction<Integer, Integer, Z> LAMBDA = (n, m) -> Integers.SINGLETON.product(1, n, j -> Integers.SINGLETON.product(1, j - 1, i -> Z.ONE.subtract(Z.valueOf(m + 1).multiply(3 * i - 2))));

  private static Z b(final int n, final int k) {
    return LAMBDA.apply(n, k);
  }

  /** Construct the sequence. */
  public A156722() {
    this(0, 1);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param q auxiliary parameter, to be used in <code>lambda</code> and method <code>t</code>
   */
  public A156722(final int offset, final int q) {
    super(offset, 0, 0, (n, k) -> (k == 0 || k.equals(n)) ? Z.ONE : b(n, q).divide(b(n - k, q).multiply(b(k, q))));
  }
}
