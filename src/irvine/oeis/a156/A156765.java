package irvine.oeis.a156;
// Generated by gen_seq4.pl 2026-03-14.ack/trink1 at 2026-03-14 23:12

import java.util.function.BiFunction;

import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;


/**
 * A156765 Triangle T(n, k, m) = f(n, m)/(f(k, m)*f(n-k, m)), where T(0, k, m) = 1, f(n, k) = Product_{j=1..n} ( j!*((k+1)^j -1)/k ), f(n, 0) = n!, and m = 1, read by rows.
 * @author Georg Fischer
 */
public class A156765 extends LambdaTriangle {

  private static final BiFunction<Integer, Integer, Z> LAMBDA = (n, k) -> (k == 0) ? Functions.FACTORIAL.z(n) : Integers.SINGLETON.product(1, n, j -> Z.valueOf(k + 1).pow(j).subtract(1).divide(k).multiply(Functions.FACTORIAL.z(j)));

  private static Z b(final int n, final int k) {
    return LAMBDA.apply(n, k);
  }

  /** Construct the sequence. */
  public A156765() {
    this(0, 1);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param q auxiliary parameter, to be used in <code>lambda</code> and method <code>t</code>
   */
  public A156765(final int offset, final int q) {
    super(offset, 0, 0, (n, k) -> (n == 0) ? Z.ONE : b(n, q).divide(b(n - k, q).multiply(b(k, q))));
  }
}
