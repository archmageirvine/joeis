package irvine.oeis.a159;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A159662 Any number of necklaces made from n distinct colored beads then linearly arranged in a display case.
 * E.g.f.: 1/(1-x/2-x^2/4+log(1-x)/2)
 * @author Georg Fischer
 */
public class A159662 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.add(RING.subtract(RING.subtract(RING.one(), RING.series(RING.x(), RING.monomial(Q.TWO, 0), mN)), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.FOUR, 0), mN)), RING.series(RING.log(RING.oneMinusXToTheN(1), mN), RING.monomial(Q.TWO, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
