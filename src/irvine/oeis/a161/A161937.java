package irvine.oeis.a161;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A161937 The number of indirect isometries that are derangements of the (n-1)-dimensional facets of an n-cube.
 * E.g.f.: x*exp(-x) / (1 - 2*x)
 * @author Georg Fischer
 */
public class A161937 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.x(), RING.exp(RING.negate(RING.x()), mN), mN), RING.subtract(RING.one(), RING.monomial(Q.TWO, 1)), mN).coeff(mN).multiply(mF).toZ();
  }
}
