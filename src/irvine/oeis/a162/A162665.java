package irvine.oeis.a162;
// Generated by gen_seq4.pl simbinom/simjagfa at 2023-03-17 21:33

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A162665 a(n) = sigma_2(n) + 3 sigma(n) tau(n) + tau(n)^3.
 * @author Georg Fischer
 */
public class A162665 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A162665() {
    super(1);
  }

  @Override
  public Z next() {
    final FactorSequence fs = Jaguar.factor(++mN);
    return fs.sigma(2).add(fs.sigma().multiply(fs.tau()).multiply(3)).add(fs.tau().pow(3));
  }
}
