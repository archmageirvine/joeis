package irvine.oeis.a163;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A163982 Real part of the coefficient [x^n] of the expansion of (1+i)/(1-i*exp(x)) - 1 multiplied by 2*n!, where i is the imaginary unit.
 * E.g.f.: (-2)*exp(x/2)*cosh(x/2)/cosh(x)
 * @author Georg Fischer
 */
public class A163982 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.multiply(RING.negate(RING.monomial(Q.TWO, 0)), RING.exp(RING.series(RING.x(), RING.monomial(Q.TWO, 0), mN), mN), mN), RING.cosh(RING.series(RING.x(), RING.monomial(Q.TWO, 0), mN), mN), mN), RING.cosh(RING.x(), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
