package irvine.oeis.a168;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A168546 Decimal expansion of the argument z in (0,Pi/2) for which the function log(cos(sin(x)))/log(sin(cos(x))) possesses the maximum in (0,Pi/2).
 * Equation; -cos(x)*sin(sin(x))/cos(sin(x))/log(sin(cos(x)))+log(cos(sin(x)))/log(sin(cos(x)))^2*sin(x)*cos(cos(x))/sin(cos(x))
 * @author Georg Fischer
 */
public class A168546 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A168546() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ZERO.subtract(x.cos().multiply(x.sin().sin()).divide(x.sin().cos()).divide(x.cos().sin().log())).add(x.sin().cos().log().divide(x.cos().sin().log().pow(2)).multiply(x.sin()).multiply(x.cos().cos()).divide(x.cos().sin()));
      }
    }.inverseMonotone(CR.valueOf(0.830), CR.valueOf(0.864)).execute(CR.ZERO));
  }
}
