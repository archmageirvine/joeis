package irvine.oeis.a172;
// Generated by gen_seq4.pl 2026-03-14.ack/trink1 at 2026-03-14 23:12

import java.util.function.BiFunction;

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;


/**
 * A172091 Triangle, read by rows, T(n,k,q) = c(k,q) + c(n-k,q) - c(n, q) where c(n,q) = Product_{j=1..n-1} ((q^(j+1) - 1)/(q-1)) and q = 2.
 * @author Georg Fischer
 */
public class A172091 extends LambdaTriangle {

  private static final BiFunction<Integer, Integer, Z> LAMBDA = (n, m) -> Integers.SINGLETON.product(1, n - 1, j -> Z.valueOf(m).pow(j + 1).subtract(1).divide(Z.valueOf(m).subtract(1)));

  private static Z b(final int n, final int k) {
    return LAMBDA.apply(n, k);
  }

  /** Construct the sequence. */
  public A172091() {
    this(0, 2);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param q auxiliary parameter, to be used in <code>lambda</code> and method <code>t</code>
   */
  public A172091(final int offset, final int q) {
    super(offset, 0, 0, (n, k) -> b(k, q).add(b(n - k, q)).subtract(b(n, q)));
  }
}
