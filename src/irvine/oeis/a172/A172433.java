package irvine.oeis.a172;
// Generated by gen_seq4.pl 2024-11-21.ack/lambdan at 2024-11-21 23:03

import irvine.math.cr.CR;
import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A172433 Let u(n) = Sum [n/[sqrt k]] and v(n) = Sum [n/(sqrt k)] where the summation index k ranges from 1 to infinity, although both sums are actually finite. Here [a] denotes the integer part of a. Then a(n) = u(n) - v(n).
 * @author Georg Fischer
 */
public class A172433 extends LambdaSequence {

  /** Construct the sequence. */
  public A172433() {
    super(1, n -> Integers.SINGLETON.sum(1, (n + 1) * (n + 1), k -> Z.valueOf(n).divide(Functions.SQRT.z(k))).subtract(Integers.SINGLETON.sum(1, (n + 1) * (n + 1), k -> CR.valueOf(n).divide(CR.valueOf(k).sqrt()).floor())));
  }
}
