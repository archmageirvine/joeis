package irvine.oeis.a173;
// Generated by gen_seq4.pl 2026-03-14.ack/trink0 at 2026-03-14 23:12

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;


/**
 * A173117 Triangle T(n, k, q) = q*[n=2] + Sum_{j=0..5} q^j*binomial(n-2*j, k-j)*[n&gt;2*j] with T(n,0) = T(n,n) = 1 for q = 1, read by rows.
 * @author Georg Fischer
 */
public class A173117 extends LambdaTriangle {

  /** Construct the sequence. */
  public A173117() {
    this(0, 1);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param q auxiliary parameter, to be used in <code>lambda</code> and method <code>t</code>
   */
  public A173117(final int offset, final int q) {
    super(offset, 0, 0, (n, k) -> (k == 0 || k.equals(n)) ? Z.ONE : Z.valueOf(q).multiply(n == 2 ? 1 : 0).add(Integers.SINGLETON.sum(0, 5, j -> Z.valueOf(q).pow(j).multiply(Binomial.binomial(n - 2 * j, k - j)).multiply(n > 2 * j ? 1 : 0))));
  }
}
