package irvine.oeis.a173;
// Generated by gen_seq4.pl 2026-03-14.ack/trink1 at 2026-03-14 23:12

import java.util.function.BiFunction;

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.LambdaTriangle;


/**
 * A173507 Triangle T(n, k, q) = 2*c(n, q)/(c(k,q)*c(n-k,q)) where c(n,q) = Product_{j=0..n} (q^(q^j) - 1) and q=3, read by rows.
 * @author Georg Fischer
 */
public class A173507 extends LambdaTriangle {

  private static final BiFunction<Integer, Integer, Z> LAMBDA = (n, qq) -> Integers.SINGLETON.product(0, n, j -> Z.valueOf(qq).pow(Z.valueOf(qq).pow(j)).subtract(1));

  private static Z b(final int n, final int k) {
    return LAMBDA.apply(n, k);
  }

  /** Construct the sequence. */
  public A173507() {
    this(0, 3);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param q auxiliary parameter, to be used in <code>lambda</code> and method <code>t</code>
   */
  public A173507(final int offset, final int q) {
    super(offset, 0, 0, (n, k) -> b(n, q).multiply(2).divide(b(n - k, q).multiply(b(k, q))));
  }
}
