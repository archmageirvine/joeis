package irvine.oeis.a174;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-13 11:40

import irvine.math.factorial.MemoryFactorial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A174764 Sum of the numerators for computing the second moment of the probability mass function (PMF) of the number of 2-cycles in the involutions on n elements (A000085) assuming the involutions are all equally likely.
 * a(n) = sum(k=0,( n/2 ),k^2*n!/((n-2*k)!*2^k*k!))
 * @author Georg Fischer
 */
public class A174764 extends LambdaSequence {

  /** Construct the sequence. */
  public A174764() {
    super(1, n -> Integers.SINGLETON.sum(0, n / 2, k -> Z.valueOf(k).multiply(k).multiply(MemoryFactorial.SINGLETON.factorial(n)).divide(MemoryFactorial.SINGLETON.factorial(n - 2 * k).multiply(Z.ONE.shiftLeft(k)).multiply(MemoryFactorial.SINGLETON.factorial(k)))));
  }
}
