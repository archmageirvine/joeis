package irvine.oeis.a177;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A177257 a(n) = Sum((binomial(n,j)-j-1)*B(j), j=0..n-1), where B(j)=A000110(j) are the Bell numbers.
 * a(n)=sum(j=0, n-1, (binom(n, j)-j-1)*bell(j))
 * @author Georg Fischer
 */
public class A177257 extends LambdaSequence {

  /** Construct the sequence. */
  public A177257() {
    super(0, n -> Integers.SINGLETON.sum(0, n - 1, j -> {
      return Binomial.binomial(n, j).subtract(j).subtract(Z.ONE).multiply(Functions.BELL.z(j));
    }));
  }
}
