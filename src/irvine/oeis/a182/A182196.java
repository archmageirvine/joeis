package irvine.oeis.a182;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A182196 Decimal expansion of the larger root of x^sqrt(x+1) = sqrt(x+1)^x.
 * Equation; x^sqrt(x+1)-(sqrt(x+1)^x)
 * @author Georg Fischer
 */
public class A182196 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A182196() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(x.add(CR.ONE).sqrt()).subtract(x.add(CR.ONE).sqrt().pow(x));
      }
    }.inverseMonotone(CR.valueOf(3.529), CR.valueOf(3.673)).execute(CR.ZERO));
  }
}
