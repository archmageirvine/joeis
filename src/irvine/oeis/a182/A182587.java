package irvine.oeis.a182;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A182587 Decimal expansion of the only solution of x^(4/x^2) = x - 1.
 * Equation; x^(4/x^2)-(x-1)
 * @author Georg Fischer
 */
public class A182587 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A182587() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(CR.FOUR.divide(x.pow(2))).subtract(x.subtract(CR.ONE));
      }
    }.inverseMonotone(CR.valueOf(2.664), CR.valueOf(2.772)).execute(CR.ZERO));
  }
}
