package irvine.oeis.a182;
// Generated by gen_seq4.pl 2026-07-28.ack/lambdin at 2026-07-28 23:20

import irvine.math.function.Functions;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A182676 a(n) is the largest n-digit number with exactly 8 divisors, a(n) = 0 if no such number exists.
 * @author Georg Fischer
 */
public class A182676 extends LambdaSequence {

  /** Construct the sequence. */
  public A182676() {
    super(1, n -> {
      final Z lim = Z.TEN.pow(n - 1);
      Z k = lim.multiply(10).subtract(1);
      while (k.compareTo(lim) > 0 && Functions.TAU.l(k) != 8) {
        k = k.subtract(1);
      }
      return k;
    }, 0);
  }
}
