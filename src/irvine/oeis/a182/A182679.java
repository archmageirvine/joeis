package irvine.oeis.a182;
// Generated by gen_seq4.pl 2026-07-28.ack/lambdin at 2026-07-28 23:20

import irvine.math.function.Functions;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A182679 a(n) = the smallest n-digit number with exactly 10 divisors, a(n) = 0 if no such number exists.
 * @author Georg Fischer
 */
public class A182679 extends LambdaSequence {

  /** Construct the sequence. */
  public A182679() {
    super(1, n -> {
      final Z lim = Z.TEN.pow(n);
      Z k = lim.divide(10);
      while (k.compareTo(lim) < 0 && Functions.TAU.l(k) != 10) {
        k = k.add(1);
      }
      return k.equals(lim) ? Z.ZERO : k;
    }, 0);
  }
}
