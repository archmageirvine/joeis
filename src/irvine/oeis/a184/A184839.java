package irvine.oeis.a184;
// Generated by gen_seq4.pl 2026-07-25.ack/lambdan at 2026-07-25 22:01

import irvine.math.cr.CR;
import irvine.oeis.LambdaSequence;
import irvine.oeis.a103.A103814;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A184839 a(n) = n + floor(n*t) + floor(n*t^2) + floor(n*t^3) + floor(n*t^4), where t is the pentanacci constant.
 * @author Georg Fischer
 */
public class A184839 extends LambdaSequence {

  private static final DecimalExpansionSequence A103814 = new A103814();

  /** Construct the sequence. */
  public A184839() {
    super(1, n -> {
      final CR cn = CR.valueOf(n);
      final CR t = A103814.getCR();
      return cn.add(cn.multiply(t)).floor().add(cn.multiply(t.square()).floor()).add(cn.multiply(t.pow(3)).floor()).add(cn.multiply(t.pow(4)).floor());
    });
  }
}
