package irvine.oeis.a184;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A184903 n+floor(nr/t)+floor(ns/t), where r=(1+sqrt(5))/2, s=r+1, t=r+2.
 * @author Georg Fischer
 */
public class A184903 extends FloorSequence {

  private final CR mR = CR.HALF.add(CR.HALF.multiply(CR.FIVE.sqrt()));
  private final CR mS = mR.add(CR.ONE);
  private final CR mT = mR.add(CR.TWO);

  /** Construct the sequence */
  public A184903() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mR).divide(mT).floor()).add(CR.valueOf(mN).multiply(mS).divide(mT).floor());
  }

}
