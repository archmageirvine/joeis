package irvine.oeis.a184;
// Generated by gen_seq4.pl 2025-02-16.ack/lambdan at 2025-02-16 22:50

import irvine.math.cr.CR;
import irvine.math.q.Q;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A184911 a(n) = n + floor(n*r/t) + floor(n*s/t) with r = 2^(1/5), s = r^2, t = r^3.
 * @author Georg Fischer
 */
public class A184911 extends LambdaSequence {

  /** Construct the sequence. */
  public A184911() {
    super(1, n -> {
      final CR r = CR.TWO.pow(new Q(1, 5));
      final CR s = r.square();
      final CR t = r.pow(3);
      return Z.valueOf(n).add(CR.valueOf(n).multiply(r).divide(t).floor()).add(CR.valueOf(n).multiply(s).divide(t).floor());
    });
  }
}
