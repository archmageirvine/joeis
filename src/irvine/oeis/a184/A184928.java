package irvine.oeis.a184;
// Generated by gen_seq4.pl floor at 2021-08-28 23:11

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A184928 a(n) = n + [sn/r] + [tn/r] + [un/r], where []=floor and r=sin(Pi/2), s=sin(Pi/3), t=sin(Pi/4), u=sin(Pi/5).
 * @author Georg Fischer
 */
public class A184928 extends FloorSequence {

  private final CR mR = CR.ONE;
  private final CR mS = CR.HALF.multiply(CR.THREE.sqrt());
  private final CR mT = CR.HALF.multiply(CR.SQRT2);
  private final CR mU = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();

  /** Construct the sequence */
  public A184928() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(mS.multiply(CR.valueOf(mN)).divide(mR).floor()).add(mT.multiply(CR.valueOf(mN)).divide(mR).floor()).add(mU.multiply(CR.valueOf(mN)).divide(mR).floor());
  }

}
