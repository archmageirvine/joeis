package irvine.oeis.a184;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A184942 E.g.f.: 1/( cos(log(1-x)) + sin(log(1-x)) ).
 * E.g.f.: 1/( cos(log(1-x)) + sin(log(1-x)) )
 * @author Georg Fischer
 */
public class A184942 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.add(RING.cos(RING.log(RING.oneMinusXToTheN(1), mN), mN), RING.sin(RING.log(RING.oneMinusXToTheN(1), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
