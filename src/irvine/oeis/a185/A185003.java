package irvine.oeis.a185;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.factor.Jaguar;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A185003 a(n) = Sum_{k=1..n} binomial(n,k)*sigma(k).
 * a(n)=sum(k=1, n, binom(n, k)*sigma(k, 1))
 * @author Georg Fischer
 */
public class A185003 extends LambdaSequence {

  /** Construct the sequence. */
  public A185003() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Binomial.binomial(n, k).multiply(Jaguar.factor(k).sigma(1))));
  }
}
