package irvine.oeis.a185;
// Generated by gen_seq4.pl egfu/egfue at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A185071 E.g.f.: 1/sqrt(cos(x)*cosh(x) - sin(x)*sinh(x)), omitting the zero-valued coefficients of odd powers of x.
 * E.g.f.: 1/sqrt(cos(x)*cosh(x) - sin(x)*sinh(x))
 * @author Georg Fischer
 */
public class A185071 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A185071() {
    super(0, 0, 2);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.pow(RING.subtract(RING.multiply(RING.cos(RING.x(), n), RING.cosh(RING.x(), n), n), RING.multiply(RING.sin(RING.x(), n), RING.sinh(RING.x(), n), n)), Q.HALF, n), n);
  }
}
