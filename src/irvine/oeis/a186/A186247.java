package irvine.oeis.a186;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A186247 n-th derivative of sec(x)^x at x=0.
 * E.g.f.: cos(x)^(-x)
 * @author Georg Fischer
 */
public class A186247 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.log(RING.cos(RING.x(), mN), mN), RING.subtract(RING.zero(), RING.x()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
