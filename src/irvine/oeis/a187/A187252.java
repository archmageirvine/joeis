package irvine.oeis.a187;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A187252 Number of cycles with at least 3 alternating runs in all permutations of [n] (it is assumed that the smallest element of a cycle is in the first position).
 * E.g.f.: -(1/4)(2*x-1+exp(2*x)+4*log(1-x))/(1-x)
 * @author Georg Fischer
 */
public class A187252 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A187252() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.negate(RING.series(RING.multiply(RING.series(RING.one(), RING.monomial(Q.FOUR, 0), n), RING.add(RING.add(RING.subtract(RING.monomial(Q.TWO, 1), RING.one()), RING.exp(RING.monomial(Q.TWO, 1), n)), RING.multiply(RING.monomial(Q.FOUR, 0), RING.log(RING.oneMinusXToTheN(1), n), n)), n), RING.oneMinusXToTheN(1), n));
  }
}
