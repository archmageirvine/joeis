package irvine.oeis.a190;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A190060 a(n) = n + [n*r/s] + [n*t/s];  r=1, s=sin(Pi/5), t=csc(Pi/5).
 * @author Georg Fischer
 */
public class A190060 extends FloorSequence {

  private final CR mR = CR.ONE;
  private final CR mS = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mT = CR.ONE.divide(CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin());

  /** Construct the sequence. */
  public A190060() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mR).divide(mS).floor()).add(CR.valueOf(n).multiply(mT).divide(mS).floor());
  }

}
