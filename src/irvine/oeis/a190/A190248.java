package irvine.oeis.a190;
// Generated by gen_seq4.pl floor3cr 2020-06-09 22:00
// DO NOT EDIT here!

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A190248 <code>a(n) = [nu+nv+nw]-[nu]-[nv]-[nw]</code>, where <code>u=(1+sqrt(5))/2, v=u^2, w=u^3, []=floor</code>.
 * @author Georg Fischer
 */
public class A190248 implements Sequence {
  private static final ComputableReals REALS = ComputableReals.SINGLETON;
  private long mN = 0;
  private static final CR U = CR.ONE.add(CR.FIVE.sqrt()).divide(CR.TWO);
  private static final CR V = REALS.pow(U, 2);
  private static final CR W = REALS.pow(U, 3);

  @Override
  public Z next() {
    final CR n = CR.valueOf(++mN);
    return n.multiply(U).add(n.multiply(V)).add(n.multiply(W)).floor().subtract(n.multiply(U).floor()).subtract(n.multiply(V).floor()).subtract(n.multiply(W).floor());
  }
}
