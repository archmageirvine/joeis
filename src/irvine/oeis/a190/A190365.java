package irvine.oeis.a190;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A190365 n + [n*r/s] + [n*t/s] + [n*u/s]; r=sqrt(2), s=1/r, t=sqrt(3), u=1/t.
 * @author Georg Fischer
 */
public class A190365 extends FloorSequence {

  private final CR mR = CR.SQRT2;
  private final CR mS = CR.ONE.divide(mR);
  private final CR mT = CR.THREE.sqrt();
  private final CR mU = CR.ONE.divide(mT);

  /** Construct the sequence */
  public A190365() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mR).divide(mS).floor()).add(CR.valueOf(mN).multiply(mT).divide(mS).floor()).add(CR.valueOf(mN).multiply(mU).divide(mS).floor());
  }

}
