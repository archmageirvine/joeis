package irvine.oeis.a190;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A190368 a(n) = n + [n*s/r] + [n*t/r] + [n*u/r]; r=sin(Pi/5), s=cos(Pi/5), t=sin(2*Pi/5), u=cos(2*Pi/5).
 * @author Georg Fischer
 */
public class A190368 extends FloorSequence {

  private final CR mR = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mS = CR.ONE.divide(CR.FIVE).multiply(CR.PI).cos();
  private final CR mT = CR.TWO.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mU = CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos();

  /** Construct the sequence */
  public A190368() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mS).divide(mR).floor()).add(CR.valueOf(mN).multiply(mT).divide(mR).floor()).add(CR.valueOf(mN).multiply(mU).divide(mR).floor());
  }

}
