package irvine.oeis.a190;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A190371 a(n) = n + [n*r/u] + [n*s/u] + [n*t/u]; r=sin(Pi/5), s=cos(Pi/5), t=sin(2*Pi/5), u=cos(2*Pi/5).
 * @author Georg Fischer
 */
public class A190371 extends FloorSequence {

  private final CR mR = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mS = CR.ONE.divide(CR.FIVE).multiply(CR.PI).cos();
  private final CR mT = CR.TWO.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mU = CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos();

  /** Construct the sequence. */
  public A190371() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mR).divide(mU).floor()).add(CR.valueOf(n).multiply(mS).divide(mU).floor()).add(CR.valueOf(n).multiply(mT).divide(mU).floor());
  }

}
