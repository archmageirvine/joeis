package irvine.oeis.a190;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A190372 a(n) = n + [n*s/r] + [n*t/r] + [n*u/r]; r=sin(Pi/5), s=1/r, t=sin(2*Pi/5), u=1/t.
 * @author Georg Fischer
 */
public class A190372 extends FloorSequence {

  private final CR mR = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mS = CR.ONE.divide(mR);
  private final CR mT = CR.TWO.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mU = CR.ONE.divide(mT);

  /** Construct the sequence */
  public A190372() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mS).divide(mR).floor()).add(CR.valueOf(n).multiply(mT).divide(mR).floor()).add(CR.valueOf(n).multiply(mU).divide(mR).floor());
  }

}
