package irvine.oeis.a190;
// Generated by gen_seq4.pl floorr at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;

/**
 * A190518 a(n) = n + [nr/w] + [ns/w] + [nt/w] + [nu/w] + [nv/w], where r=sin(x), s=cos(x), t=tan(x), u=csc(x), v=sec(x), w=cot(x), x=Pi/5.
 * @author Georg Fischer
 */
public class A190518 extends FloorSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;
  private final CR mR = CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mS = CR.ONE.divide(CR.FIVE).multiply(CR.PI).cos();
  private final CR mT = REALS.tan(CR.ONE.divide(CR.FIVE).multiply(CR.PI));
  private final CR mU = CR.ONE.divide(CR.ONE.divide(CR.FIVE).multiply(CR.PI).sin());
  private final CR mV = CR.ONE.divide(CR.ONE.divide(CR.FIVE).multiply(CR.PI).cos());
  private final CR mW = REALS.cot(CR.ONE.divide(CR.FIVE).multiply(CR.PI));

  /** Construct the sequence */
  public A190518() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mR).divide(mW).floor()).add(CR.valueOf(n).multiply(mS).divide(mW).floor()).add(CR.valueOf(n).multiply(mT).divide(mW).floor()).add(CR.valueOf(n).multiply(mU).divide(mW).floor()).add(CR.valueOf(n).multiply(mV).divide(mW).floor());
  }

}
