package irvine.oeis.a190;
// Generated by gen_seq4.pl floorr at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;

/**
 * A190523 a(n) = n + [nr/v] + [ns/v] + [nt/v] + [nu/v] + [nw/v], where r=sin(x), s=cos(x), t=tan(x), u=csc(x), v=sec(x), w=cot(x), x=2*Pi/5.
 * @author Georg Fischer
 */
public class A190523 extends FloorSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;
  private final CR mR = CR.TWO.divide(CR.FIVE).multiply(CR.PI).sin();
  private final CR mS = CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos();
  private final CR mT = REALS.tan(CR.TWO.divide(CR.FIVE).multiply(CR.PI));
  private final CR mU = CR.ONE.divide(CR.TWO.divide(CR.FIVE).multiply(CR.PI).sin());
  private final CR mV = CR.ONE.divide(CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos());
  private final CR mW = REALS.cot(CR.TWO.divide(CR.FIVE).multiply(CR.PI));

  /** Construct the sequence */
  public A190523() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mR).divide(mV).floor()).add(CR.valueOf(mN).multiply(mS).divide(mV).floor()).add(CR.valueOf(mN).multiply(mT).divide(mV).floor()).add(CR.valueOf(mN).multiply(mU).divide(mV).floor()).add(CR.valueOf(mN).multiply(mW).divide(mV).floor());
  }

}
