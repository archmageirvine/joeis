package irvine.oeis.a191;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A191422 Expansion of e.g.f. (1 + x + x^2)^x.
 * E.g.f.: (1 + x + x^2)^x
 * @author Georg Fischer
 */
public class A191422 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.log(RING.add(RING.onePlusXToTheN(1), RING.pow(RING.x(), 2, mN)), mN), RING.x(), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
