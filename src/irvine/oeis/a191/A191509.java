package irvine.oeis.a191;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A191509 E.g.f. exp(x*sqrt(1+sin(x)^2)).
 * E.g.f.: exp(x*sqrt(1+sin(x)^2))
 * @author Georg Fischer
 */
public class A191509 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.x(), RING.sqrt(RING.add(RING.one(), RING.pow(RING.sin(RING.x(), mN), 2, mN)), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
