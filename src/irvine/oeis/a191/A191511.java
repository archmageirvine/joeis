package irvine.oeis.a191;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A191511 E.g.f. 1 - cos(3*x)^(1/3). (even powers only).
 * E.g.f.: 1 - cos(3*x)^(1/3), even powers
 * @author Georg Fischer
 */
public class A191511 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.one(), RING.exp(RING.multiply(RING.log(RING.cos(RING.monomial(Q.THREE, 1), mN), mN), RING.series(RING.one(), RING.monomial(Q.THREE, 0), mN), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
