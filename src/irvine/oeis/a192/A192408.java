package irvine.oeis.a192;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A192408 Decimal expansion of the solution to x = sin( Pi/6 - x*sqrt(1 - x^2) ).
 * Equation; x-(sin(Pi/6-x*sqrt(1-x^2)))
 * @author Georg Fischer
 */
public class A192408 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A192408() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.subtract(CR.PI.divide(CR.SIX).subtract(x.multiply(CR.ONE.subtract(x.pow(2)).sqrt())).sin());
      }
    }.inverseMonotone(CR.valueOf(0.260), CR.valueOf(0.270)).execute(CR.ZERO));
  }
}
