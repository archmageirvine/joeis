package irvine.oeis.a192;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A192918 The decimal expansion of the real root of r^3 + r^2 + r - 1.
 * Equation; x^3+x^2+x-1
 * @author Georg Fischer
 */
public class A192918 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A192918() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(3).add(x.pow(CR.TWO)).add(x).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(0.533), CR.valueOf(0.554)).execute(CR.ZERO));
  }
}
