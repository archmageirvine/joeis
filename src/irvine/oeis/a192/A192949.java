package irvine.oeis.a192;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A192949 E.g.f. satisfies: A(x) = 1 + x*Sum_{n&gt;=0} (A(x)^2 - 1)^n/n!.
 * E.g.f.: 1 + reverse( x/exp(2*x + x^2) )
 * @author Georg Fischer
 */
public class A192949 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A192949() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.add(RING.one(), RING.reversion(RING.series(RING.x(), RING.exp(RING.add(RING.monomial(Q.TWO, 1), RING.pow(RING.x(), 2, n)), n), n), n));
  }
}
