package irvine.oeis.a196;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A196505 Decimal expansion of greatest x&gt;0 satisfying sin(1/x)=1/sqrt(1+x^2).
 * Equation; sin(1/x)-1/sqrt(1+x^2)
 * @author Georg Fischer
 */
public class A196505 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A196505() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).sin().subtract(CR.ONE.divide(CR.ONE.add(x.pow(2)).sqrt()));
      }
    }.inverseMonotone(CR.valueOf(0.483), CR.valueOf(0.503)).execute(CR.ZERO));
  }
}
