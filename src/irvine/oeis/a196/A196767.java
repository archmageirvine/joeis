package irvine.oeis.a196;
// Generated by gen_seq4.pl decsolv at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A196767 Decimal expansion of the least x &gt; 0 satisfying 1 = x*sin(x - Pi/2), or, equivalently, -1 = x*cos(x).
 * Equation; 1-(x*sin(x-Pi/2))
 * @author Georg Fischer
 */
public class A196767 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A196767() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(x.multiply(x.subtract(CR.PI.divide(CR.TWO)).sin()));
      }
    }.inverseMonotone(CR.valueOf(2.032), CR.valueOf(2.114)).execute(CR.ZERO));
  }
}
