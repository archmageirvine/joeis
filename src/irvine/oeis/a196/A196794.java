package irvine.oeis.a196;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A196794 a(n) = Sum_{k=0..n} binomial(n,k)*2^k*(k+1)^(n-k).
 * E.g.f.: exp(x + 2*x*exp(x))
 * @author Georg Fischer
 */
public class A196794 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.x(), RING.multiply(RING.monomial(Q.TWO, 1), RING.exp(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
