package irvine.oeis.a196;
// Generated by gen_seq4.pl decsolv at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A196826 Decimal expansion of the least x&gt;0 satisfying 1/(1+x^2)=2*sin(x).
 * Equation; 1/(1+x^2)-(2*sin(x))
 * @author Georg Fischer
 */
public class A196826 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A196826() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(CR.ONE.add(x.square())).subtract(CR.TWO.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(0.426), CR.valueOf(0.443)).execute(CR.ZERO));
  }
}
