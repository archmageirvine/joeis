package irvine.oeis.a197;
// generated by patch_offset.pl at 2022-08-17 15:41

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197252 Decimal expansion of least x&gt;0 having sin(x)=(sin 6x)^2.
 * Equation; sin(x)-(sin(6*x))^2
 * @author Georg Fischer
 */
public class A197252 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197252() {
    super(0, new UnaryCrFunction() {
      @Override
      public CR execute(final CR x) {
        return x.sin().subtract(CR.SIX.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.027), CR.valueOf(0.029)).execute(CR.ZERO));
  }
}
