package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197261 Decimal expansion of least x&gt;0 having sin(3x)=(sin 2x)^2.
 * Equation; sin(3*x)-(sin(2*x))^2
 * @author Georg Fischer
 */
public class A197261 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197261() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x).sin().subtract(CR.TWO.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.637), CR.valueOf(0.663)).execute(CR.ZERO));
  }
}
