package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197264 Decimal expansion of least x&gt;0 having sin(3x) = sin(6x)^2.
 * Equation; sin(3*x)-(sin(6*x))^2
 * @author Georg Fischer
 */
public class A197264 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197264() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x).sin().subtract(CR.SIX.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.089), CR.valueOf(0.093)).execute(CR.ZERO));
  }
}
