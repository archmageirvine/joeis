package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 10:07

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197267 Decimal expansion of least x&gt;0 having sin(4x) = (sin x)^2.
 * Equation; sin(4*x)-(sin(1*x))^2
 * @author Georg Fischer
 */
public class A197267 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197267() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.FOUR.multiply(x).sin().subtract(CR.ONE.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.5), CR.valueOf(0.8)).execute(CR.ZERO));
  }
}
