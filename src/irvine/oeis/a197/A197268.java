package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 10:39

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197268 Decimal expansion of least x>0 having sin(4x)=(sin 3x)^2.
 * Equation; sin(4*x)-(sin(3*x))^2
 * @author Georg Fischer
 */
public class A197268 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A197268() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.FOUR.multiply(x).sin().subtract(CR.THREE.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.3), CR.valueOf(0.6)).execute(CR.ZERO));
  }
}
