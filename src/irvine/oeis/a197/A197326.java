package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197326 Decimal expansion of least x &gt; 0 having sin(x) = sin(Pi*x/3)^2.
 * Equation; sin(x)-(sin(Pi/3*x))^2
 * @author Georg Fischer
 */
public class A197326 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197326() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.sin().subtract(CR.PI.divide(CR.THREE).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.324), CR.valueOf(1.378)).execute(CR.ZERO));
  }
}
