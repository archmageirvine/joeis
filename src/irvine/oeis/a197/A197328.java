package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197328 Decimal expansion of least x &gt; 0 having sin(x) = sin(Pi*x/6)^2.
 * Equation; sin(x)-(sin(Pi/6*x))^2
 * @author Georg Fischer
 */
public class A197328 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197328() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.sin().subtract(CR.PI.divide(CR.SIX).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(2.130), CR.valueOf(2.216)).execute(CR.ZERO));
  }
}
