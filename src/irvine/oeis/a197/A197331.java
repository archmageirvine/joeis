package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197331 Decimal expansion of least x &gt; 0 having sin(2*x) = sin(Pi*x/6)^2.
 * Equation; sin(2*x)-(sin(Pi/6*x))^2
 * @author Georg Fischer
 */
public class A197331 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197331() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).sin().subtract(CR.PI.divide(CR.SIX).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.325), CR.valueOf(1.379)).execute(CR.ZERO));
  }
}
