package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197332 Decimal expansion of least x &gt; 0 having sin(3*x) = sin(Pi*x/3)^2.
 * Equation; sin(3*x)-(sin(Pi/3*x))^2
 * @author Georg Fischer
 */
public class A197332 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197332() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x).sin().subtract(CR.PI.divide(CR.THREE).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.820), CR.valueOf(0.853)).execute(CR.ZERO));
  }
}
