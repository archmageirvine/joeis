package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197333 Decimal expansion of least x &gt; 0 having sin(3*x) = sin(Pi*x/4)^2.
 * Equation; sin(3*x)-(sin(Pi/4*x))^2
 * @author Georg Fischer
 */
public class A197333 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197333() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x).sin().subtract(CR.PI.divide(CR.FOUR).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.884), CR.valueOf(0.920)).execute(CR.ZERO));
  }
}
