package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197382 Decimal expansion of least x &gt; 0 having sin(Pi*x/4) = sin(Pi*x/6)^2.
 * Equation; sin(Pi/4*x)-(sin(Pi/6*x))^2
 * @author Georg Fischer
 */
public class A197382 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197382() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.divide(CR.FOUR).multiply(x).sin().subtract(CR.PI.divide(CR.SIX).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(2.433), CR.valueOf(2.533)).execute(CR.ZERO));
  }
}
