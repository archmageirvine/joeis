package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197392 Decimal expansion of least x &gt; 0 having sin(Pi*x/4) = sin(3*x)^2.
 * Equation; sin(Pi/4*x)-(sin(3*x))^2
 * @author Georg Fischer
 */
public class A197392 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A197392() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.divide(CR.FOUR).multiply(x).sin().subtract(CR.THREE.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.088), CR.valueOf(0.091)).execute(CR.ZERO));
  }
}
