package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197393 Decimal expansion of least x &gt; 0 having sin(Pi*x/4) = sin(x/2)^2.
 * Equation; sin(Pi/4*x)-(sin(1/2*x))^2
 * @author Georg Fischer
 */
public class A197393 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197393() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.divide(CR.FOUR).multiply(x).sin().subtract(CR.ONE.divide(CR.TWO).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(2.487), CR.valueOf(2.589)).execute(CR.ZERO));
  }
}
