package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197476 Decimal expansion of least x&gt;0 having cos(x)=(cos 2x)^2.
 * Equation; cos(x)-(cos(2*x))^2
 * @author Georg Fischer
 */
public class A197476 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A197476() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.cos().subtract(CR.TWO.multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.114), CR.valueOf(1.160)).execute(CR.ZERO));
  }
}
