package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197480 Decimal expansion of least x&gt;0 having cos(2x)=(cos 4x)^2.
 * Equation; cos(2*x)-(cos(4*x))^2
 * @author Georg Fischer
 */
public class A197480 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197480() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).cos().subtract(CR.FOUR.multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.557), CR.valueOf(0.580)).execute(CR.ZERO));
  }
}
