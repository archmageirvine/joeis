package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197482 Decimal expansion of least x&gt;0 having cos(3x)=(cos 2x)^2.
 * Equation; cos(3*x)-(cos(2*x))^2
 * @author Georg Fischer
 */
public class A197482 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197482() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x).cos().subtract(CR.TWO.multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.806), CR.valueOf(1.880)).execute(CR.ZERO));
  }
}
