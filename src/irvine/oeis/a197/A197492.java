package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197492 Decimal expansion of least x &gt; 0 having cos(x) = cos(Pi*x)^2.
 * Equation; cos(x)-(cos(Pi*x))^2
 * @author Georg Fischer
 */
public class A197492 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197492() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.cos().subtract(CR.PI.multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.795), CR.valueOf(0.828)).execute(CR.ZERO));
  }
}
