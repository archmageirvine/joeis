package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197493 Decimal expansion of least x &gt; 0 having cos(x) = cos(Pi*x/2)^2.
 * Equation; cos(x)-(cos(Pi/2*x))^2
 * @author Georg Fischer
 */
public class A197493 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197493() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.cos().subtract(CR.PI.divide(CR.TWO).multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.299), CR.valueOf(1.353)).execute(CR.ZERO));
  }
}
