package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197506 Decimal expansion of least x &gt; 0 having cos(x) = cos(2*Pi*x/3)^2.
 * Equation; cos(x)-(cos(2*Pi/3*x))^2
 * @author Georg Fischer
 */
public class A197506 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A197506() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.cos().subtract(CR.TWO.multiply(CR.PI).divide(CR.THREE).multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.079), CR.valueOf(1.123)).execute(CR.ZERO));
  }
}
