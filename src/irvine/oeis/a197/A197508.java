package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197508 Decimal expansion of least x &gt; 0 having cos(2*x) = cos(3*Pi*x/2)^2.
 * Equation; cos(2*x)-(cos(3*Pi/2*x))^2
 * @author Georg Fischer
 */
public class A197508 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197508() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).cos().subtract(CR.THREE.multiply(CR.PI).divide(CR.TWO).multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.496), CR.valueOf(0.516)).execute(CR.ZERO));
  }
}
