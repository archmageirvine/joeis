package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197514 Decimal expansion of least x &gt; 0 having cos(2*x) = cos(Pi*x/6)^2.
 * Equation; cos(2*x)-(cos(Pi/6*x))^2
 * @author Georg Fischer
 */
public class A197514 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197514() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).cos().subtract(CR.PI.divide(CR.SIX).multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(2.355), CR.valueOf(2.451)).execute(CR.ZERO));
  }
}
