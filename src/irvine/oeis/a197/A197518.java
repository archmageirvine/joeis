package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197518 Decimal expansion of least x&gt;0 having cos(2*Pi*x)=(cos x)^2.
 * Equation; cos(2*Pi*x)-(cos(x))^2
 * @author Georg Fischer
 */
public class A197518 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197518() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(CR.PI).multiply(x).cos().subtract(x.cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.809), CR.valueOf(0.842)).execute(CR.ZERO));
  }
}
