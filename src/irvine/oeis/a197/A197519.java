package irvine.oeis.a197;
// generated by patch_offset.pl at 2022-08-17 15:41

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197519 Decimal expansion of least x&gt;0 having cos(2*Pi*x)=(cos 2x)^2.
 * Equation; cos(2*Pi*x)-(cos(2*x))^2
 * @author Georg Fischer
 */
public class A197519 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197519() {
    super(0, new UnaryCrFunction() {
      @Override
      public CR execute(final CR x) {
        return CR.TWO.multiply(CR.PI).multiply(x).cos().subtract(CR.TWO.multiply(x).cos().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.736), CR.valueOf(0.766)).execute(CR.ZERO));
  }
}
