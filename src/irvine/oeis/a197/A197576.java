package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197576 Decimal expansion of least x &gt; 0 having cos(Pi*x) = cos(x)^2.
 * Equation; sin(Pi*x)-(sin(x))^2
 * @author Georg Fischer
 */
public class A197576 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197576() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.multiply(x).sin().subtract(x.sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.804), CR.valueOf(0.837)).execute(CR.ZERO));
  }
}
