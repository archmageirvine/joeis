package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197577 Decimal expansion of least x &gt; 0 having cos(Pi*x) = cos(2*x)^2.
 * Equation; sin(Pi*x)-(sin(2*x))^2
 * @author Georg Fischer
 */
public class A197577 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197577() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.multiply(x).sin().subtract(CR.TWO.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.622), CR.valueOf(0.647)).execute(CR.ZERO));
  }
}
