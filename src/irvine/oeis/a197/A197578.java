package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197578 Decimal expansion of least x &gt; 0 having cos(Pi*x) = cos(3*x)^2.
 * Equation; sin(Pi*x)-(sin(3*x))^2
 * @author Georg Fischer
 */
public class A197578 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197578() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.PI.multiply(x).sin().subtract(CR.THREE.multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.503), CR.valueOf(0.524)).execute(CR.ZERO));
  }
}
