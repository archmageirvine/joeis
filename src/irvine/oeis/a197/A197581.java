package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197581 Decimal expansion of least x &gt; 0 having cos(Pi*x/2) = cos(x/2)^2.
 * Equation; sin(Pi/2*x)-(sin(1/2*x))^2
 * @author Georg Fischer
 */
public class A197581 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197581() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.PI.divide(CR.TWO).multiply(x).sin().subtract(CR.ONE.divide(CR.TWO).multiply(x).sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(1.607), CR.valueOf(1.673)).execute(CR.ZERO));
  }
}
