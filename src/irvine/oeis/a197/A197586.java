package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-28 18:47

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197586 Decimal expansion of least x&gt;0 having cos(3*Pi*x) = (cos x)^2.
 * Equation; sin(3*Pi*x)-(sin(x))^2
 * @author Georg Fischer
 */
public class A197586 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197586() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(CR.PI).multiply(x).sin().subtract(x.sin().pow(CR.TWO));
      }
    }.inverseMonotone(CR.valueOf(0.316), CR.valueOf(0.329)).execute(CR.ZERO));
  }
}
