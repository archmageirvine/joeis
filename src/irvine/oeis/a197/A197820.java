package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197820 Decimal expansion of greatest x having x^2-2x=-cos(x).
 * Equation; x^2-2x-(-cos(x))
 * @author Georg Fischer
 */
public class A197820 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197820() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).subtract(CR.TWO.multiply(x)).subtract(CR.ZERO.subtract(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(2.241), CR.valueOf(2.333)).execute(CR.ZERO));
  }
}
