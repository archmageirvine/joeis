package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A197821 Decimal expansion of least x &gt; 0 having sin(2*x) = Pi*sin(2*Pi*x).
 * Equation; sin(2*x)-Pi*sin(2*Pi*x)
 * @author Georg Fischer
 */
public class A197821 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197821() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).sin().subtract(CR.PI.multiply(CR.TWO.multiply(CR.PI).multiply(x).sin()));
      }
    }.inverseMonotone(CR.valueOf(0.450), CR.valueOf(0.468)).execute(CR.ZERO));
  }
}
