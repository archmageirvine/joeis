package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197833 Decimal expansion of least x &gt; 0 having sin(2*x) = 3*Pi*sin(3*Pi*x).
 * Equation; sin(2*x)-3*Pi*sin(6*Pi*x)
 * @author Georg Fischer
 */
public class A197833 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197833() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).sin().subtract(CR.THREE.multiply(CR.PI).multiply(CR.SIX.multiply(CR.PI).multiply(x).sin()));
      }
    }.inverseMonotone(CR.valueOf(0.162), CR.valueOf(0.168)).execute(CR.ZERO));
  }
}
