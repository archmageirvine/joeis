package irvine.oeis.a197;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A197845 Decimal expansion of least x having x^2+2x=3*cos(x).
 * Equation; x^2+2*x-3*cos(x)
 * @author Georg Fischer
 */
public class A197845 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A197845() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.square().add(CR.TWO.multiply(x)).subtract(CR.THREE.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(-1.763), CR.valueOf(-1.693)).execute(CR.ZERO).negate());
  }
}
