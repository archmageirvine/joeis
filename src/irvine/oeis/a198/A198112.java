package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198112 Decimal expansion of least x having 2*x^2+x=cos(x).
 * Equation; 2*x^2+x-cos(x)
 * @author Georg Fischer
 */
public class A198112 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198112() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).add(x).subtract(x.cos());
      }
    }.inverseMonotone(CR.valueOf(-0.888), CR.valueOf(-0.853)).execute(CR.ZERO).negate());
  }
}
