package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198116 Decimal expansion of least x having 2*x^2+x=3*cos(x).
 * Equation; 2*x^2+x-3*cos(x)
 * @author Georg Fischer
 */
public class A198116 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198116() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).add(x).subtract(CR.THREE.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(-1.128), CR.valueOf(-1.084)).execute(CR.ZERO).negate());
  }
}
