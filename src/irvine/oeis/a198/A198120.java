package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198120 Decimal expansion of least x having 2*x^2-3x=-cos(x).
 * Equation; 2*x^2-3x-(-cos(x))
 * @author Georg Fischer
 */
public class A198120 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198120() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).subtract(CR.THREE.multiply(x)).subtract(CR.ZERO.subtract(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(0.415), CR.valueOf(0.432)).execute(CR.ZERO));
  }
}
