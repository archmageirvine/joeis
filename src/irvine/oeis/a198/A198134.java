package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198134 Decimal expansion of least x having 2*x^2+3x=4*cos(x).
 * Equation; 2*x^2+3*x-4*cos(x)
 * @author Georg Fischer
 */
public class A198134 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198134() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(CR.THREE.multiply(x)).subtract(CR.FOUR.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(-1.57), CR.valueOf(-1.508)).execute(CR.ZERO).negate());
  }
}
