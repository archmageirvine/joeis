package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198140 Decimal expansion of least x having x^2-2x=-3*cos(x). Decimal expansion of greatest x having x^2-2x=-3*cos(x).
 * Equation; x^2-2x-(-3*cos(x))
 * @author Georg Fischer
 */
public class A198140 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198140() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.square().subtract(CR.TWO.multiply(x)).subtract(CR.THREE.multiply(x.cos()).negate());
      }
    }.inverseMonotone(CR.valueOf(1.228), CR.valueOf(1.278)).execute(CR.ZERO));
  }
}
