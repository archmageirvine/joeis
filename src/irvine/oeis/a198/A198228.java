package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198228 Decimal expansion of least x having 3*x^2+2x=4*cos(x).
 * Equation; 3*x^2+2*x-4*cos(x)
 * @author Georg Fischer
 */
public class A198228 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198228() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.square()).add(CR.TWO.multiply(x)).subtract(CR.FOUR.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(-1.169), CR.valueOf(-1.123)).execute(CR.ZERO).negate());
  }
}
