package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 18:13

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198232 Decimal expansion of least x having 3*x^2+3x=2*cos(x).
 * Equation; 3*x^2+3*x-2*cos(x)
 * @author Georg Fischer
 */
public class A198232 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198232() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).add(CR.THREE.multiply(x)).subtract(CR.TWO.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(-1.224), CR.valueOf(-1.176)).execute(CR.ZERO).negate());
  }
}
