package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198235 Decimal expansion of greatest x having 3*x^2+3x=4*cos(x).
 * Equation; 3*x^2+3x-(4*cos(x))
 * @author Georg Fischer
 */
public class A198235 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A198235() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).add(CR.THREE.multiply(x)).subtract(CR.FOUR.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(0.633), CR.valueOf(0.659)).execute(CR.ZERO));
  }
}
