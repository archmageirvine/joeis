package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198360 Decimal expansion of greatest x having 4*x^2+2x=3*cos(x).
 * Equation; 4*x^2+2*x-(3*cos(x))
 * @author Georg Fischer
 */
public class A198360 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A198360() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.FOUR.multiply(x.pow(2)).add(CR.TWO.multiply(x)).subtract(CR.THREE.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(0.569), CR.valueOf(0.592)).execute(CR.ZERO));
  }
}
