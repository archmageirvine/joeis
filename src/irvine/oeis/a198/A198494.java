package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198494 Decimal expansion of x&gt;0 having 2x^2+x=3*sin(x).
 * Equation; 2x^2+x-(3*sin(x))
 * @author Georg Fischer
 */
public class A198494 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198494() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).add(x).subtract(CR.THREE.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(0.816), CR.valueOf(0.849)).execute(CR.ZERO));
  }
}
