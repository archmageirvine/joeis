package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198546 Decimal expansion of x&gt;0 having 2*x^2-x=sin(x).
 * Equation; 2*x^2-x-(sin(x))
 * @author Georg Fischer
 */
public class A198546 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198546() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).subtract(x).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(0.912), CR.valueOf(0.949)).execute(CR.ZERO));
  }
}
