package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198547 Decimal expansion of x&gt;0 having 2*x^2-x=2*sin(x).
 * Equation; 2*x^2-x-(2*sin(x))
 * @author Georg Fischer
 */
public class A198547 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198547() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).subtract(x).subtract(CR.TWO.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(1.231), CR.valueOf(1.281)).execute(CR.ZERO));
  }
}
