package irvine.oeis.a198;
// generated by patch_offset.pl at 2022-08-17 15:41

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A198560 Decimal expansion of x &gt; 0 having 3*x^2-2*x = sin(x).
 * Equation; 3*x^2-2*x-sin(x)
 * @author Georg Fischer
 */
public class A198560 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198560() {
    super(0, new UnaryCrFunction() {
      @Override
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.square()).subtract(CR.TWO.multiply(x)).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(0.933), CR.valueOf(0.971)).execute(CR.ZERO));
  }
}
