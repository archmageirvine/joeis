package irvine.oeis.a198;
// Generated by gen_seq4.pl decsolvn at 2021-08-01 19:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A198608 Decimal expansion of -x where x&lt;0 satisfies 2*x^2+3x=sin(x).
 * Equation; 2*x^2+3*x-sin(x)
 * @author Georg Fischer
 */
public class A198608 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A198608() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).add(CR.THREE.multiply(x)).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(-1.115), CR.valueOf(-1.071)).execute(CR.ZERO).negate());
  }
}
