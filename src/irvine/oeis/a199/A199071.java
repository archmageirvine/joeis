package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-01 19:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199071 Decimal expansion of x&lt;0 satisfying 2*x^2+3*sin(x)=1.
 * Equation; 2*x^2+3*sin(x)-1
 * @author Georg Fischer
 */
public class A199071 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199071() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).add(CR.THREE.multiply(x.sin())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-1.435), CR.valueOf(-1.379)).execute(CR.ZERO).negate());
  }
}
