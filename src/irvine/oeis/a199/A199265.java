package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199265 Decimal expansion of x&lt;0 satisfying 2*x^2+x*cos(x)=2.
 * Equation; 2*x^2+x*cos(x)-2
 * @author Georg Fischer
 */
public class A199265 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199265() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(x.multiply(x.cos())).subtract(CR.TWO);
      }
    }.inverseMonotone(CR.valueOf(-1.137), CR.valueOf(-1.093)).execute(CR.ZERO).negate());
  }
}
