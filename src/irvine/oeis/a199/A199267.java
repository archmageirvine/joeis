package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199267 Decimal expansion of x &lt; 0 satisfying 2*x^2 + x*cos(x) = 3.
 * Equation; 2*x^2+x*cos(x)-3
 * @author Georg Fischer
 */
public class A199267 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199267() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(x.multiply(x.cos())).subtract(CR.THREE);
      }
    }.inverseMonotone(CR.valueOf(-1.32), CR.valueOf(-1.268)).execute(CR.ZERO).negate());
  }
}
