package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199270 Decimal expansion of x &gt; 0 satisfying 2*x^2 + 2*x*cos(x) = 1.
 * Equation; 2*x^2+2*x*cos(x)-1
 * @author Georg Fischer
 */
public class A199270 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199270() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(CR.TWO.multiply(x).multiply(x.cos())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(0.374), CR.valueOf(0.389)).execute(CR.ZERO));
  }
}
