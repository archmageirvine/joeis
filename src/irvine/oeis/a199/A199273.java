package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199273 Decimal expansion of x&lt;0 satisfying 2*x^2+3*x*cos(x)=1.
 * Equation; 2*x^2+3*x*cos(x)-1
 * @author Georg Fischer
 */
public class A199273 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199273() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(CR.THREE.multiply(x).multiply(x.cos())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-1.134), CR.valueOf(-1.09)).execute(CR.ZERO).negate());
  }
}
