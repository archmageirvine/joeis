package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199290 Decimal expansion of x &gt; 0 satisfying 3*x^2 + 2*x*cos(x) = 3.
 * Equation; 3*x^2+2*x*cos(x)-3
 * @author Georg Fischer
 */
public class A199290 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199290() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.square()).add(CR.TWO.multiply(x).multiply(x.cos())).subtract(CR.THREE);
      }
    }.inverseMonotone(CR.valueOf(0.777), CR.valueOf(0.809)).execute(CR.ZERO));
  }
}
