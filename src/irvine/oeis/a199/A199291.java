package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199291 Decimal expansion of x &lt; 0 satisfying 3*x^2 + 3*x*cos(x) = 1.
 * Equation; 3*x^2+3*x*cos(x)-1
 * @author Georg Fischer
 */
public class A199291 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199291() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).add(CR.THREE.multiply(x).multiply(x.cos())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-0.961), CR.valueOf(-0.923)).execute(CR.ZERO).negate());
  }
}
