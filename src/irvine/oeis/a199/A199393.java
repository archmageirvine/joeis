package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199393 Decimal expansion of x&gt;0 satisfying 3*x^2+3*x*sin(x)=1.
 * Equation; 3*x^2+3*x*sin(x)-(1)
 * @author Georg Fischer
 */
public class A199393 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199393() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.square()).add(CR.THREE.multiply(x).multiply(x.sin())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(0.403), CR.valueOf(0.419)).execute(CR.ZERO));
  }
}
