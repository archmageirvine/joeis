package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199395 Decimal expansion of x&gt;0 satisfying 3*x^2+3*x*sin(x)=2.
 * Equation; 3*x^2+3*x*sin(x)-(2)
 * @author Georg Fischer
 */
public class A199395 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A199395() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).add(CR.THREE.multiply(x).multiply(x.sin())).subtract(CR.TWO);
      }
    }.inverseMonotone(CR.valueOf(0.574), CR.valueOf(0.597)).execute(CR.ZERO));
  }
}
