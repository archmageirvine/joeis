package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199444 Decimal expansion of x&gt;0 satisfying 2*x^2+3*x*sin(x)=2*cos(x).
 * Equation; 2*x^2+3*x*sin(x)-(2*cos(x))
 * @author Georg Fischer
 */
public class A199444 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199444() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).add(CR.THREE.multiply(x).multiply(x.sin())).subtract(CR.TWO.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(0.575), CR.valueOf(0.599)).execute(CR.ZERO));
  }
}
