package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199447 Decimal expansion of x&gt;0 satisfying 3*x^2+x*sin(x)=2*cos(x).
 * Equation; 3*x^2+x*sin(x)-(2*cos(x))
 * @author Georg Fischer
 */
public class A199447 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199447() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).add(x.multiply(x.sin())).subtract(CR.TWO.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(0.626), CR.valueOf(0.652)).execute(CR.ZERO));
  }
}
