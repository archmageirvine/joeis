package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199457 Decimal expansion of greatest x satisfying x^2-2*x*sin(x)=-3*cos(x).
 * Equation; x^2-2*x*sin(x)-(-3*cos(x))
 * @author Georg Fischer
 */
public class A199457 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199457() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.square().subtract(CR.TWO.multiply(x).multiply(x.sin())).subtract(CR.ZERO.subtract(CR.THREE.multiply(x.cos())));
      }
    }.inverseMonotone(CR.valueOf(2.286), CR.valueOf(2.380)).execute(CR.ZERO));
  }
}
