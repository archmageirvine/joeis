package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199459 Decimal expansion of greatest x satisfying x^2-2*x*sin(x)=-cos(x).
 * Equation; x^2-2*x*sin(x)-(-cos(x))
 * @author Georg Fischer
 */
public class A199459 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199459() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.square().subtract(CR.TWO.multiply(x).multiply(x.sin())).subtract(x.cos().negate());
      }
    }.inverseMonotone(CR.valueOf(1.977), CR.valueOf(2.057)).execute(CR.ZERO));
  }
}
