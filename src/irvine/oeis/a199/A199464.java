package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199464 Decimal expansion of greatest x satisfying x^2-3*x*sin(x)=-3*cos(x).
 * Equation; x^2-3*x*sin(x)-(-3*cos(x))
 * @author Georg Fischer
 */
public class A199464 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199464() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.square().subtract(CR.THREE.multiply(x).multiply(x.sin())).subtract(CR.THREE.multiply(x.cos()).negate());
      }
    }.inverseMonotone(CR.valueOf(1.000), CR.valueOf(1.040)).execute(CR.ZERO));
  }
}
