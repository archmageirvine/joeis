package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199465 Decimal expansion of greatest x satisfying x^2-3*x*sin(x)=-2*cos(x).
 * Equation; x^2-3*x*sin(x)-(-2*cos(x))
 * @author Georg Fischer
 */
public class A199465 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A199465() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).subtract(CR.THREE.multiply(x).multiply(x.sin())).subtract(CR.ZERO.subtract(CR.TWO.multiply(x.cos())));
      }
    }.inverseMonotone(CR.valueOf(2.429), CR.valueOf(2.529)).execute(CR.ZERO));
  }
}
