package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199471 Decimal expansion of x&gt;0 satisfying 2*x^2-x*sin(x)=cos(x).
 * Equation; 2*x^2-x*sin(x)-(cos(x))
 * @author Georg Fischer
 */
public class A199471 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199471() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).subtract(x.multiply(x.sin())).subtract(x.cos());
      }
    }.inverseMonotone(CR.valueOf(0.780), CR.valueOf(0.812)).execute(CR.ZERO));
  }
}
