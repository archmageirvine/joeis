package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199473 Decimal expansion of x&gt;0 satisfying 2*x^2-x*sin(x)=3*cos(x).
 * Equation; 2*x^2-x*sin(x)-(3*cos(x))
 * @author Georg Fischer
 */
public class A199473 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A199473() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).subtract(x.multiply(x.sin())).subtract(CR.THREE.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(1.064), CR.valueOf(1.108)).execute(CR.ZERO));
  }
}
