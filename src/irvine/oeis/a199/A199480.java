package irvine.oeis.a199;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A199480 E.g.f. exp(x*(1+log(1+x))/(1-log(1+x))).
 * E.g.f.: exp(x*(1+log(1+x))/(1-log(1+x)))
 * @author Georg Fischer
 */
public class A199480 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.multiply(RING.x(), RING.add(RING.one(), RING.log(RING.onePlusXToTheN(1), mN)), mN), RING.subtract(RING.one(), RING.log(RING.onePlusXToTheN(1), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
