package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199510 Decimal expansion of x>0 satisfying 3*x^2-2*x*sin(x)=3*cos(x).
 * Equation; 3*x^2-2*x*sin(x)-(3*cos(x))
 * @author Georg Fischer
 */
public class A199510 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A199510() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).subtract(CR.TWO.multiply(x).multiply(x.sin())).subtract(CR.THREE.multiply(x.cos()));
      }
    }.inverseMonotone(CR.valueOf(1.030), CR.valueOf(1.072)).execute(CR.ZERO));
  }
}
