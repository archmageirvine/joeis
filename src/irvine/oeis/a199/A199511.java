package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199511 Decimal expansion of x&gt;0 satisfying 3*x^2-3*x*sin(x)=cos(x).
 * Equation; 3*x^2-3*x*sin(x)-(cos(x))
 * @author Georg Fischer
 */
public class A199511 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199511() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).subtract(CR.THREE.multiply(x).multiply(x.sin())).subtract(x.cos());
      }
    }.inverseMonotone(CR.valueOf(1.003), CR.valueOf(1.043)).execute(CR.ZERO));
  }
}
