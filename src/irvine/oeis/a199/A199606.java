package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199606 Decimal expansion of greatest x satisfying x^2+3*x*cos(x)=sin(x).
 * Equation; x^2+3*x*cos(x)-(sin(x))
 * @author Georg Fischer
 */
public class A199606 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199606() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.THREE.multiply(x).multiply(x.cos())).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(2.957), CR.valueOf(3.077)).execute(CR.ZERO));
  }
}
