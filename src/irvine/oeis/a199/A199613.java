package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199613 Decimal expansion of least x satisfying x^2+4*x*cos(x)=sin(x) (negated).
 * Equation; x^2+4*x*cos(x)-sin(x)
 * @author Georg Fischer
 */
public class A199613 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199613() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.FOUR.multiply(x).multiply(x.cos())).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(-1.099), CR.valueOf(-1.055)).execute(CR.ZERO).negate());
  }
}
