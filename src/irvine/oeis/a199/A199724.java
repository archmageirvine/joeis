package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199724 Decimal expansion of x&gt;0 satisfying x^2-2*x*cos(x)=3*sin(x).
 * Equation; x^2-2*x*cos(x)-(3*sin(x))
 * @author Georg Fischer
 */
public class A199724 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199724() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).subtract(CR.TWO.multiply(x).multiply(x.cos())).subtract(CR.THREE.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(1.618), CR.valueOf(1.684)).execute(CR.ZERO));
  }
}
