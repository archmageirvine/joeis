package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199789 Decimal expansion of x &gt; 0 satisfying 3*x^2 - 3*x*cos(x) = sin(x).
 * Equation; 3*x^2-3*x*cos(x)-sin(x)
 * @author Georg Fischer
 */
public class A199789 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199789() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.pow(2)).subtract(CR.THREE.multiply(x).multiply(x.cos())).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(0.888), CR.valueOf(0.924)).execute(CR.ZERO));
  }
}
