package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199793 Decimal expansion of x &gt; 0 satisfying 3*x^2 - 2*x*cos(x) = sin(x).
 * Equation; 3*x^2-2*x*cos(x)-sin(x)
 * @author Georg Fischer
 */
public class A199793 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199793() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.THREE.multiply(x.square()).subtract(CR.TWO.multiply(x).multiply(x.cos())).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(0.761), CR.valueOf(0.792)).execute(CR.ZERO));
  }
}
