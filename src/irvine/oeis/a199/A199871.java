package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199871 Decimal expansion of x &gt; 0 satisfying 4*x^2 - 3*x*cos(x) = sin(x).
 * Equation; 4*x^2-3*x*cos(x)-sin(x)
 * @author Georg Fischer
 */
public class A199871 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199871() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.FOUR.multiply(x.pow(2)).subtract(CR.THREE.multiply(x).multiply(x.cos())).subtract(x.sin());
      }
    }.inverseMonotone(CR.valueOf(0.751), CR.valueOf(0.782)).execute(CR.ZERO));
  }
}
