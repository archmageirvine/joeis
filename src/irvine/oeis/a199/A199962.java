package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A199962 Decimal expansion of greatest x satisfying x^2 + 3*cos(x) = 4*sin(x).
 * Equation; x^2+3*cos(x)-4*sin(x)
 * @author Georg Fischer
 */
public class A199962 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199962() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.THREE.multiply(x.cos())).subtract(CR.FOUR.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(2.190), CR.valueOf(2.280)).execute(CR.ZERO));
  }
}
