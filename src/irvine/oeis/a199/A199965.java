package irvine.oeis.a199;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A199965 Decimal expansion of least x satisfying x^2 + 4*cos(x) = 4*sin(x).
 * Equation; x^2+4*cos(x)-4*sin(x)
 * @author Georg Fischer
 */
public class A199965 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A199965() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.FOUR.multiply(x.cos())).subtract(CR.FOUR.multiply(x.sin()));
      }
    }.inverseMonotone(CR.valueOf(0.924), CR.valueOf(0.962)).execute(CR.ZERO));
  }
}
