package irvine.oeis.a200;
// Generated by gen_seq4.pl decsolvr at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A200493 Decimal expansion of least x&gt;0 satisfying 2*x^2-x+1=tan(x).
 * Equation; 2*x^2-x+1-(tan(x))
 * @author Georg Fischer
 */
public class A200493 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A200493() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.square()).subtract(x).add(CR.ONE).subtract(REALS.tan(x));
      }
    }.inverseMonotone(CR.valueOf(1.201), CR.valueOf(1.250)).execute(CR.ZERO));
  }
}
