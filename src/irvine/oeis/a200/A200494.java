package irvine.oeis.a200;
// Generated by gen_seq4.pl decsolvr at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A200494 Decimal expansion of least x&gt;0 satisfying 2*x^2-x+2=tan(x).
 * Equation; 2*x^2-x+2-(tan(x))
 * @author Georg Fischer
 */
public class A200494 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A200494() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(2)).subtract(x).add(CR.TWO).subtract(REALS.tan(x));
      }
    }.inverseMonotone(CR.valueOf(1.312), CR.valueOf(1.366)).execute(CR.ZERO));
  }
}
