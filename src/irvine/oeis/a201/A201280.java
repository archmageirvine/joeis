package irvine.oeis.a201;
// Generated by gen_seq4.pl decsolvr at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A201280 Decimal expansion of x satisfying x^2 + 1 = cot(x) and 0 &lt; x &lt; Pi.
 * Equation; x^2+1-cot(x)
 * @author Georg Fischer
 */
public class A201280 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A201280() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.ONE).subtract(REALS.cot(x));
      }
    }.inverseMonotone(CR.valueOf(0.611), CR.valueOf(0.636)).execute(CR.ZERO));
  }
}
