package irvine.oeis.a201;
// generated by patch_offset.pl at 2022-08-27 22:54

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A201923 E.g.f. satisfies: A(x) = 1/(cos(x*A(x)) - sin(x*A(x))).
 * E.g.f.: <code>divx(reverse(x*(cos(x)-sin(x))))</code>
 * @author Georg Fischer
 */
public class A201923 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A201923() {
    super(0);
    next();
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return n == 0 ? RING.one() : RING.divide(RING.reversion(RING.multiply(RING.x(), RING.subtract(RING.cos(RING.x(), n), RING.sin(RING.x(), n)), n), n), new Q(n));
  }
}
