package irvine.oeis.a201;
// Generated by gen_seq4.pl decsolvn at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A201925 Decimal expansion of the x nearest 0 that satisfies x^2+4x+3=e^x.
 * Equation; x^2+4*x+3-exp(x)
 * @author Georg Fischer
 */
public class A201925 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A201925() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(2).add(CR.FOUR.multiply(x)).add(CR.THREE).subtract(x.exp());
      }
    }.inverseMonotone(CR.valueOf(-0.811), CR.valueOf(-0.779)).execute(CR.ZERO).negate());
  }
}
