package irvine.oeis.a202;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A202364 Number of n-permutations with at least one cycle of length &gt;=4.
 * E.g.f.: 1/(1-x) - exp(x + x^2/2 + x^3/3)
 * @author Georg Fischer
 */
public class A202364 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.one(), RING.oneMinusXToTheN(1), mN), RING.exp(RING.add(RING.add(RING.x(), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(new Q(2), 0), mN)), RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(new Q(3), 0), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
