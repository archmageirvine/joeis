package irvine.oeis.a205;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-25 11:10

import irvine.factor.factor.Jaguar;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A205812 a(n) = Sum_{k=1..n} binomial(n,k) * sigma(n,k).
 * a(n)=sum(k=1,n,binom(n,k)*sigma(n,k))
 * @author Georg Fischer
 */
public class A205812 extends LambdaSequence {

  /** Construct the sequence. */
  public A205812() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Binomial.binomial(n, k).multiply(Jaguar.factor(n).sigma(k))));
  }
}
