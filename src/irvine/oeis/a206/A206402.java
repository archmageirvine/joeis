package irvine.oeis.a206;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A206402 E.g.f. A(x) satisfies: exp(A(x)) = x + exp(2*A(x)^2), with A(0) = 0.
 * E.g.f.: reverse( exp(x) - exp(2*x^2) )
 * @author Georg Fischer
 */
public class A206402 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A206402() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.subtract(RING.exp(RING.x(), n), RING.exp(RING.monomial(new Q(2), 2), n)), n);
  }
}
