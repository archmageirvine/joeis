package irvine.oeis.a206;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A206403 E.g.f. A(x) satisfies: exp(A(x)) = 2*exp(A(x)^2) - (1-x), with A(0) = 0.
 * E.g.f.: reverse( 1 + exp(x) - 2*exp(x^2) )
 * @author Georg Fischer
 */
public class A206403 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A206403() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.subtract(RING.add(RING.one(), RING.exp(RING.x(), n)), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.pow(RING.x(), 2, n), n), n)), n);
  }
}
