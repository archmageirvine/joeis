package irvine.oeis.a206;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A206405 E.g.f. A(x) satisfies: exp(A(x)) = x + 2*exp(A(x)^2) - exp(A(x)^3), with A(0) = 0.
 * E.g.f.: reverse( exp(x) - 2*exp(x^2) + exp(x^3) )
 * @author Georg Fischer
 */
public class A206405 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A206405() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.add(RING.subtract(RING.exp(RING.x(), n), RING.multiply(RING.monomial(new Q(2), 0), RING.exp(RING.pow(RING.x(), 2, n), n), n)), RING.exp(RING.pow(RING.x(), 3, n), n)), n);
  }
}
