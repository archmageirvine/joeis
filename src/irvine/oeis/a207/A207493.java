package irvine.oeis.a207;
// Generated by gen_seq4.pl egfu at 2021-12-05 23:06

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A207493 E.g.f. A(x) is the series reversion of 2*x-1/2*x^2-exp(x)+1.
 * E.g.f.: 0
 * @author Georg Fischer
 */
public class A207493 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A207493() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.reversion(RING.add(RING.subtract(RING.subtract(RING.monomial(new Q(2), 1), RING.multiply(RING.series(RING.one(), RING.monomial(new Q(2), 0), mN), RING.pow(RING.x(), 2, mN), mN)), RING.exp(RING.x(), mN)), RING.one()), mN);
  }
}
