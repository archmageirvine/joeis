package irvine.oeis.a209;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction3;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A209406 Triangular array read by rows: T(n,k) is the number of multisets of exactly k nonempty binary words with a total of n letters.
 * @author Georg Fischer
 */
public class A209406 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A209406() {
    super(1, 1, 1);
  }

  final MemoryFunction3<Long, Z> mB = new MemoryFunction3<>() {
    @Override
    protected Z compute(final Long n, final Long i, final Long p) {
      return
        p > n ? Z.ZERO : (n == 0 ? Z.ONE : (Functions.MIN.i(i, p) < 1 ? Z.ZERO : Integers.SINGLETON.sum(0, Functions.MIN.i(n / i, p), j -> get(n - i * j, i - 1, p - j).multiply(Binomial.binomial(Z.TWO.pow(i).add(j - 1), j)))));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, n, k);
  }
}
