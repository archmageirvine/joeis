package irvine.oeis.a211;
// Generated by gen_seq4.pl 2026-06-21.ack/filnum at 2026-06-21 22:51

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.oeis.FilterNumberSequence;

/**
 * A211484 Numbers for which the canonical prime factorization contains only an even number of exponents, all of which are congruent to 1 modulo 3.
 * @author Georg Fischer
 */
public class A211484 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A211484() {
    super(1, 1, k -> {
      final FactorSequence fs = Jaguar.factor(k);
      return (fs.omega() & 1) == 0 && FactorUtils.iterate(k, 0L, (x, p, e) -> x + (e % 3 == 1 ? 0 : 1)) == 0L;
    });
  }
}
