package irvine.oeis.a214;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A214363 E.g.f. satisfies: A(x) = x + A(x)^2 * cosh(A(x))^2 / 2.
 * E.g.f.: reverse(x-(x^2*cosh(x)^2)/2)
 * @author Georg Fischer
 */
public class A214363 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A214363() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.subtract(RING.x(), RING.series(RING.multiply(RING.pow(RING.x(), 2, n), RING.pow(RING.cosh(RING.x(), n), 2, n), n), RING.monomial(new Q(2), 0), n)), n);
  }
}
