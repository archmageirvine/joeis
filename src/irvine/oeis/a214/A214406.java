package irvine.oeis.a214;
// Generated by gen_seq4.pl trecpas

import irvine.math.z.Z;
import irvine.oeis.triangle.Triangle;

/**
 * A214406 Triangle of second-order Eulerian numbers of type B.
 * @author Georg Fischer
 */
public class A214406 extends Triangle {

  /** Construct the sequence. */
  public A214406() {
    hasRAM(false);
  }

  @Override
  protected Z compute(final int n, final int k) {
    return n == 0 ? Z.ONE : get(n - 1, k - 1).multiply(4 * n - 2 * k - 1).add(get(n - 1, k).multiply(2 * k + 1));
  }
}
