package irvine.oeis.a215;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A215003 E.g.f. satisfies: A(x) = x + A(x)^2*exp(A(x))/2.
 * E.g.f.: reverse(x - x^2*exp(x)/2)
 * @author Georg Fischer
 */
public class A215003 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A215003() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.reversion(RING.subtract(RING.x(), RING.series(RING.multiply(RING.pow(RING.x(), 2, mN), RING.exp(RING.x(), mN), mN), RING.monomial(new Q(2), 0), mN)), mN);
  }
}
