package irvine.oeis.a215;
// Generated by gen_seq4.pl arsimple/prev1 at 2021-11-18 23:20

import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A215203 a(0) = 0, a(n) = a(n - 1)*2^(n + 1) + 2^n - 1. That is, add one 0 and n 1's to the binary representation of previous term.
 * @author Georg Fischer
 */
public class A215203 extends Sequence0 {

  private int mN = -1;
  private Z mAn1 = Z.ZERO;

  @Override
  public Z next() {
    ++mN;
    if (mN > 0) {
      mAn1 = mAn1.multiply(Z.ONE.shiftLeft(mN + 1)).add(Z.ONE.shiftLeft(mN)).subtract(1);
    }
    return mAn1;
  }
}
