package irvine.oeis.a215;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A215362 E.g.f. satisfies: A(x) = x - (exp(A(x)) - 1) * log(1 - A(x)).
 * E.g.f.: reverse(x+(exp(x)-1)*log(1-x))
 * @author Georg Fischer
 */
public class A215362 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A215362() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.add(RING.x(), RING.multiply(RING.subtract(RING.exp(RING.x(), n), RING.one()), RING.log(RING.oneMinusXToTheN(1), n), n)), n);
  }
}
