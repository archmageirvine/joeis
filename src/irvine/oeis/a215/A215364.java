package irvine.oeis.a215;
// generated by patch_offset.pl at 2022-08-27 22:54

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A215364 E.g.f. satisfies: A(x) = 1 + x*A(x)^2*cosh(x*A(x)).
 * E.g.f.: <code>divx(reverse(x-x^2*cosh(x)))</code>
 * @author Georg Fischer
 */
public class A215364 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A215364() {
    super(0);
    next();
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return n == 0 ? RING.one() : RING.divide(RING.reversion(RING.subtract(RING.x(), RING.multiply(RING.pow(RING.x(), 2, n), RING.cosh(RING.x(), n), n)), n), new Q(n));
  }
}
