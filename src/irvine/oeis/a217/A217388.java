package irvine.oeis.a217;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A217388 Alternating sums of the ordered Bell numbers (number of preferential arrangements) A000670.
 * E.g.f.: 1/(2-exp(x))-exp(-x)*log(1/(2-exp(x)))
 * @author Georg Fischer
 */
public class A217388 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.one(), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), mN), RING.multiply(RING.exp(RING.negate(RING.x()), mN), RING.log(RING.series(RING.one(), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), mN), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
