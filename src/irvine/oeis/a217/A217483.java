package irvine.oeis.a217;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A217483 Alternating sums of the numbers in sequence A080253.
 * E.g.f.: exp(x)/(2-exp(2*x)) - (1/2)*exp(-x)*log(1/(2-exp(2*x)))
 * @author Georg Fischer
 */
public class A217483 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.exp(RING.x(), mN), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.monomial(Q.TWO, 1), mN)), mN), RING.multiply(RING.multiply(RING.series(RING.one(), RING.monomial(Q.TWO, 0), mN), RING.exp(RING.negate(RING.x()), mN), mN), RING.log(RING.series(RING.one(), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.monomial(Q.TWO, 1), mN)), mN), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
