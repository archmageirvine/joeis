package irvine.oeis.a217;
// Generated by gen_seq4.pl egfu/egfue at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A217582 E.g.f. 1/2*sqrt(sec(2*x))-1/2, (even part).
 * E.g.f.: 1/2*sqrt(sec(2*x))-1/2
 * @author Georg Fischer
 */
public class A217582 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A217582() {
    super(0, 0, 2);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.subtract(RING.multiply(RING.series(RING.one(), RING.monomial(Q.TWO, 0), n), RING.pow(RING.sec(RING.monomial(Q.TWO, 1), n), Q.HALF, n), n), RING.series(RING.one(), RING.monomial(Q.TWO, 0), n));
  }
}
