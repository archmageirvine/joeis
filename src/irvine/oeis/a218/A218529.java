package irvine.oeis.a218;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A218529 Binomial transform of -1, -1, 1, 2, -5, -16, ... (signed variant of A000111).
 * E.g.f.: exp(x)*(1-exp(2*x)-2*exp(x))/(1+exp(2*x))
 * @author Georg Fischer
 */
public class A218529 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.subtract(RING.one(), RING.exp(RING.monomial(Q.TWO, 1), mN)), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN), mN)), mN), RING.add(RING.one(), RING.exp(RING.monomial(Q.TWO, 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
