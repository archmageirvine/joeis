package irvine.oeis.a220;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A220232 Number of rooted labeled trees of height 2 such that every leaf is at a distance 2 from the root.
 * E.g.f.: x*(exp(x*(exp(x)-1))-1)
 * @author Georg Fischer
 */
public class A220232 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.x(), RING.subtract(RING.exp(RING.multiply(RING.x(), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
