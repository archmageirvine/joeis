package irvine.oeis.a220;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A220359 Decimal expansion of the root of the equation (1-r)^(2*r-1) = r^(2*r).
 * Equation; (1-x)^(2*x-1)-x^(2*x)
 * @author Georg Fischer
 */
public class A220359 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A220359() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(x).pow(CR.TWO.multiply(x).subtract(CR.ONE)).subtract(x.pow(CR.TWO.multiply(x)));
      }
    }.inverseMonotone(CR.valueOf(0.689), CR.valueOf(0.718)).execute(CR.ZERO));
  }
}
