package irvine.oeis.a221;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A221160 G.f.: Sum_{n&gt;=0} (4*n+1)^n * x^n / (1 + (4*n+1)*x)^n.
 * E.g.f.: (1 - 3*x + 4*x^2) / (1-4*x)^2
 * @author Georg Fischer
 */
public class A221160 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.subtract(RING.one(), RING.monomial(new Q(3), 1)), RING.monomial(new Q(4), 2)), RING.pow(RING.subtract(RING.one(), RING.monomial(new Q(4), 1)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
