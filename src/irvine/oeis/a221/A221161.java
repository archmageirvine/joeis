package irvine.oeis.a221;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A221161 G.f.: Sum_{n&gt;=0} (4*n+3)^n * x^n / (1 + (4*n+3)*x)^n.
 * E.g.f.: (1 - x - 4*x^2) / (1-4*x)^2
 * @author Georg Fischer
 */
public class A221161 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.oneMinusXToTheN(1), RING.monomial(Q.FOUR, 2)), RING.pow(RING.subtract(RING.one(), RING.monomial(Q.FOUR, 1)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
