package irvine.oeis.a223;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A223901 Poly-Cauchy numbers of the second kind hat c_n^(-3).
 * E.g.f.: (1 - 7 * log(1 + x) + 6 * log(1 + x)^2 - log(1 + x)^3) / (1 + x)
 * @author Georg Fischer
 */
public class A223901 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.add(RING.subtract(RING.one(), RING.multiply(RING.monomial(new Q(7), 0), RING.log(RING.onePlusXToTheN(1), mN), mN)), RING.multiply(RING.monomial(new Q(6), 0), RING.pow(RING.log(RING.onePlusXToTheN(1), mN), 2, mN), mN)), RING.pow(RING.log(RING.onePlusXToTheN(1), mN), 3, mN)), RING.onePlusXToTheN(1), mN).coeff(mN).multiply(mF).toZ();
  }
}
