package irvine.oeis.a225;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A225147 a(n) = Im((1-I)^(1-n)*A_{n, 3}(I)) where A_{n, k}(x) are the generalized Eulerian polynomials.
 * E.g.f.: -exp(-2*x)*sech(3*x)
 * @author Georg Fischer
 */
public class A225147 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.zero(), RING.multiply(RING.exp(RING.subtract(RING.zero(), RING.monomial(Q.TWO, 1)), mN), RING.sech(RING.monomial(Q.THREE, 1), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
