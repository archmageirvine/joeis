package irvine.oeis.a225;
// Generated by gen_seq4.pl A225466/trecpas

import irvine.math.z.Z;
import irvine.oeis.triangle.Triangle;

/**
 * A225469 Triangle read by rows, S_4(n, k) where S_m(n, k) are the Stirling-Frobenius subset numbers of order m; n &gt;= 0, k &gt;= 0.
 * @author Georg Fischer
 */
public class A225469 extends Triangle {

  /** Construct the sequence. */
  public A225469() {
    hasRAM(false);
  }

  @Override
  protected Z compute(final int n, final int k) {
    return n == 0 ? Z.ONE : get(n - 1, k - 1).add(get(n - 1, k).multiply(4L * k + 3));
  }
}
