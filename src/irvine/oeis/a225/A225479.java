package irvine.oeis.a225;
// Generated by gen_seq4.pl A225466/trecpas

import irvine.math.z.Z;
import irvine.oeis.triangle.Triangle;

/**
 * A225479 Triangle read by rows, the ordered Stirling cycle numbers, T(n, k) = k!* s(n, k); n &gt;= 0 k &gt;= 0.
 * @author Georg Fischer
 */
public class A225479 extends Triangle {

  /** Construct the sequence. */
  public A225479() {
    hasRAM(false);
  }

  @Override
  protected Z compute(final int n, final int k) {
    return n == 0 ? Z.ONE : get(n - 1, k - 1).multiply(k).add(get(n - 1, k).multiply(n - 1));
  }
}
