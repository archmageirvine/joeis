package irvine.oeis.a225;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A225688 E.g.f.: sec(x)^3+(sec(x)^2*tan(x)).
 * E.g.f.: sec(x)^3+(sec(x)^2*tan(x))
 * @author Georg Fischer
 */
public class A225688 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.pow(RING.sec(RING.x(), mN), 3, mN), RING.multiply(RING.pow(RING.sec(RING.x(), mN), 2, mN), RING.tan(RING.x(), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
