package irvine.oeis.a226;
// Generated by gen_seq4.pl 2026-08-18.ack/mtrifun

import irvine.math.MemoryFunctionInt2;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A226392 Triangle with first column identical to 1 and the other entries defined by the sum of entries above and to the left.
 * @author Georg Fischer
 */
public class A226392 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A226392() {
    super(0, 0, 0);
  }

  final MemoryFunctionInt2<Z> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Z compute(final int n, final int k) {
      return
        k == 0 ? Z.ONE : (k > n || k < 0 ? Z.ZERO : Integers.SINGLETON.sum(0, k - 1, c -> get(n, c)).add(Integers.SINGLETON.sum(k, n - 1, r -> get(r, k))));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return mB.get(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
