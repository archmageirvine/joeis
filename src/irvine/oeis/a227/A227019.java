package irvine.oeis.a227;
// Generated by gen_seq4.pl 2024-11-13.ack/filnum at 2024-11-13 22:27

import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A227019 Numbers k such that exactly one of {2^k-1, 2^k+1, 2^k+3} is semiprime.
 * @author Georg Fischer
 */
public class A227019 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A227019() {
    super(1, 1, k -> {
      final Z k2 = Z.TWO.pow(k);
      final boolean m1 = Predicates.SEMIPRIME.is(k2.subtract(1));
      final boolean p1 = Predicates.SEMIPRIME.is(k2.add(1));
      final boolean p3 = Predicates.SEMIPRIME.is(k2.add(3));
      return m1 && !p1 && !p3 || !m1 && p1 && !p3 || !m1 && !p1 && p3;
    });
  }
}
