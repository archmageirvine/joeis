package irvine.oeis.a228;
// Generated by gen_seq4.pl 2026-08-10.ack/determ00 at 2026-08-10 18:43

import java.util.function.BiFunction;

import irvine.math.group.MatrixField;
import irvine.math.matrix.LambdaMatrix;
import irvine.math.predicate.Predicates;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;


/**
 * A228623 Determinant of the n X n matrix with (i,j)-entry (i,j = 0,...,n-1) equal to 1 or 0 according as n + i - j and n - i + j are both prime or not.
 * @author Georg Fischer
 */
public class A228623 extends AbstractSequence {

  private long mN = 0;
  private final BiFunction<Long, Long, Q> mLambda;

  /** Construct the sequence */
  public A228623() {
    super(1);
    mN = 0;
    mLambda = (i, j) -> new Q((Predicates.PRIME.is(mN + i - j) && Predicates.PRIME.is(mN - i + j)) ? 1 : 0);
  }

  @Override
  public Z next() {
    if (++mN == 0) {
      return Z.ONE;
    }
    final LambdaMatrix<Q> mat = new LambdaMatrix<>(mN, mN, Q.ZERO, mLambda, 0);
    return new MatrixField<>(mN, Rationals.SINGLETON).det(mat).toZ();
  }
}
