package irvine.oeis.a229;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A229039 G.f.: Sum_{n&gt;=0} (n+2)^n * x^n / (1 + (n+2)*x)^n.
 * E.g.f.: (2 + 2*x - 3*x^2)/(2*(1-x)^2)
 * @author Georg Fischer
 */
public class A229039 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.add(RING.monomial(Q.TWO, 0), RING.monomial(Q.TWO, 1)), RING.monomial(Q.THREE, 2)), RING.multiply(RING.monomial(Q.TWO, 0), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
