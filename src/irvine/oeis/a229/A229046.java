package irvine.oeis.a229;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A229046 G.f.: Sum_{n&gt;=0} n! * x^n * (1+x)^n / Product_{k=1..n} (1 + k*x).
 * a(n)=sum(k=0,floor(n/2),sum(i=0,k,(-1)^i*binom(k,i)*(k-i+1)^(n-k)))
 * @author Georg Fischer
 */
public class A229046 extends LambdaSequence {

  /** Construct the sequence. */
  public A229046() {
    super(0, n -> Integers.SINGLETON.sum(0, n / 2, k -> Integers.SINGLETON.sum(0, k, i -> Z.NEG_ONE.pow(i).multiply(Binomial.binomial(k, i)).multiply(Z.valueOf(k - i + 1).pow(n - k)))));
  }
}
