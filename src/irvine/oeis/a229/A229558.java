package irvine.oeis.a229;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A229558 E.g.f.: exp(x) / (2 - exp(4*x))^(1/4).
 * E.g.f.: exp(x) / (2 - exp(4*x))^(1/4)
 * @author Georg Fischer
 */
public class A229558 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.x(), mN), RING.exp(RING.multiply(RING.log(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.monomial(Q.FOUR, 1), mN)), mN), RING.series(RING.one(), RING.monomial(Q.FOUR, 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
