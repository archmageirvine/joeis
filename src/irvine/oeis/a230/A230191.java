package irvine.oeis.a230;
// generated by patch_offset.pl at 2022-08-17 15:41

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A230191 Decimal expansion of log( 2^(1/2)*3^(1/3)*5^(1/5) / 30^(1/30) ).
 * @author Georg Fischer
 */
public class A230191 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A230191() {
    super(0, REALS.pow(CR.TWO, CR.HALF).multiply(REALS.pow(CR.THREE, CR.ONE_THIRD)).multiply(REALS.pow(CR.FIVE, CR.ONE.divide(CR.FIVE))).divide(REALS.pow(CR.valueOf(30), CR.ONE.divide(CR.valueOf(30)))).log());
  }
}
