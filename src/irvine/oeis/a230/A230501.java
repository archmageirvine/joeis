package irvine.oeis.a230;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A230501 Floor(Sum(d(k), k=1..n)/n), where d(k) is the number of divisors of k.
 * a(n)=floor(sum(k=1, n, sigma(k, 0))/n)
 * @author Georg Fischer
 */
public class A230501 extends LambdaSequence {

  /** Construct the sequence. */
  public A230501() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Jaguar.factor(k).sigma(0)).divide(n));
  }
}
