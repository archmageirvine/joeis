package irvine.oeis.a231;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A231895 a(n) = 2*A000111(n+1)+A000111(n).
 * E.g.f.: 1+(sec(x)+tan(x)+1)*(sec(x)+tan(x))
 * @author Georg Fischer
 */
public class A231895 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.one(), RING.multiply(RING.add(RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), RING.one()), RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
