package irvine.oeis.a232;
// Generated by gen_seq4.pl 2025-10-12.ack/lambdan at 2025-10-12 22:36

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A232442 a(n) = |{0 &lt; k &lt; n: m*prime(m) - 1 and m*prime(m) + 1 are both prime with m = sigma(k) + phi(n-k)}|, where sigma(k) is the sum of all positive divisors of k and phi(.) is Euler&apos;s totient function.
 * @author Georg Fischer
 */
public class A232442 extends LambdaSequence {

  /** Construct the sequence. */
  public A232442() {
    super(1, n -> Integers.SINGLETON.count(1, n - 1, k -> {
      final Z m = Functions.SIGMA1.z(k).add(Functions.PHI.z(n - k));
      final Z p = Functions.PRIME.z(m).multiply(m);
      return Predicates.PRIME.is(p.subtract(1)) && Predicates.PRIME.is(p.add(1));
    }));
  }
}
