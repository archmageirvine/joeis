package irvine.oeis.a232;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A232475 Number of preferential arrangements of n labeled elements when at least k=4 elements per rank are required.
 * E.g.f.: 1/(2 + x - exp(x) + x^2/2 + x^3/6)
 * @author Georg Fischer
 */
public class A232475 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.add(RING.add(RING.subtract(RING.add(RING.monomial(new Q(2), 0), RING.x()), RING.exp(RING.x(), mN)), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(new Q(2), 0), mN)), RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(new Q(6), 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
