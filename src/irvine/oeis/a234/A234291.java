package irvine.oeis.a234;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A234291 E.g.f. satisfies: A(x) = 1 + A(x)^3 * Integral 1/A(x)^2 dx.
 * E.g.f.: 1 + reverse( 3*x/(1+x) - 2*log(1+x) )
 * @author Georg Fischer
 */
public class A234291 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A234291() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.add(RING.one(), RING.reversion(RING.subtract(RING.series(RING.monomial(new Q(3), 1), RING.onePlusXToTheN(1), mN), RING.multiply(RING.monomial(new Q(2), 0), RING.log(RING.onePlusXToTheN(1), mN), mN)), mN));
  }
}
