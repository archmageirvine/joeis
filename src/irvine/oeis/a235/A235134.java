package irvine.oeis.a235;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A235134 E.g.f. 1/(1 - sinh(2*x))^(1/2).
 * E.g.f.: 1/(1 - sinh(2*x))^(1/2)
 * @author Georg Fischer
 */
public class A235134 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.sinh(RING.monomial(Q.TWO, 1), mN)), mN), RING.series(RING.one(), RING.monomial(Q.TWO, 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
