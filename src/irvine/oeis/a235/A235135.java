package irvine.oeis.a235;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A235135 E.g.f. 1/(1 - sinh(3*x))^(1/3).
 * E.g.f.: 1/(1 - sinh(3*x))^(1/3)
 * @author Georg Fischer
 */
public class A235135 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.sinh(RING.monomial(Q.THREE, 1), mN)), mN), RING.series(RING.one(), RING.monomial(Q.THREE, 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
