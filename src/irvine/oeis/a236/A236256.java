package irvine.oeis.a236;
// Generated by gen_seq4.pl 2025-10-12.ack/lambdan at 2025-10-12 22:36

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.q.Q;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A236256 a(n) = |{0 &lt; k &lt; n: m = phi(k) + phi(n-k)/4 is an integer with C(2*m, m) - prime(m) prime}|, where C(2*m, m) = (2*m)!/(m!)^2.
 * @author Georg Fischer
 */
public class A236256 extends LambdaSequence {

  /** Construct the sequence. */
  public A236256() {
    super(1, n -> Integers.SINGLETON.count(1, n - 1, k -> {
      final Q mq = new Q(Functions.PHI.z(k)).add(new Q(Functions.PHI.z(n - k), 4));
      final int m = mq.num().intValueExact();
      return mq.isInteger() && Predicates.PRIME.is(Binomial.binomial(2L * m, m).subtract(Functions.PRIME.z(m)));
    }));
  }
}
