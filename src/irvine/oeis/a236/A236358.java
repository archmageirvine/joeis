package irvine.oeis.a236;
// Generated by gen_seq4.pl 2025-10-12.ack/lambdan at 2025-10-12 22:36

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.q.Q;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A236358 a(n) = |{0 &lt; k &lt; n: m = phi(k)/2 + phi(n-k)/12 is an integer with 2*3^m + 1 prime}|, where phi(.) is Euler&apos;s totient function.
 * @author Georg Fischer
 */
public class A236358 extends LambdaSequence {

  /** Construct the sequence. */
  public A236358() {
    super(1, n -> Integers.SINGLETON.count(1, n - 1, k -> {
      final Q mq = new Q(Functions.PHI.z(k), 2).add(new Q(Functions.PHI.z(n - k), 12));
      final int m = mq.num().intValueExact();
      return mq.isInteger() && Predicates.PRIME.is(Z.THREE.pow(m).multiply(2).add(1));
    }));
  }
}
