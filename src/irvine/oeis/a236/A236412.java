package irvine.oeis.a236;
// Generated by gen_seq4.pl 2025-10-12.ack/lambdan at 2025-10-12 22:36

import irvine.math.function.Functions;
import irvine.math.predicate.Predicates;
import irvine.math.q.Q;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A236412 a(n) = |{0 &lt; k &lt; n: m = phi(k)/2 + phi(n-k)/8 is an integer with p(m)^2 + q(m)^2 prime}|, where phi(.) is Euler&apos;s totient, p(.) is the partition function (A000041) and q(.) is the strict partition function (A000009).
 * @author Georg Fischer
 */
public class A236412 extends LambdaSequence {

  /** Construct the sequence. */
  public A236412() {
    super(1, n -> Integers.SINGLETON.count(1, n - 1, k -> {
      final Q mq = new Q(Functions.PHI.z(k), 2).add(new Q(Functions.PHI.z(n - k), 8));
      final int m = mq.num().intValueExact();
      return mq.isInteger() && Predicates.PRIME.is(Functions.PARTITIONS.z(m).square().add(Functions.DISTINCT_PARTITIONS.z(m).square()));
    }));
  }
}
