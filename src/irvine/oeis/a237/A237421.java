package irvine.oeis.a237;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A237421 Decimal expansion of the root of the equation (1-r)^(2*r) = r^(2*r+1).
 * Equation; (1-x)^(2*x)-x^(2*x+1)
 * @author Georg Fischer
 */
public class A237421 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A237421() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(x).pow(CR.TWO.multiply(x)).subtract(x.pow(CR.TWO.multiply(x).add(CR.ONE)));
      }
    }.inverseMonotone(CR.valueOf(0.591), CR.valueOf(0.615)).execute(CR.ZERO));
  }
}
