package irvine.oeis.a238;
// Generated by gen_seq4.pl 2026-08-17.ack/mtripol

import irvine.math.MemoryFunctionInt2;
import irvine.math.group.IntegerField;
import irvine.math.group.PolynomialRingField;
import irvine.math.polynomial.Polynomial;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A238130 Triangle read by rows: T(n,k) is the number of compositions into nonzero parts with k parts directly followed by a different part, n&gt;=0, 0&lt;=k&lt;=n.
 * @author Georg Fischer
 */
public class A238130 extends BaseTriangle implements DirectArray {

  protected static final PolynomialRingField<Z> RING = new PolynomialRingField<>(IntegerField.SINGLETON);

  /** Construct the sequence. */
  public A238130() {
    super(0, 0, 0);
  }

  final MemoryFunctionInt2<Polynomial<Z>> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Polynomial<Z> compute(final int n, final int v) {
      return
        (n == 0) ? RING.one() : RING.sum(1, n, i -> RING.multiply(get(n - i, i), (v == 0 || v == i) ? RING.one() : RING.x()));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return mB.get(n, 0).coeff(k);
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
