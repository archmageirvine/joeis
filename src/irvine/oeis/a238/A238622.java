package irvine.oeis.a238;
// Generated by gen_seq4.pl 2026-09-02.ack/partpred at 2026-09-02 15: 37

import irvine.math.partition.IntegerPartition;
import irvine.math.partition.PartitionUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A238622 Number of partitions of n such that floor(n/2) or ceiling(n/2) is a part.
 * @author Georg Fischer
 */
public class A238622 extends LambdaSequence {

  /** Construct the sequence. */
  public A238622() {
    super(1, n -> (n == 0) ? Z.ZERO : IntegerPartition.count(Math.toIntExact(n), p -> {
      final int fn = Math.toIntExact(n / 2);
      final int cn = Math.toIntExact((n & 1) == 0 ? n / 2 : (n + 1) / 2);
      return PartitionUtils.isPart(p, fn) || PartitionUtils.isPart(p, cn);
    }));
  }
}
