package irvine.oeis.a239;
// Generated by gen_seq4.pl 2026-08-27.ack/mlamlon

import irvine.math.MemoryFunction3;
import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A239950
 * @author Georg Fischer
 */
public class A239950 extends AbstractSequence implements DirectSequence {

  private long mN;
  private long mColNo;

  /** Construct the sequence. */
  public A239950() {
    this(0, 1);
  }

  /**
   * Generic constructor with parameters.
   * @param offset first index
   * @param colNo column number
   */
  public A239950(final int offset, final long colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunction3<Long, Z> mB = new MemoryFunction3<>() {
    @Override
    protected Z compute(final Long n, final Long i, final Long d) {
      return
        Functions.MIN.l(i, n) < d + 1 ? Z.ZERO : (n % i == 0 && i == d + 1 ? Z.ONE : get(n, i - 1, d).add(Integers.SINGLETON.sum(1, n / i, j -> get(n - i * j, i - 1, d + 1))));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n, n, 0L);
  }
}
