package irvine.oeis.a239;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A239977 a(n) = -Sum_{k=0..n} binomial(n, k)*A226158(k).
 * E.g.f.: 2*x*exp(2*x)/(1 + exp(x))
 * @author Georg Fischer
 */
public class A239977 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A239977() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.multiply(RING.monomial(Q.TWO, 1), RING.exp(RING.monomial(Q.TWO, 1), n), n), RING.add(RING.one(), RING.exp(RING.x(), n)), n);
  }
}
