package irvine.oeis.a240;
// Generated by gen_seq4.pl 2026-08-16.ack/mlamfun

import irvine.math.MemoryFunctionInt3;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A240010 Number of partitions of n, where the difference between the number of odd parts and the number of even parts is 1.
 * @author Georg Fischer
 */
public class A240010 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A240010() {
    this(1, 0);
  }

  /**
   * Generic cosntructor with parameter.
   * @param offset first index
   * @param colNo column number
   */
  public A240010(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int i, final int t) {
      return
        (Math.abs(t) > n) ? Z.ZERO : (n == 0 ? Z.ONE : (i < 1 ? Z.ZERO : get(n, i - 1, t).add(i > n ? Z.ZERO : get(n - i, i, t + ((i & 1) == 0 ? -1 : 1)))));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n, n, -1);
  }
}
