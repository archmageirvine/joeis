package irvine.oeis.a240;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A240358 Decimal expansion of 'c', a constant linked to an estimate of density of zeros of an entire function of exponential type.
 * Equation; log(x+Sqrt(x^2+1))-sqrt(1+1/x^2)
 * @author Georg Fischer
 */
public class A240358 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A240358() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.add(x.square().add(CR.ONE).sqrt()).log().subtract(CR.ONE.add(CR.ONE.divide(x.pow(CR.TWO))).sqrt());
      }
    }.inverseMonotone(CR.valueOf(1.478), CR.valueOf(1.538)).execute(CR.ZERO));
  }
}
