package irvine.oeis.a241;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A241209 a(n) = E(n) - E(n+1), where E(n) are the Euler numbers A122045(n).
 * E.g.f.: exp(x)/cosh(x)^2
 * @author Georg Fischer
 */
public class A241209 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.x(), mN), RING.pow(RING.cosh(RING.x(), mN), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
