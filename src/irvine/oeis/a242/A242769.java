package irvine.oeis.a242;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A242769 Decimal expansion of the positive solution to the equation x/(1-x) = 1+log(1/(1-x)), an auxiliary constant associated with the problem of enumeration of trees by inversions.
 * Equation; x/(1-x)-(1+log(1/(1-x)))
 * @author Georg Fischer
 */
public class A242769 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A242769() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.divide(CR.ONE.subtract(x)).subtract(CR.ONE.add(CR.ONE.divide(CR.ONE.subtract(x)).log()));
      }
    }.inverseMonotone(CR.valueOf(0.668), CR.valueOf(0.696)).execute(CR.ZERO));
  }
}
