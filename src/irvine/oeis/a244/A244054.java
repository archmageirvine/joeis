package irvine.oeis.a244;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A244054 Decimal expansion of the maximal circumradius of a planar convex set containing no lattice point except for the origin where it has its circumcenter, and not protuding in opposite directions outside the square max(|x|,|y|) &lt; 1 unless it protudes significantly elsewhere, too.
 * Equation; 5*x^6-15*x^4+3*x^2-2
 * @author Georg Fischer
 */
public class A244054 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A244054() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.FIVE.multiply(x.pow(6)).subtract(CR.valueOf(15).multiply(x.pow(CR.FOUR))).add(CR.THREE.multiply(x.pow(CR.TWO))).subtract(CR.TWO);
      }
    }.inverseMonotone(CR.valueOf(1.650), CR.valueOf(1.718)).execute(CR.ZERO));
  }
}
