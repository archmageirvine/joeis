package irvine.oeis.a244;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A244262 Decimal expansion of theta = 2.472548..., an auxiliary constant used to compute the best constant in Friedrichs' inequality in one dimension.
 * Equation; cos(x)-x/(x^2+1)*sin(x)+1
 * @author Georg Fischer
 */
public class A244262 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A244262() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.cos().subtract(x.divide(x.pow(2).add(CR.ONE)).multiply(x.sin())).add(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(2.423), CR.valueOf(2.521)).execute(CR.ZERO));
  }
}
