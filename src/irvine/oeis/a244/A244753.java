package irvine.oeis.a244;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-25 11:10

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A244753 a(n) = Sum_{k=0..n} C(n,k) * (n + 2^k)^k.
 * a(n) = sum(k=0,n,binom(n,k) * (1 + n*2^k)^(n-k) * 2^(k^2) )
 * @author Georg Fischer
 */
public class A244753 extends LambdaSequence {

  /** Construct the sequence. */
  public A244753() {
    super(0, n -> Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(n, k).multiply((Z.ONE).add(Z.valueOf(n).multiply(Z.ONE.shiftLeft(k))).pow(Z.valueOf(n - k))).multiply(Z.ONE.shiftLeft((long) k * k))));
  }
}
