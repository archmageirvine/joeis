package irvine.oeis.a245;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A245116 E.g.f.: (cos(x) + sin(x)*exp(x)) / (cos(x)*exp(x) - sin(x)).
 * E.g.f.: (cos(x) + sin(x)*exp(x)) / (cos(x)*exp(x) - sin(x))
 * @author Georg Fischer
 */
public class A245116 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.cos(RING.x(), mN), RING.multiply(RING.sin(RING.x(), mN), RING.exp(RING.x(), mN), mN)), RING.subtract(RING.multiply(RING.cos(RING.x(), mN), RING.exp(RING.x(), mN), mN), RING.sin(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
