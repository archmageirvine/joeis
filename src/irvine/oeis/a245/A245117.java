package irvine.oeis.a245;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A245117 E.g.f.: (cos(2*x) + sin(2*x)*exp(x)) / (cos(2*x)*exp(x) - sin(2*x)).
 * E.g.f.: (cos(2*x) + sin(2*x)*exp(x)) / (cos(2*x)*exp(x) - sin(2*x))
 * @author Georg Fischer
 */
public class A245117 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.cos(RING.monomial(Q.TWO, 1), mN), RING.multiply(RING.sin(RING.monomial(Q.TWO, 1), mN), RING.exp(RING.x(), mN), mN)), RING.subtract(RING.multiply(RING.cos(RING.monomial(Q.TWO, 1), mN), RING.exp(RING.x(), mN), mN), RING.sin(RING.monomial(Q.TWO, 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
