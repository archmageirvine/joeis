package irvine.oeis.a245;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A245155 E.g.f.: (cosh(3*x) + sinh(3*x)*cosh(x)) / (cosh(x) - sinh(x)*cosh(3*x)).
 * E.g.f.: (cosh(x)*cosh(3*x) + sinh(x) + sinh(3*x)) / (1 - sinh(x)*sinh(3*x))
 * @author Georg Fischer
 */
public class A245155 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.add(RING.multiply(RING.cosh(RING.x(), mN), RING.cosh(RING.monomial(Q.THREE, 1), mN), mN), RING.sinh(RING.x(), mN)), RING.sinh(RING.monomial(Q.THREE, 1), mN)), RING.subtract(RING.one(), RING.multiply(RING.sinh(RING.x(), mN), RING.sinh(RING.monomial(Q.THREE, 1), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
