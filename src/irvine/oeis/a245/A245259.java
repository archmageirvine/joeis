package irvine.oeis.a245;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A245259 Decimal expansion of the root of the equation r^(2*r-1) = (r+1)^(2*r).
 * Equation; x^(2*x-1)-(x+1)^(2*x)
 * @author Georg Fischer
 */
public class A245259 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A245259() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(CR.TWO.multiply(x).subtract(CR.ONE)).subtract(x.add(CR.ONE).pow(CR.TWO.multiply(x)));
      }
    }.inverseMonotone(CR.valueOf(0.369), CR.valueOf(0.384)).execute(CR.ZERO));
  }
}
