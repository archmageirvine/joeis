package irvine.oeis.a245;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A245502 Number of endofunctions f on [n] such that f^6(i) = f(i) for all i in [n].
 * E.g.f.: exp(x*exp(x)+x^5*exp(x)^5/5)
 * @author Georg Fischer
 */
public class A245502 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.multiply(RING.x(), RING.exp(RING.x(), mN), mN), RING.series(RING.multiply(RING.pow(RING.x(), 5, mN), RING.pow(RING.exp(RING.x(), mN), 5, mN), mN), RING.monomial(Q.FIVE, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
