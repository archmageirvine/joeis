package irvine.oeis.a245;
// Generated by gen_seq4.pl 2026-08-16.ack/mlamfun

import irvine.math.MemoryFunctionInt4;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A245747 Number of identity trees with n nodes where the maximal outdegree (branching factor) equals 2.
 * @author Georg Fischer
 */
public class A245747 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A245747() {
    this(4, 2);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param colNo column number
   */
  public A245747(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt4<Z> mB = new MemoryFunctionInt4<>() {
    @Override
    protected Z compute(final int n, final int i, final int t, final int k) {
      return n == 0
        ? Z.ONE
        : i < 1
        ? Z.ZERO
        : Integers.SINGLETON.sum(0, Functions.MIN.i(t, n / i), j -> Binomial.binomial(get(i - 1, i - 1, k, k), j).multiply(get(n - i * j, i - 1, t - j, k)));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n - 1, n - 1, mColNo, mColNo).subtract(mB.get(n - 1, n - 1, mColNo - 1, mColNo - 1));
  }
}
