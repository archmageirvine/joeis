package irvine.oeis.a245;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A245854 Number of preferential arrangements of n labeled elements such that the minimal number of elements per rank equals 1.
 * E.g.f.: 1/(2-exp(x))-1/(2-exp(x)+x)
 * @author Georg Fischer
 */
public class A245854 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.one(), RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.x(), mN)), mN), RING.series(RING.one(), RING.add(RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.x(), mN)), RING.x()), mN)).coeff(mN).multiply(mF).toZ();
  }
}
