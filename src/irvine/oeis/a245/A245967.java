package irvine.oeis.a245;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A245967 Decimal expansion of a constant appearing in a theorem by \u00c1rp\u00e1d Baricz about Mills' ratio of the standard normal distribution.
 * Equation; x*(x^2+2)*exp(x^2/2)*sqrt(Pi/2)*erfc(x/sqrt(2))-x^2-1
 * @author Georg Fischer
 */
public class A245967 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A245967() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.multiply(x.pow(2).add(CR.TWO)).multiply(x.pow(CR.TWO).divide(CR.TWO).exp()).multiply(CR.PI.divide(CR.TWO).sqrt()).multiply(x.divide(CR.SQRT2).erfc()).subtract(x.pow(CR.TWO)).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(1.138), CR.valueOf(1.184)).execute(CR.ZERO));
  }
}
