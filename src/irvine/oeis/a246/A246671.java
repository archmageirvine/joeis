package irvine.oeis.a246;
// Generated by gen_seq4.pl decsolv at 2021-08-11 21:56

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A246671 Decimal expansion of Shepp's constant 'alpha', an optimal stopping constant associated with the case of a zero mean and unit variance distribution function.
 * Equation; 2*x-sqrt(2*Pi)*(1-x^2)*exp(x^2/2)*(1+erf(x/sqrt(2)))
 * @author Georg Fischer
 */
public class A246671 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A246671() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x).subtract(CR.TWO.multiply(CR.PI).sqrt().multiply(CR.ONE.subtract(x.square())).multiply(x.pow(CR.TWO).divide(CR.TWO).exp()).multiply(CR.ONE.add(x.divide(CR.SQRT2).erf())));
      }
    }.inverseMonotone(CR.valueOf(0.823), CR.valueOf(0.857)).execute(CR.ZERO));
  }
}
