package irvine.oeis.a246;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A246813 G.f.: Sum_{n&gt;=0} x^n / (1-3*x)^(2*n+1) * [Sum_{k=0..n} C(n,k)^2 * x^k] * [Sum_{k=0..n} C(n,k)^2 * 3^k * x^k].
 * a(n)=sum(k=0,n\2,sum(j=0,n-2*k,binom(n-k,k+j)^2 * binom(k+j,j)^2 * 3^j))
 * @author Georg Fischer
 */
public class A246813 extends LambdaSequence {

  /** Construct the sequence. */
  public A246813() {
    super(0, n -> Integers.SINGLETON.sum(0, n / 2, k -> Integers.SINGLETON.sum(0, n - 2 * k, j -> Binomial.binomial(n - k, k + j).square().multiply(Binomial.binomial(k + j, j).square()).multiply(Z.THREE.pow(j)))));
  }
}
