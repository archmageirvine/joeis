package irvine.oeis.a248;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A248200 Decimal expansion of x in the solution to x^e = e^(-x), where e = exp(1). Also the smallest value of the constant c where there exists a solution to x^c = c^(-x).
 * Equation; x^e-(e^(-x))
 * @author Georg Fischer
 */
public class A248200 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A248200() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(CR.E).subtract(CR.E.pow(x.negate()));
      }
    }.inverseMonotone(CR.valueOf(0.742), CR.valueOf(0.772)).execute(CR.ZERO));
  }
}
