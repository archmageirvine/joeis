package irvine.oeis.a252;
// Generated by gen_seq4.pl 2026-08-27.ack/mlamlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A252782 a(n) = n-th term of Euler transform of n-th powers.
 * @author Georg Fischer
 */
public class A252782 extends AbstractSequence implements DirectSequence {

  private long mN;
  private long mColNo;

  /** Construct the sequence. */
  public A252782() {
    this(0, 1);
  }

  /**
   * Generic constructor with parameters.
   * @param offset first index
   * @param colNo column number
   */
  public A252782(final int offset, final long colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        (n == 0) ? Z.ONE : Integers.SINGLETON.sum(1, n, j -> Integers.SINGLETON.sumdiv(j, d -> Z.valueOf(d).pow(k).multiply(d)).multiply(get(n - j, k))).divide(n);
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n, n);
  }
}
