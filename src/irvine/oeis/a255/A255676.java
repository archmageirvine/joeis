package irvine.oeis.a255;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A255676 E.g.f. satisfies: A'(x) = (1 + A(x))*(1 + A(x)^2).
 * E.g.f.: reverse( (log((1+x)^2/(1+x^2)) + 2*atan(x))/4 )
 * @author Georg Fischer
 */
public class A255676 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A255676() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.series(RING.add(RING.log(RING.series(RING.pow(RING.onePlusXToTheN(1), 2, n), RING.onePlusXToTheN(2), n), n), RING.multiply(RING.monomial(Q.TWO, 0), RING.atan(RING.x(), n), n)), RING.monomial(Q.FOUR, 0), n), n);
  }
}
