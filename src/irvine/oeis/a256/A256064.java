package irvine.oeis.a256;
// Generated by gen_seq4.pl 2026-08-27.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A256064
 * @author Georg Fischer
 */
public class A256064 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A256064() {
    super(1, 1, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        (n < 2) ? Z.valueOf(n) : Integers.SINGLETON.sum(1, n - 1, j -> Integers.SINGLETON.sumdiv(j, d ->
          get(d, k).multiply(Z.valueOf(d))).multiply(k).multiply(get(n - j, k))).divide(n - 1);
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return Integers.SINGLETON.sum(0, k, i -> mB.get(n, k - i).multiply(Z.NEG_ONE.pow(i)).multiply(Binomial.binomial(k, i)));
  }
}
