package irvine.oeis.a256;
// Generated by gen_seq4.pl 2026-08-27.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A256068 Number T(n,k) of rooted identity trees with n nodes and colored non-root nodes using exactly k colors; triangle T(n,k), n&gt;=1, 0&lt;=k&lt;=n-1, read by rows.
 * @author Georg Fischer
 */
public class A256068 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A256068() {
    super(1, 1, 1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n < 2 ? Z.valueOf(n) : Integers.SINGLETON.sum(1, n - 1, j -> get(n - j, k).multiply(Integers.SINGLETON.sumdiv(j, d ->
          get(d, k).multiply(d).multiply(k).multiply(Z.NEG_ONE.pow(j / d + 1))))).divide(n - 1);
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return Integers.SINGLETON.sum(0, k - 1, i -> mB.get(n, k - 1 - i).multiply(Z.NEG_ONE.pow(i)).multiply(Binomial.binomial(k - 1, i)));
  }
}
