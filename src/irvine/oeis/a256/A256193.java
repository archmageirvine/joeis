package irvine.oeis.a256;
// Generated by gen_seq4.pl 2026-08-17.ack/mtripol

import irvine.math.MemoryFunctionInt2;
import irvine.math.group.IntegerField;
import irvine.math.group.PolynomialRingField;
import irvine.math.polynomial.Polynomial;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A256193 Number T(n,k) of partitions of n into two sorts of parts having exactly k parts of the second sort; triangle T(n,k), n&gt;=0, 0&lt;=k&lt;=n, read by rows.
 * @author Georg Fischer
 */
public class A256193 extends BaseTriangle implements DirectArray {

  protected static final PolynomialRingField<Z> RING = new PolynomialRingField<>(IntegerField.SINGLETON);

  /** Construct the sequence. */
  public A256193() {
    super(0, 0, 0);
  }

  final MemoryFunctionInt2<Polynomial<Z>> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Polynomial<Z> compute(final int n, final int i) {
      return
        (n == 0) ? RING.one() : (i < 1 ? RING.zero() : RING.sum(0, n / i, j -> RING.multiply(get(n - i * j, i - 1), RING.sum(0, j, t -> RING.multiply(RING.pow(RING.x(), t), Binomial.binomial(j, t))))));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return mB.get(n, n).coeff(k);
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
