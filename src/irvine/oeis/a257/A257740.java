package irvine.oeis.a257;
// Generated by gen_seq4.pl 2026-08-16.ack/mtrifun

import irvine.math.MemoryFunctionInt2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A257740 Number T(n,k) of multisets of nonempty words with a total of n letters over k-ary alphabet such that all k letters occur at least once in the multiset; triangle T(n,k), n&gt;=0, 0&lt;=k&lt;=n, read by rows.
 * @author Georg Fischer
 */
public class A257740 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A257740() {
    super(0, 0, 0);
  }

  final MemoryFunctionInt2<Z> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Z compute(final int n, final int k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(1, n, j -> Integers.SINGLETON.sumdiv(j, d -> Z.valueOf(k).pow(d).multiply(d)).multiply(get(n - j, k))).divide(n);
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return Integers.SINGLETON.sum(0, k, i -> mB.get(n, k - i).multiply(Binomial.binomial(k, i)).multiply(Z.NEG_ONE.pow(i)));
  }

  @Override
  public Z a(final long n, final long k) {
    return triangleElement((int) n, (int) k);
  }
}
