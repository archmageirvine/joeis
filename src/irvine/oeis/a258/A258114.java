package irvine.oeis.a258;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A258114 E.g.f.: Sum_{n&gt;=0} x^n * cosh(n*x).
 * E.g.f.: (1 - x*cosh(x)) / (1 - 2*x*cosh(x) + x^2)
 * @author Georg Fischer
 */
public class A258114 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.one(), RING.multiply(RING.x(), RING.cosh(RING.x(), mN), mN)), RING.add(RING.subtract(RING.one(), RING.multiply(RING.monomial(Q.TWO, 1), RING.cosh(RING.x(), mN), mN)), RING.pow(RING.x(), 2, mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
