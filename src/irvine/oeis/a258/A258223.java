package irvine.oeis.a258;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction4;
import irvine.math.function.Functions;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A258223
 * @author Georg Fischer
 */
public class A258223 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A258223() {
    super(0, 0, 0);
  }

  final MemoryFunction4<Long, Q> mB = new MemoryFunction4<>() {
    @Override
    protected Q compute(final Long x, final Long y, final Long t, final Long k) {
      return
        y > x || y < 0 ? new Q(0) : (x == 0 ? new Q(1) : get(x - 1, y - 1, 0L, k).multiply(t == 1L ? new Q(k * x + y).divide(y) : new Q(1)).add(get(x - 1, y + 1, 1L, k)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return Rationals.SINGLETON.sum(0, k, i -> mB.get(2 * n, 0L, 0L, i).multiply(Z.NEG_ONE.pow(k - i)).multiply(Binomial.binomial(k, i))).divide(Functions.FACTORIAL.z(k)).num();
  }
}
