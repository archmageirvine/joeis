package irvine.oeis.a258;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A258369 Stirling-Bernoulli transform of A027656.
 * E.g.f.: 1/(exp(x)*(2 - exp(x))^2)
 * @author Georg Fischer
 */
public class A258369 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.multiply(RING.exp(RING.x(), mN), RING.pow(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), 2, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
