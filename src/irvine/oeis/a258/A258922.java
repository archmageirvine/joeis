package irvine.oeis.a258;
// generated by patch_offset.pl at 2022-08-27 22:54

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A258922 E.g.f. satisfies: A(x) = 1/(3 - 2*exp(x*A(x))).
 * E.g.f.: <code>divx(reverse(3*x-2*x*exp(x)))</code>
 * @author Georg Fischer
 */
public class A258922 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A258922() {
    super(0);
    next();
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return n == 0 ? RING.one() : RING.divide(RING.reversion(RING.subtract(RING.monomial(Q.THREE, 1), RING.multiply(RING.multiply(RING.monomial(Q.TWO, 0), RING.x(), n), RING.exp(RING.x(), n), n)), n), new Q(n));
  }
}
