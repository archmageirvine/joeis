package irvine.oeis.a258;
// Generated by gen_seq4.pl egfu/egfux at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A258923 E.g.f. satisfies: A(x) = 1/(4 - 3*exp(x*A(x))).
 * E.g.f.: <code>divx(reverse(4*x-3*x*exp(x)))</code>
 * @author Georg Fischer
 */
public class A258923 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A258923() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return n == 0 ? RING.one() : RING.divide(RING.reversion(RING.subtract(RING.monomial(Q.FOUR, 1), RING.multiply(RING.multiply(RING.monomial(Q.THREE, 0), RING.x(), n), RING.exp(RING.x(), n), n)), n), new Q(n));
  }
}
