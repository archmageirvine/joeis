package irvine.oeis.a259;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A259062 E.g.f.: Series_Reversion( -x + 2*x*exp(-x) ).
 * E.g.f.: reverse( -x + 2*x*exp(-x) )
 * @author Georg Fischer
 */
public class A259062 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A259062() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.reversion(RING.add(RING.subtract(RING.zero(), RING.x()), RING.multiply(RING.monomial(new Q(2), 1), RING.exp(RING.subtract(RING.zero(), RING.x()), mN), mN)), mN);
  }
}
