package irvine.oeis.a259;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A259063 E.g.f.: Series_Reversion( 3*x - 2*x*exp(x) ).
 * E.g.f.: reverse( 3*x - 2*x*exp(x) )
 * @author Georg Fischer
 */
public class A259063 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A259063() {
    super(1);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.reversion(RING.subtract(RING.monomial(Q.THREE, 1), RING.multiply(RING.multiply(RING.monomial(Q.TWO, 0), RING.x(), mN), RING.exp(RING.x(), mN), mN)), mN);
  }
}
