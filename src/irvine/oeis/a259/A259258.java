package irvine.oeis.a259;
// Generated by gen_seq4.pl decsolvr at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A259258 Decimal expansion of the smallest positive solution to sin(x) + cos(x) + tan(x) = 0.
 * Equation; sin(x)+cos(x)+tan(x)
 * @author Georg Fischer
 */
public class A259258 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A259258() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.sin().add(x.cos()).add(REALS.tan(x));
      }
    }.inverseMonotone(CR.valueOf(4.008), CR.valueOf(4.172)).execute(CR.ZERO));
  }
}
