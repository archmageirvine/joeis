package irvine.oeis.a259;
// Generated by gen_seq4.pl decsolvnr at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A259259 Decimal expansion of the largest negative solution to sin(x) + cos(x) + tan(x) = 0 (negated).
 * Equation; sin(x)+cos(x)+tan(x)
 * @author Georg Fischer
 */
public class A259259 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence */
  public A259259() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.sin().add(x.cos()).add(REALS.tan(x));
      }
    }.inverseMonotone(CR.valueOf(-0.452), CR.valueOf(-0.434)).execute(CR.ZERO).negate());
  }
}
