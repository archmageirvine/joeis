package irvine.oeis.a259;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A259533 Number of restricted barred preferential arrangements of an n-set having 3 bars in which 3 fixed sections are restricted sections and 1 section is a free section.
 * E.g.f.: exp(3*x)/(2-exp(x))
 * @author Georg Fischer
 */
public class A259533 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.monomial(Q.THREE, 1), mN), RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
