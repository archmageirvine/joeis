package irvine.oeis.a259;
// Generated by gen_seq4.pl 2025-02-18.ack/filnum at 2025-02-18 23:09

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A259585 Numbers k such that [r[s*k]] - [s[r*k]] = -1, where r = sqrt(2), s=sqrt(3), and [ ] = floor.
 * @author Georg Fischer
 */
public class A259585 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A259585() {
    super(1, 1, k -> {
      final CR s = CR.THREE.sqrt();
      final CR r = CR.SQRT2;
      return r.multiply(s.multiply(k).floor()).floor().subtract(s.multiply(r.multiply(k).floor()).floor()).compareTo(Z.NEG_ONE) == 0;
    });
  }
}
