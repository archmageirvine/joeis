package irvine.oeis.a259;
// Generated by gen_seq4.pl 2025-02-20.ack/filnum at 2025-02-20 23:05

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A259587 Numbers k such that [r[s*k]] - [s[r*k]] = 2, where r = sqrt(2), s=sqrt(3), and [ ] = floor.
 * @author Georg Fischer
 */
public class A259587 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A259587() {
    super(1, 1, k -> {
      final CR s = CR.THREE.sqrt();
      final CR r = CR.SQRT2;
      return r.multiply(s.multiply(k).floor()).floor().subtract(s.multiply(r.multiply(k).floor()).floor()).compareTo(Z.ONE) == 0;
    });
  }
}
