package irvine.oeis.a259;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 14:55

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A259695 a(n) = Sum_{k=1..n-1} k^7 * sigma(k) * sigma(n-k).
 * a(n)=sum(k=1,n-1,k^7*sigma(k,1)*sigma(n-k,1))
 * @author Georg Fischer
 */
public class A259695 extends LambdaSequence {

  /** Construct the sequence. */
  public A259695() {
    super(1, n -> Integers.SINGLETON.sum(1, n - 1, k -> Z.valueOf(k).pow(7).multiply(Jaguar.factor(k).sigma(1)).multiply(Jaguar.factor(n - k).sigma(1))));
  }
}
