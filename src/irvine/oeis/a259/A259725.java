package irvine.oeis.a259;
// Generated by gen_seq4.pl 2025-02-18.ack/filnum at 2025-02-18 23:09

import irvine.math.cr.CR;
import irvine.oeis.FilterNumberSequence;

/**
 * A259725 Numbers k such that [r[s*k]] = [s[r*k]], where r = sqrt(2), s=sqrt(3), and [ ] = floor.
 * @author Georg Fischer
 */
public class A259725 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A259725() {
    super(1, 1, k -> {
      final CR s = CR.THREE.sqrt();
      final CR r = CR.SQRT2;
      return r.multiply(s.multiply(k).floor()).floor().compareTo(s.multiply(r.multiply(k).floor()).floor()) == 0;
    });
  }
}
