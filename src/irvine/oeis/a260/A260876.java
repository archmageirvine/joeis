package irvine.oeis.a260;
// Generated by gen_seq4.pl 2026-09-04.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A260876
 * @author Georg Fischer
 */
public class A260876 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A260876() {
    super(0, 0, 0, 1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long m, final Long n) {
      return
        m == 0 ? Functions.PARTITIONS.z(n) : (n == 0 ? Z.ONE : Integers.SINGLETON.sum(1, n, k -> Binomial.binomial(m * n - 1, m * k - 1).multiply(get(m, n - k))));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
