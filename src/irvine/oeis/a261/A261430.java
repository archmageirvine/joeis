package irvine.oeis.a261;
// Generated by gen_seq4.pl 2026-08-27.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A261430 Number A(n,k) of permutations p of [n] without fixed points such that p^k = Id; square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals.
 * @author Georg Fischer
 */
public class A261430 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A261430() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        (n < 0) ? Z.ZERO : (n == 0 ? Z.ONE : Integers.SINGLETON.sumdiv(k, j -> (j == 1) ? Z.ZERO : Integers.SINGLETON.product(1, j - 1, i -> Z.valueOf(n - i)).multiply(get(n - j, k))));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
