package irvine.oeis.a262;
// Generated by gen_seq4.pl 2026-08-27.ack/mtrilon

import irvine.math.MemoryFunction3;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A262372
 * @author Georg Fischer
 */
public class A262372 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A262372() {
    super(0, 0, 0);
  }

  final MemoryFunction3<Long, Z> mB = new MemoryFunction3<>() {
    @Override
    protected Z compute(final Long u, final Long o, final Long h) {
      return
        u + o == 0
          ? Z.ONE
          : Integers.SINGLETON.sum(1, u, j -> Integers.SINGLETON.sum(1, u + o - h, i -> get(u - j, o + j - 1, h + i - 1))).add(Integers.SINGLETON.sum(1, o, j -> Integers.SINGLETON.sum(1, h, i -> get(u + j - 1, o - j, h - i))));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return k == 0 ? (n == 0 ? Z.ONE : Z.ZERO) : mB.get(k - 1, n - k, n - k);
  }
}
