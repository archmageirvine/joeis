package irvine.oeis.a263;
// Generated by gen_seq4.pl egfu/egfue at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A263185 E.g.f. is the series S(x) such that C(x) + i*S(x) = 1 + i * Integral C(x)/(C(x) + i*S(x)) dx, odd powers only, where i^2 = -1.
 * E.g.f.: reverse(2*x - arctan(x))
 * @author Georg Fischer
 */
public class A263185 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A263185() {
    super(1, 0, 2);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.reversion(RING.subtract(RING.monomial(Q.TWO, 1), RING.atan(RING.x(), n)), n);
  }
}
