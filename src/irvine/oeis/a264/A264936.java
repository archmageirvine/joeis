package irvine.oeis.a264;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A264936 Decimal expansion of the positive root of x^(x^x) = gamma.
 * Equation; x^(x^x)-(gamma)
 * @author Georg Fischer
 */
public class A264936 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A264936() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(x.pow(x)).subtract(CR.GAMMA);
      }
    }.inverseMonotone(CR.valueOf(0.446), CR.valueOf(0.465)).execute(CR.ZERO));
  }
}
