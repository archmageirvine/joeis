package irvine.oeis.a269;
// Generated by gen_seq4.pl 2024-05-27/trecpab

import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;

/**
 * A269939 Triangle read by rows, Ward numbers T(n, k) = Sum_{m=0..k} (-1)^(m + k) * binomial(n + k, n + m) * Stirling2(n + m, m), for n &gt;= 0, 0 &lt;= k &lt;= n.
 * @author Georg Fischer
 */
public class A269939 extends BaseTriangle {

  /** Construct the sequence. */
  public A269939() {
    super(0, 0, 0);
    hasRAM(false);
  }

  @Override
  public Z triangleElement(final int n, final int k) {
    return n == 0 ? Z.ONE : get(n - 1, k - 1).multiply(n + k - 1).add(get(n - 1, k).multiply(k));
  }
}
