package irvine.oeis.a272;
// Generated by gen_seq4.pl egfu/egfue at 2021-12-04 17:50

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A272158 Expansion of e.g.f.: (sin(x) + sin(4*x)) / sin(5*x), even-indexed terms only.
 * E.g.f.: (cos(x) + cos(4*x)) / (1 + cos(5*x))
 * @author Georg Fischer
 */
public class A272158 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A272158() {
    super(0, 0, 2);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.add(RING.cos(RING.x(), n), RING.cos(RING.monomial(Q.FOUR, 1), n)), RING.add(RING.one(), RING.cos(RING.monomial(Q.FIVE, 1), n)), n);
  }
}
