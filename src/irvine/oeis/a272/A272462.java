package irvine.oeis.a272;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-02 19:59

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A272462 E.g.f.: (sin(3^2*x) + sin(4^2*x)) / sin(5^2*x).
 * E.g.f.: (cos(3^2*x) + cos(4^2*x))/(1 + cos(5^2*x)), even powers
 * @author Georg Fischer
 */
public class A272462 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.cos(RING.multiply(RING.pow(RING.monomial(Q.THREE, 0), 2, mN), RING.x(), mN), mN), RING.cos(RING.multiply(RING.pow(RING.monomial(Q.FOUR, 0), 2, mN), RING.x(), mN), mN)), RING.add(RING.one(), RING.cos(RING.multiply(RING.pow(RING.monomial(Q.FIVE, 0), 2, mN), RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
