package irvine.oeis.a272;
// Generated by gen_seq4.pl egfu/egfue at 2021-12-04 17:50

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A272467 E.g.f.: (sin(2*x) + sin(3*x)) / sin(5*x).
 * E.g.f.: (cos(2*x) + cos(3*x)) / (1 + cos(5*x))
 * @author Georg Fischer
 */
public class A272467 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A272467() {
    super(0, 0, 2);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.series(RING.add(RING.cos(RING.monomial(Q.TWO, 1), mN), RING.cos(RING.monomial(Q.THREE, 1), mN)), RING.add(RING.one(), RING.cos(RING.monomial(Q.FIVE, 1), mN)), mN);
  }
}
