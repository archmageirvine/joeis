package irvine.oeis.a272;
// Generated by gen_seq4.pl 2026-08-16.ack/mlamfun

import irvine.math.MemoryFunctionInt3;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A272515 Number of set partitions of [n] into three blocks with distinct sizes.
 * @author Georg Fischer
 */
public class A272515 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A272515() {
    this(6, 3);
  }

  /**
   * Generic constructor with parameter.
   * @param offset first index
   * @param colNo column number
   */
  public A272515(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int i, final int t) {
      return
        t > i || t * (t + 1) / 2 > n || t * (2 * i + 1 - t) / 2 < n
          ? Z.ZERO
          : n == 0 ? Z.ONE : get(n, i - 1, t).add(i > n ? Z.ZERO : get(n - i, i - 1, t - 1).multiply(Binomial.binomial(n, i)));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n, n, mColNo);
  }
}
