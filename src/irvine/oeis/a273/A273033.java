package irvine.oeis.a273;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-02 19:59

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A273033 E.g.f.: (sin(3*x) + sin(4*x)) / sin(7*x).
 * E.g.f.: (cos(3*x) + cos(4*x)) / (1 + cos(7*x)), even powers
 * @author Georg Fischer
 */
public class A273033 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.add(RING.cos(RING.monomial(Q.THREE, 1), mN), RING.cos(RING.monomial(Q.FOUR, 1), mN)), RING.add(RING.one(), RING.cos(RING.monomial(Q.SEVEN, 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
