package irvine.oeis.a275;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A275385 Number of labeled functional digraphs on n nodes with only odd sized cycles and such that every vertex is at a distance of at most 1 from a cycle.
 * E.g.f.: sqrt((1 + x*exp(x))/(1 - x*exp(x)))
 * @author Georg Fischer
 */
public class A275385 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.log(RING.series(RING.add(RING.one(), RING.multiply(RING.x(), RING.exp(RING.x(), mN), mN)), RING.subtract(RING.one(), RING.multiply(RING.x(), RING.exp(RING.x(), mN), mN)), mN), mN), RING.monomial(Q.HALF, 0), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
