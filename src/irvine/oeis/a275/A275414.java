package irvine.oeis.a275;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction3;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A275414
 * @author Georg Fischer
 */
public class A275414 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A275414() {
    super(1, 1, 1);
  }

  final MemoryFunction3<Long, Z> mB = new MemoryFunction3<>() {
    @Override
    protected Z compute(final Long n, final Long i, final Long p) {
      return
        p > n ? Z.ZERO : (n == 0 ? Z.ONE : (Functions.MIN.i(i, p) < 1 ? Z.ZERO : Integers.SINGLETON.sum(0, Functions.MIN.i(n / i, p), j -> get(n - i * j, i - 1, p - j).multiply(Binomial.binomial(Z.THREE.pow(i).add(j - 1), j)))));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, n, k);
  }
}
