package irvine.oeis.a275;
// Generated by gen_seq4.pl 2025-01-03.ack/egfsi at 2025-01-06 00:11

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A275759 E.g.f. satisfies: A(x)^A(-x) = exp(2*x) * A(-x)^A(x).
 * @author Georg Fischer
 */
public class A275759 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A275759() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.one(), RING.reversion(RING.log(RING.pow(RING.series(RING.exp(RING.multiply(RING.log(RING.onePlusXToTheN(1), mN), RING.oneMinusXToTheN(1), mN), mN), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.onePlusXToTheN(1), mN), mN), mN), Q.HALF, mN), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
