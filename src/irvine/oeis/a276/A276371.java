package irvine.oeis.a276;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A276371 E.g.f.: exp(x/2)/(2 - exp(2*x))^(1/4).
 * E.g.f.: exp(x/2)/(2 - exp(2*x))^(1/4)
 * @author Georg Fischer
 */
public class A276371 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.series(RING.x(), RING.monomial(new Q(2), 0), mN), mN), RING.exp(RING.multiply(RING.log(RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.monomial(new Q(2), 1), mN)), mN), RING.series(RING.one(), RING.monomial(new Q(4), 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
