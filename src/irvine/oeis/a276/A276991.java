package irvine.oeis.a276;
// Generated by gen_seq4.pl 2026-03-14.ack/lambdan at 2026-03-14 23:12

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.oeis.LambdaSequence;

/**
 * A276991 a(n) = ((phi; phi)_n - (1-phi; 1-phi)_n)/sqrt(5), where (q; q)_n is the q-Pochhammer symbol, phi = (1+sqrt(5))/2 is the golden ratio.
 * @author Georg Fischer
 */
public class A276991 extends LambdaSequence {

  /** Construct the sequence. */
  public A276991() {
    super(0, n -> {
      final CR p1 = CR.ONE.subtract(CR.PHI);
      return ComputableReals.SINGLETON.product(0, n - 1, j -> CR.ONE.subtract(CR.PHI.multiply(CR.PHI.pow(j)))).subtract(ComputableReals.SINGLETON.product(0, n - 1, j -> CR.ONE.subtract(p1.multiply(p1.pow(j))))).divide(CR.FIVE.sqrt()).floor();
    });
  }
}
