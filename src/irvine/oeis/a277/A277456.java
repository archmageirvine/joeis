package irvine.oeis.a277;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A277456 a(n) = 1 + Sum_{k=1..n} binomial(n,k) * 3^k * k^k.
 * a(n) = 1 + sum(k=1,n,binom(n,k) * 3^k * k^k);
 * @author Georg Fischer
 */
public class A277456 extends LambdaSequence {

  /** Construct the sequence. */
  public A277456() {
    super(0, n -> (Z.ONE).add(Integers.SINGLETON.sum(1, n, k -> Binomial.binomial(n, k).multiply(Z.THREE.pow(k)).multiply(Z.valueOf(k).pow(k)))));
  }
}
