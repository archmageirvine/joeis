package irvine.oeis.a277;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A277536
 * @author Georg Fischer
 */
public class A277536 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A277536() {
    super(0, 0, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : (k == 0 ? Z.ZERO : Integers.SINGLETON.sum(0, n - 1, j -> Binomial.binomial(n - 1, j).multiply(get(j, k)).multiply(Integers.SINGLETON.sum(1, n - j, i -> Binomial.binomial(n - j, i).multiply(Z.NEG_ONE.pow(i)).multiply(get(n - j - i, k - 1).multiply(Functions.FACTORIAL.z(i - 1)))))).negate());
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, Functions.MIN.l(k, n)).subtract(k == 0 ? Z.ZERO : mB.get(n, Functions.MIN.l(k - 1, n)));
  }
}
