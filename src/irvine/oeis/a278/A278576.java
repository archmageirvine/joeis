package irvine.oeis.a278;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A278576 E.g.f. = exp(5*(exp(t)-1) + 3*(exp(t)-1)^2).
 * E.g.f.: exp(5*(exp(x)-1) + 3*(exp(x)-1)^2)
 * @author Georg Fischer
 */
public class A278576 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.multiply(RING.monomial(new Q(5), 0), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.multiply(RING.monomial(new Q(3), 0), RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 2, mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
