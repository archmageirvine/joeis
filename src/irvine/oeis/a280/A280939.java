package irvine.oeis.a280;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A280939 Expansion of e.g.f.: 2*sinh(x/2) / sqrt(2 - exp(x)).
 * E.g.f.: 2*sinh(x/2) / sqrt(2 - exp(x))
 * @author Georg Fischer
 */
public class A280939 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.monomial(Q.TWO, 0), RING.sinh(RING.series(RING.x(), RING.monomial(Q.TWO, 0), mN), mN), mN), RING.sqrt(RING.subtract(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
