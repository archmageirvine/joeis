package irvine.oeis.a282;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A282988 Triangle of partitions of an n-set into boxes of size &gt;= m.
 * @author Georg Fischer
 */
public class A282988 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A282988() {
    super(1, 1, 1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(k, n, j -> get(n - j, k).multiply(Binomial.binomial(n - 1, j - 1)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return Integers.SINGLETON.sum(0, n - k, i -> Binomial.binomial(n - 1, i + k - 1).multiply(mB.get(n - i - k, k)));
  }
}
