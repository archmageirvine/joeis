package irvine.oeis.a285;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A285064 Row sums of Sheffer triangle S2[4,1] = A285061.
 * E.g.f.: exp(x)*exp(exp(4*x) - 1)
 * @author Georg Fischer
 */
public class A285064 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.exp(RING.x(), mN), RING.exp(RING.subtract(RING.exp(RING.monomial(Q.FOUR, 1), mN), RING.one()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
