package irvine.oeis.a287;
// Generated by gen_seq4.pl 2026-09-04.ack/marrlon

import irvine.math.MemoryFunction4;
import irvine.math.function.Functions;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A287216
 * @author Georg Fischer
 */
public class A287216 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A287216() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction4<Long, Z> mB = new MemoryFunction4<>() {
    @Override
    protected Z compute(final Long n, final Long k, final Long m, final Long l) {
      return
        n < 1 ? Z.ONE : (l - n > k ? Z.ZERO : get(n - 1, k, m + 1, n)).add(get(n - 1, k, m, l).multiply(m));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n - 1, Functions.MIN.l(k, n - 1), 1L, n);
  }
}
