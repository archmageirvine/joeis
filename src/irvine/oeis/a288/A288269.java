package irvine.oeis.a288;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A288269 Expansion of e.g.f.: exp(Sum_{k&gt;=1} (k-1)*k*x^k).
 * E.g.f.: exp(2*x^2/(1 - x)^3)
 * @author Georg Fischer
 */
public class A288269 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.monomial(Q.TWO, 2), RING.pow(RING.oneMinusXToTheN(1), 3, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
