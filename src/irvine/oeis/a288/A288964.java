package irvine.oeis.a288;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A288964 Number of key comparisons to sort all n! permutations of n elements by quicksort.
 * E.g.f.: -2*log(1-x)/(1-x)^2 - 2*x/(1-x)^2
 * @author Georg Fischer
 */
public class A288964 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.negate(RING.series(RING.multiply(RING.monomial(Q.TWO, 0), RING.log(RING.oneMinusXToTheN(1), mN), mN), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN)), RING.series(RING.monomial(Q.TWO, 1), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
