package irvine.oeis.a290;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A290690 E.g.f.: exp(Sum_{k&gt;=1} (k-1)^3*x^k).
 * E.g.f.: exp(x^2*(1 + 4*x + x^2)/(1-x)^4)
 * @author Georg Fischer
 */
public class A290690 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.multiply(RING.pow(RING.x(), 2, mN), RING.add(RING.add(RING.one(), RING.monomial(new Q(4), 1)), RING.pow(RING.x(), 2, mN)), mN), RING.pow(RING.oneMinusXToTheN(1), 4, mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
