package irvine.oeis.a290;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A290716 Number of minimal dominating sets in the n-triangular (Johnson) graph.
 * E.g.f.: exp(x^2/2) + x*exp(x*exp(x) - (x+x^2/2))
 * @author Georg Fischer
 */
public class A290716 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.exp(RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN), mN), RING.multiply(RING.x(), RING.exp(RING.subtract(RING.multiply(RING.x(), RING.exp(RING.x(), mN), mN), RING.add(RING.x(), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN))), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
