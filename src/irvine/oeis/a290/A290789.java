package irvine.oeis.a290;
// Generated by gen_seq4.pl 2026-09-06.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A290789 A(n,k) is the n-th Carlitz-Riordan q-Catalan number (recurrence version) for q = -k; square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals.
 * @author Georg Fischer
 */
public class A290789 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A290789() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(0, n - 1, j -> get(j, k).multiply(get(n - j - 1, k)).multiply(Z.valueOf(-k).pow(j)));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
