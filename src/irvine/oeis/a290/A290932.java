package irvine.oeis.a290;
// Generated by gen_seq4.pl 2026-08-17.ack/mlamfun

import irvine.math.MemoryFunctionInt2;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A290932 Sum of the LCM of cycle lengths over all endofunctions on [n].
 * @author Georg Fischer
 */
public class A290932 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A290932() {
    this(0, 0);
  }

  /**
   * Generic constructor with parameters.
   * @param offset first index
   * @param colNo column number
   */
  public A290932(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt2<Z> mB = new MemoryFunctionInt2<>() {
    @Override
    protected Z compute(final int n, final int m) {
      return
        n == 0
          ? Z.valueOf(m)
          : Integers.SINGLETON.sum(1, n, j -> Functions.FACTORIAL.z(j - 1).multiply(get(n - j, Functions.LCM.i(m, j)).multiply(Binomial.binomial(n - 1, j - 1))));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return Integers.SINGLETON.sum(0, n, j -> mB.get(j, 1).multiply(Z.valueOf(n).pow(n - j)).multiply(Binomial.binomial(n - 1, j - 1)));
  }
}
