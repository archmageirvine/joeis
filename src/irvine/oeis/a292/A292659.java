package irvine.oeis.a292;
// Generated by gen_seq4.pl 2026-06-02.ack/lambdan at 2026-06-02 22:17

import irvine.math.cr.CR;
import irvine.oeis.LambdaSequence;

/**
 * A292659 Rank of n*sin(1) when all the numbers h, j*cos(1), k*sin(1), for h&gt;=1, j&gt;=1, k&gt;=1, are jointly ranked.
 * @author Georg Fischer
 */
public class A292659 extends LambdaSequence {

  /** Construct the sequence. */
  public A292659() {
    super(1, n -> {
      final CR r = CR.ONE;
      final CR s = CR.ONE.cos();
      final CR t = CR.ONE.sin();
      return t.multiply(n).divide(r).floor().add(t.multiply(n).divide(s).floor()).add(n);
    });
  }
}
