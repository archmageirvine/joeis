package irvine.oeis.a292;
// Generated by gen_seq4.pl 2026-08-16.ack/marrfun

import irvine.math.MemoryFunctionInt3;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A292804 Number A(n,k) of sets of nonempty words with a total of n letters over k-ary alphabet; square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals.
 * @author Georg Fischer
 */
public class A292804 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A292804() {
    super(0, 0, 0, -1);
  }

  final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int i, final int k) {
      return n == 0 ? Z.ONE : (i < 1 ? Z.ZERO : Integers.SINGLETON.sum(0, n / i, j -> get(n - i * j, i - 1, k).multiply(Binomial.binomial(Z.valueOf(k).pow(i), j))));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return mB.get(n, n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return matrixElement((int) n, (int) k);
  }
}
