package irvine.oeis.a292;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A292934 E.g.f.: x^2/(x+3-2*exp(x)).
 * E.g.f.: x^2/(x+3-2*exp(x))
 * @author Georg Fischer
 */
public class A292934 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.pow(RING.x(), 2, mN), RING.subtract(RING.add(RING.x(), RING.monomial(Q.THREE, 0)), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
