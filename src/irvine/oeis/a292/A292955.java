package irvine.oeis.a292;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A292955 E.g.f.: exp(-1/4! * x^4 * exp(x)).
 * E.g.f.: exp(-1/24 * x^4 * exp(x))
 * @author Georg Fischer
 */
public class A292955 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A292955() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.negate(RING.multiply(RING.multiply(RING.series(RING.one(), RING.monomial(new Q(24), 0), n), RING.pow(RING.x(), 4, n), n), RING.exp(RING.x(), n), n)), n);
  }
}
