package irvine.oeis.a292;
// Generated by gen_seq4.pl 2026-06-02.ack/arrlamb at 2026-06-02 22:17

import irvine.math.cr.CR;
import irvine.math.z.Integers;
import irvine.oeis.triangle.LambdaArray;

/**
 * A292956 Rectangular array by antidiagonals: T(n,m) = rank of n*(r+m) when all the numbers k*(r+h), where r = sqrt(2), k&gt;=1, h&gt;=0, are jointly ranked.
 * @author Georg Fischer
 */
public class A292956 extends LambdaArray {

  /** Construct the sequence. */
  public A292956() {
    super(1, 1, 1, -1, (n, m) -> {
      final CR r = CR.SQRT2;
      return Integers.SINGLETON.sum(1, CR.valueOf(m - 1).multiply(n).divide(r).add(n).floor().intValueExact(), k -> CR.ONE.subtract(r).add(r.add(m - 1).multiply(n).divide(k)).floor());
    });
  }
}
