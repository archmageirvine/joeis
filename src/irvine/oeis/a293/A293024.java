package irvine.oeis.a293;
// Generated by gen_seq4.pl 2026-09-06.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A293024 Square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals, where column k is the expansion of e.g.f. exp(exp(x) - Sum_{i=0..k} x^i/i!).
 * @author Georg Fischer
 */
public class A293024 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A293024() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(1 + k, n, j -> get(n - j, k).multiply(Binomial.binomial(n - 1, j - 1)));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
