package irvine.oeis.a294;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A294345 Sum of the products of the smaller and larger parts of the Goldbach partitions of n into two distinct parts.
 * a(n)=sum(i=2, floor((n-1)/2), i*(n-i)*(primepi(i)-primepi(i-1))*(primepi(n-i)-primepi(n-i-1)))
 * @author Georg Fischer
 */
public class A294345 extends LambdaSequence {

  /** Construct the sequence. */
  public A294345() {
    super(1, n -> Integers.SINGLETON.sum(2, (n - 1) / 2, i -> Z.valueOf(i).multiply(Z.valueOf(n - i)).multiply(Puma.primePiZ(i).subtract(Puma.primePiZ(i - 1))).multiply(Puma.primePiZ(n - i).subtract(Puma.primePiZ(n - i - 1)))));
  }
}
