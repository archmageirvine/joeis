package irvine.oeis.a294;
// Generated by gen_seq4.pl 2026-08-17.ack/marrfun

import irvine.math.MemoryFunctionInt3;
import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A294775 Number A(n,k) of partitions of 1 into exactly k*n+1 powers of 1/(k+1); square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals.
 * @author Georg Fischer
 */
public class A294775 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A294775() {
    super(0, 0, 0, -1);
  }

  final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int r, final int k) {
      return n < r ? Z.ZERO : (r == 0 ? Z.valueOf(n == 0 ? 1 : 0) : Integers.SINGLETON.sum(0, Functions.MIN.i(n, r), j -> get(n - j, k * (r - j), k)));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return k == 0 ? Z.ONE : mB.get(k * n + 1, 1, k + 1);
  }

  @Override
  public Z a(final long n, final long k) {
    return matrixElement((int) n, (int) k);
  }
}
