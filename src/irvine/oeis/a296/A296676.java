package irvine.oeis.a296;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A296676 Expansion of e.g.f. 1/(1 - arctanh(x)).
 * E.g.f.: 1/(1 + (log(1 - x) - log(1 + x))/2)
 * @author Georg Fischer
 */
public class A296676 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.add(RING.one(), RING.series(RING.subtract(RING.log(RING.oneMinusXToTheN(1), mN), RING.log(RING.onePlusXToTheN(1), mN)), RING.monomial(new Q(2), 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
