package irvine.oeis.a296;
// Generated by gen_seq4.pl egfsi/egfsio at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A296742 Expansion of e.g.f. arcsinh(x*sec(x)) (odd powers only).
 * E.g.f.: arcsinh(x*sec(x)), odd powers
 * @author Georg Fischer
 */
public class A296742 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 0) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.asinh(RING.multiply(RING.x(), RING.sec(RING.x(), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
