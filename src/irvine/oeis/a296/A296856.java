package irvine.oeis.a296;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A296856 Expansion of e.g.f. cosh(x*tan(x/2)) (even powers only).
 * E.g.f.: cosh(x*tan(x/2)), even powers
 * @author Georg Fischer
 */
public class A296856 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.cosh(RING.multiply(RING.x(), RING.tan(RING.series(RING.x(), RING.monomial(new Q(2), 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
