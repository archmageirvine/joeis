package irvine.oeis.a297;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-27 21:29

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A297053 Sum of the larger parts of the partitions of n into two parts such that the smaller part does not divide the larger.
 * a(n)=sum(i=1,floor(n/2),(n-i) * (1 - (floor(n/i) - floor((n-1)/i))))
 * @author Georg Fischer
 */
public class A297053 extends LambdaSequence {

  /** Construct the sequence. */
  public A297053() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 2, i -> Z.valueOf(n - i).multiply(Z.ONE.subtract(Z.valueOf(n / i).subtract(Z.valueOf(n - 1).divide(i))))));
  }
}
