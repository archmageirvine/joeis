package irvine.oeis.a297;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A297151 a(n) = Sum_{i=0..n} Sum_{j=0..n} Sum_{k=0..n} binomial(n,i)*binomial(n,j)*binomial(n,k)*max(i,j,k).
 * a(n)=n*2^(3*n-1)+3*n*2^(n-2)*binom(2*n, n)
 * @author Georg Fischer
 */
public class A297151 extends LambdaSequence {

  private int mN = -1;

  /** Construct the sequence. */
  public A297151() {
    super(0, n -> Z.valueOf(n).multiply(Z.ONE.shiftLeft(3 * n - 1)).add(Z.THREE.multiply(n).multiply(Z.ONE.shiftLeft(n - 2)).multiply(Binomial.binomial(2 * n, n))));
  }

  @Override
  public Z next() {
    final Z result = super.next();
    return (++mN == 1) ? Z.SEVEN : result;
  }
}

