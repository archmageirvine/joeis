package irvine.oeis.a297;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A297214 Expansion of e.g.f. exp(cos(sin(x))-1) (even powers only).
 * E.g.f.: exp(cos(sin(x))-1), even powers
 * @author Georg Fischer
 */
public class A297214 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.cos(RING.sin(RING.x(), mN), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
