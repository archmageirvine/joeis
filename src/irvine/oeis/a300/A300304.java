package irvine.oeis.a300;
// Generated by gen_seq4.pl decsolv at 2021-08-10 22:10

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A300304 Decimal expansion of the solution to 2*(1-x)^x = x^x.
 * Equation; 2*(1-x)^x-(x^x)
 * @author Georg Fischer
 */
public class A300304 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A300304() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(CR.ONE.subtract(x).pow(x)).subtract(x.pow(x));
      }
    }.inverseMonotone(CR.valueOf(0.708), CR.valueOf(0.737)).execute(CR.ZERO));
  }
}
