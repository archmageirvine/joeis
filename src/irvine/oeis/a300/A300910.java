package irvine.oeis.a300;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A300910 Expansion of e.g.f. 1/(1 - x)^(x/(1 - x)^2).
 * E.g.f.: 1/(1 - x)^(x/(1 - x)^2)
 * @author Georg Fischer
 */
public class A300910 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.series(RING.x(), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
