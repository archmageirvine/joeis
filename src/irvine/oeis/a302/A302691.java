package irvine.oeis.a302;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A302691 Number of broken alternating permutations of n things.
 * E.g.f.: (cos(x)-sin(x)+x-1)/(sin(x)-1)
 * @author Georg Fischer
 */
public class A302691 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.add(RING.subtract(RING.cos(RING.x(), mN), RING.sin(RING.x(), mN)), RING.x()), RING.one()), RING.subtract(RING.sin(RING.x(), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
