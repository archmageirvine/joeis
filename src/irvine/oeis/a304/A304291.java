package irvine.oeis.a304;
// Generated by gen_seq4.pl 2026-07-02.ack/filnum at 2026-07-02 22:33

import irvine.factor.util.FactorUtils;
import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A304291 Composite numbers k such that for all primes p dividing k, p-1 divides k-1 and p+1 divides k+1.
 * @author Georg Fischer
 */
public class A304291 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A304291() {
    super(1, 4, k -> !Predicates.PRIME.is(k) && FactorUtils.iterate(k, 0L, (x, p, e) -> x + (Z.valueOf(k).mod(p).isZero() ? ((Z.valueOf(k - 1).mod(p.subtract(1)).isZero() && Z.valueOf(k + 1).mod(p.add(1)).isZero()) ? 0 : 1) : 0)) == 0L);
  }
}
