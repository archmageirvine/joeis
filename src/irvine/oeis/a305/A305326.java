package irvine.oeis.a305;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A305326 Decimal expansion of the greatest x such that 1/x + 1/(x+1) + 1/(x+2) = 1.
 * Equation; 1/x+1/(x+1)+1/(x+2)-(1)
 * @author Georg Fischer
 */
public class A305326 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A305326() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.ONE))).add(CR.ONE.divide(x.add(CR.TWO))).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(2.170), CR.valueOf(2.258)).execute(CR.ZERO));
  }
}
