package irvine.oeis.a305;
// Generated by gen_seq4.pl decsolvn at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A305328 Decimal expansion of the least x such that 1/x + 1/(x+1) + 1/(x+2) = 1 (negated).
 * Equation; 1/x+1/(x+1)+1/(x+2)-(1)
 * @author Georg Fischer
 */
public class A305328 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A305328() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.ONE))).add(CR.ONE.divide(x.add(CR.TWO))).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-1.709), CR.valueOf(-1.641)).execute(CR.ZERO).negate());
  }
}
