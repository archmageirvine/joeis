package irvine.oeis.a306;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A306021 Number of set-systems spanning {1,...,n} in which all sets have the same size.
 * a(n)=sum(k=0,n,(-1)^(n-k)*binom(n,k)*(1+sum(d=1,k,2^binom(k,d)-1)))
 * @author Georg Fischer
 */
public class A306021 extends LambdaSequence {

  /** Construct the sequence. */
  public A306021() {
    super(0, n -> Integers.SINGLETON.sum(0, n, k -> ((((n - k) & 1) == 0) ? Z.ONE : Z.NEG_ONE).multiply(Binomial.binomial(n, k)).multiply(Z.ONE.add(Integers.SINGLETON.sum(1, k, d -> Z.TWO.pow(Binomial.binomial(k, d)).subtract(Z.ONE))))));
  }
}
