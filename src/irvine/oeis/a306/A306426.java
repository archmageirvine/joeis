package irvine.oeis.a306;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A306426 Sum of the smallest side lengths of all integer-sided triangles with perimeter n whose largest side length is prime.
 * a(n)=sum(k=1, floor(n/3), sum(i=k, floor((n-k)/2), k*(primepi(n-i-k)-primepi(n-i-k-1))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A306426 extends LambdaSequence {

  /** Construct the sequence. */
  public A306426() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.valueOf(k).multiply(Puma.primePiZ(n - i - k).subtract(Puma.primePiZ(n - i - k - 1))).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
