package irvine.oeis.a306;
// Generated by gen_seq4.pl 2026-09-23.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Binomial;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A306646 Square array A(n,k), n &gt;= 0, k &gt;= 1, read by antidiagonals, where column k is the expansion of g.f. (k+1-x^k)/(1-x^k-x^(k+1)).
 * @author Georg Fischer
 */
public class A306646 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A306646() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return n == 0 ? Z.valueOf(k + 1) : Rationals.SINGLETON.sum(1, k == 0 ? 0 : n / k, j -> new Q(Binomial.binomial(j, n - k * j), Z.valueOf(j))).multiply(n).num();
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k + 1);
  }
}
