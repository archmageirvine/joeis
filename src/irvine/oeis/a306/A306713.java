package irvine.oeis.a306;
// Generated by gen_seq4.pl 2026-09-23.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A306713 Square array A(n,k), n &gt;= 0, k &gt;= 1, read by antidiagonals, where column k is the expansion of g.f. 1/(1-x^k-x^(k+1)).
 * @author Georg Fischer
 */
public class A306713 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A306713() {
    super(0, 0, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return n == 0 ? Z.ONE : Integers.SINGLETON.sum(0, k == 0 ? 0 : n / k, j -> Binomial.binomial(j, n - k * j));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(k, n - k + 1);
  }
}
