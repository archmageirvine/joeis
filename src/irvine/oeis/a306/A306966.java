package irvine.oeis.a306;
// Generated by gen_seq4.pl decsolv at 2021-08-10 15:30

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A306966 Decimal expansion of t+t^2, where t is the tribonacci constant, the real root of x^3 - x^2 - x - 1.
 * Equation; x^3-x^2-x-1
 * @author Georg Fischer
 */
public class A306966 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A306966() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.pow(3).subtract(x.pow(CR.TWO)).subtract(x).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(1.83), CR.valueOf(1.84)).execute(CR.ZERO).pow(3).subtract(CR.ONE));
  }
}
