package irvine.oeis.a307;
// Generated by gen_seq4.pl egfu at 2021-12-07 21:31

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A307389 a(n) is the number of elements in the species of orbit polytopes in dimension n.
 * E.g.f.: exp((exp(2*x) - 2*exp(x) + 2*x + 1)/2)
 * @author Georg Fischer
 */
public class A307389 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A307389() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.series(RING.add(RING.add(RING.subtract(RING.exp(RING.monomial(Q.TWO, 1), n), RING.multiply(RING.monomial(Q.TWO, 0), RING.exp(RING.x(), n), n)), RING.monomial(Q.TWO, 1)), RING.one()), RING.monomial(Q.TWO, 0), n), n);
  }
}
