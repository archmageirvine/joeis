package irvine.oeis.a307;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A307684 Total volume of all rectangular prisms with dimensions r X s X t where r, s and t are the smallest, middle and largest parts in each partition of n into 3 parts.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),i*k*(n-i-k)))
 * @author Georg Fischer
 */
public class A307684 extends LambdaSequence {

  /** Construct the sequence. */
  public A307684() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.valueOf(i).multiply(k).multiply(Z.valueOf(n - i - k)))));
  }
}
