package irvine.oeis.a307;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A307719 Number of partitions of n into 3 mutually coprime parts.
 * a(n)=sum(j=1, floor(n/3), sum(i=j, floor((n-j)/2), floor(1/(gcd(i, j)*gcd(j, n-i-j)*gcd(i, n-i-j)))))
 * @author Georg Fischer
 */
public class A307719 extends LambdaSequence {

  /** Construct the sequence. */
  public A307719() {
    super(0, n -> Integers.SINGLETON.sum(1, n / 3, j -> Integers.SINGLETON.sum(j, (n - j) / 2, i -> Z.ONE.divide(Z.valueOf(i).gcd(j).multiply(Z.valueOf(j).gcd(n - i - j)).multiply(Z.valueOf(i).gcd(n - i - j))))));
  }
}
