package irvine.oeis.a307;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A307737 Take all the integer-sided acute triangles with perimeter n and sides a, b, and c such that a &lt;= b &lt;= c. a(n) is the sum of all the b's.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),i*(1-sign(floor((n-i-k)^2/(i^2+k^2))))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A307737 extends LambdaSequence {

  /** Construct the sequence. */
  public A307737() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.valueOf(i).multiply(Z.ONE.subtract(Z.valueOf(n - i - k).square().divide(Z.valueOf(i * i + k * k)).signum())).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
