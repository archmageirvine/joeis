package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308202 Number of integer-sided triangles with perimeter n such that the length of the smallest side is coprime to n.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),floor(1/gcd(k,n))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308202 extends LambdaSequence {

  /** Construct the sequence. */
  public A308202() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.ONE.divide(Z.valueOf(k).gcd(n)).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
