package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308218 Take the integer-sided obtuse triangles with perimeter n and sides a, b and c such that a &lt;= b &lt;= c. a(n) is the sum of all the b&apos;s.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),i*(1-sign(floor((i^2+k^2)/(n-i-k)^2)))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308218 extends LambdaSequence {

  /** Construct the sequence. */
  public A308218() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Z.valueOf(i).multiply(Z.ONE.subtract(Z.valueOf(i * i + k * k).divide(Z.valueOf(n - i - k).square()).signum())).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
