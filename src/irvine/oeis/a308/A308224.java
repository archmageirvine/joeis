package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308224 Sum of the largest sides of all integer-sided triangles with perimeter n whose side lengths are mutually coprime.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),(n-i-k)*floor(1/(gcd(i,k)*gcd(i,n-i-k)*gcd(k,n-i-k)))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308224 extends LambdaSequence {

  /** Construct the sequence. */
  public A308224() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Z.valueOf(n - i - k).multiply(Z.ONE.divide(Z.valueOf(i).gcd(k).multiply(Z.valueOf(i).gcd(n - i - k)).multiply(Z.valueOf(k).gcd(n - i - k)))).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
