package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308225 Take the integer-sided triangles with perimeter n and mutually coprime sides a, b and c such that a &lt;= b &lt;= c. a(n) is the sum of all the b&apos;s.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),i*floor(1/(gcd(i,k)*gcd(i,n-i-k)*gcd(k,n-i-k)))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308225 extends LambdaSequence {

  /** Construct the sequence. */
  public A308225() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Z.valueOf(i).multiply(Z.ONE.divide(Z.valueOf(i).gcd(k).multiply(Z.valueOf(i).gcd(n - i - k)).multiply(Z.valueOf(k).gcd(n - i - k)))).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
