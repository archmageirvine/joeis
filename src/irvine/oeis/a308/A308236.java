package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308236 Take apart the sides of each of the integer-sided triangles with perimeter n (at their vertices) and rearrange them orthogonally in 3-space so that their endpoints coincide at a single point. a(n) is the total surface area of all rectangular prisms enclose
 * a(n)=2*sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),(i*k+i*(n-i-k)+k*(n-i-k))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308236 extends LambdaSequence {

  /** Construct the sequence. */
  public A308236() {
    super(1, n -> Z.TWO.multiply(Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Z.valueOf(i).multiply(k).add(Z.valueOf(i).multiply(Z.valueOf(n - i - k))).add(Z.valueOf(k).multiply(Z.valueOf(n - i - k))).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum())))));
  }
}
