package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308254 Sum of the perimeters of all integer-sided triangles with perimeter n and nonprime side lengths.
 * a(n)=n*sum(k=1, floor(n/3), sum(i=k, floor((n-k)/2), (1-primepi(i)+primepi(i-1))*(1-primepi(k)+primepi(k-1))*(1-primepi(n-i-k)+primepi(n-i-k-1))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308254 extends LambdaSequence {

  /** Construct the sequence. */
  public A308254() {
    super(1, n -> Z.valueOf(n).multiply(Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.ONE.subtract(Puma.primePiZ(i)).add(Puma.primePiZ(i - 1)).multiply(Z.ONE.subtract(Puma.primePiZ(k)).add(Puma.primePiZ(k - 1))).multiply(Z.ONE.subtract(Puma.primePiZ(n - i - k)).add(Puma.primePiZ(n - i - k - 1))).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum())))));
  }
}
