package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308452 Number of integer-sided triangles with perimeter n and side lengths a, b and c such that a &lt;= b &lt;= c, and b is prime.
 * a(n)=sum(k=1, floor(n/3), sum(i=k, floor((n-k)/2), (primepi(i)-primepi(i-1))*sign(floor((i+k)/(n-i-k+1)))))
 * @author Georg Fischer
 */
public class A308452 extends LambdaSequence {

  /** Construct the sequence. */
  public A308452() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, ((n - k) / 2), i -> Puma.primePiZ(i).subtract(Puma.primePiZ(i - 1)).multiply(Z.valueOf(i + k).divide(Z.valueOf(n - i - k + 1)).signum()))));
  }
}
