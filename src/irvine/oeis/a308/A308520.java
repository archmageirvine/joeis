package irvine.oeis.a308;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A308520 Expansion of e.g.f. exp(x)*(1 + x + x^2/2)*(sec(x) + tan(x)).
 * E.g.f.: exp(x)*(1 + x + x^2/2)*(sec(x) + tan(x))
 * @author Georg Fischer
 */
public class A308520 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.multiply(RING.exp(RING.x(), mN), RING.add(RING.onePlusXToTheN(1), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN)), mN), RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
