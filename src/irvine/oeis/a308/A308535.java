package irvine.oeis.a308;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A308535 Expansion of e.g.f. 1/(1 - x)^log(1 + x) (even powers only).
 * E.g.f.: 1/(1 - x)^log(1 + x), even powers
 * @author Georg Fischer
 */
public class A308535 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.log(RING.onePlusXToTheN(1), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
