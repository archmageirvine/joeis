package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308655 Alternating partial sums of the prime gaps.
 * a(n)=sum(k=1, n, ((-1)^(k+1))*(prime(k+1)-prime(k)))
 * @author Georg Fischer
 */
public class A308655 extends LambdaSequence {

  /** Construct the sequence. */
  public A308655() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> ((((k + 1) & 1) == 0) ? Z.ONE : Z.NEG_ONE).multiply(Puma.primeZ(k + 1).subtract(Puma.primeZ(k)))));
  }
}
