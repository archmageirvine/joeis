package irvine.oeis.a308;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A308862 Expansion of e.g.f. 1/(1 - x*(1 + 3*x + x^2)*exp(x)).
 * E.g.f.: 1/(1 - x*(1 + 3*x + x^2)*exp(x))
 * @author Georg Fischer
 */
public class A308862 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.one(), RING.multiply(RING.multiply(RING.x(), RING.add(RING.add(RING.one(), RING.monomial(Q.THREE, 1)), RING.pow(RING.x(), 2, mN)), mN), RING.exp(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
