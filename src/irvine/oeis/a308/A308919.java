package irvine.oeis.a308;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A308919 Sum of all the parts in the partitions of n into 6 primes.
 * a(n)=n*sum(m=1, floor(n/6), sum(l=m, floor((n-m)/5), sum(k=l, floor((n-l-m)/4), sum(j=k, floor((n-k-l-m)/3), sum(i=j, floor((n-j-k-l-m)/2), (primepi(i)-primepi(i-1))*(primepi(j)-primepi(j-1))*(primepi(k)-primepi(k-1))*(primepi(l)-primepi(l-1))*(primepi(m)-primepi(m-1))*(primepi(n-i-j-k-l-m)-primepi(n-i-j-k-l-m-1)))))))
 * @author Georg Fischer
 */
public class A308919 extends LambdaSequence {

  /** Construct the sequence. */
  public A308919() {
    super(0, n -> Z.valueOf(n).multiply(Integers.SINGLETON.sum(1, n / 6, m -> Integers.SINGLETON.sum(m, (n - m) / 5, l -> Integers.SINGLETON.sum(l, (n - l - m) / 4, k -> Integers.SINGLETON.sum(k, (n - k - l - m) / 3, j -> Integers.SINGLETON.sum(j, (n - j - k - l - m) / 2, i -> Puma.primePiZ(i).subtract(Puma.primePiZ(i - 1)).multiply(Puma.primePiZ(j).subtract(Puma.primePiZ(j - 1))).multiply(Puma.primePiZ(k).subtract(Puma.primePiZ(k - 1))).multiply(Puma.primePiZ(l).subtract(Puma.primePiZ(l - 1))).multiply(Puma.primePiZ(m).subtract(Puma.primePiZ(m - 1))).multiply(Puma.primePiZ(n - i - j - k - l - m).subtract(Puma.primePiZ(n - i - j - k - l - m - 1))))))))));
  }
}
