package irvine.oeis.a309;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A309084 a(n) = exp(3) * Sum_{k&gt;=0} (-3)^k*k^n/k!.
 * E.g.f.: exp(3*(1 - exp(x)))
 * @author Georg Fischer
 */
public class A309084 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.monomial(new Q(3), 0), RING.subtract(RING.one(), RING.exp(RING.x(), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
