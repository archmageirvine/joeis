package irvine.oeis.a309;
// Generated by gen_seq4.pl 2026-09-04.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A309522 Generalized Blasius numbers, square array read by ascending antidiagonals, A(n, k) for n, k &gt;= 0.
 * @author Georg Fischer
 */
public class A309522 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A309522() {
    super(0, 0, 0, 1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        k == 0 ? Z.ONE : Integers.SINGLETON.sum(0, k - 1, v -> Binomial.binomial(n * k - 1, n * v).multiply(get(n, v)).multiply(get(n, k - 1 - v)));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
