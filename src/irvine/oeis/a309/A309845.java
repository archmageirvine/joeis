package irvine.oeis.a309;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A309845 Expansion of e.g.f.: sec(x) + 2*tan(x).
 * E.g.f.: sec(x) + 2*tan(x)
 * @author Georg Fischer
 */
public class A309845 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.sec(RING.x(), mN), RING.multiply(RING.monomial(Q.TWO, 0), RING.tan(RING.x(), mN), mN)).coeff(mN).multiply(mF).toZ();
  }
}
