package irvine.oeis.a309;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A309878 The real part of b(n) where b(n) = (n + b(n-1)) * (1 + i) with b(-1)=0; i = sqrt(-1).
 * E.g.f.: exp(x)*(-x + 2*sin(x))
 * @author Georg Fischer
 */
public class A309878 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.exp(RING.x(), mN), RING.add(RING.negate(RING.x()), RING.multiply(RING.monomial(Q.TWO, 0), RING.sin(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
