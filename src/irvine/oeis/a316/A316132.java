package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A316132 Decimal expansion of the middle x such that 1/x + 1/(x+1) + 1/(x+3) = 1, negated.
 * Equation; 1/x+1/(x+1)+1/(x+3)-(1)
 * @author Georg Fischer
 */
public class A316132 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316132() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.ONE))).add(CR.ONE.divide(x.add(CR.THREE))).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-0.583), CR.valueOf(-0.56)).execute(CR.ZERO).negate());
  }
}
