package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A316133 Decimal expansion of the greatest x such that 1/x + 1/(x+1) + 1/(x+3) = 1.
 * Equation; 1/x+1/(x+1)+1/(x+3)-(1)
 * @author Georg Fischer
 */
public class A316133 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316133() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.ONE))).add(CR.ONE.divide(x.add(CR.THREE))).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(2.044), CR.valueOf(2.128)).execute(CR.ZERO));
  }
}
