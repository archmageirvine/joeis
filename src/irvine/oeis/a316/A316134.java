package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolvn at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A316134 Decimal expansion of the least x such that 1/x + 1/(x+2) + 1/(x+3) = 1 (negated).
 * Equation; 1/x+1/(x+2)+1/(x+3)-(1)
 * @author Georg Fischer
 */
public class A316134 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316134() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.TWO))).add(CR.ONE.divide(x.add(CR.THREE))).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(-2.708), CR.valueOf(-2.602)).execute(CR.ZERO).negate());
  }
}
