package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolv at 2021-08-10 20:45

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A316163 Decimal expansion of the greatest x such that 1/x + 1/(x+1) + 1/(x+2) = 2.
 * Equation; 2*x^3+3*x^2-2*x-2
 * @author Georg Fischer
 */
public class A316163 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316163() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(3)).add(CR.THREE.multiply(x.pow(CR.TWO))).subtract(CR.TWO.multiply(x)).subtract(CR.TWO);
      }
    }.inverseMonotone(CR.valueOf(0.871), CR.valueOf(0.907)).execute(CR.ZERO));
  }
}
