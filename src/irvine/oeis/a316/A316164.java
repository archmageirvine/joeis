package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolvn at 2021-08-10 20:45

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A316164 Decimal expansion of the least x such that 1/x + 1/(x+1) + 1/(x+3) = 2.
 * Equation; 2*x^3+5*x^2-2*x-3
 * @author Georg Fischer
 */
public class A316164 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316164() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.TWO.multiply(x.pow(3)).add(CR.FIVE.multiply(x.pow(CR.TWO))).subtract(CR.TWO.multiply(x)).subtract(CR.THREE);
      }
    }.inverseMonotone(CR.valueOf(-2.717), CR.valueOf(-2.611)).execute(CR.ZERO).negate());
  }
}
