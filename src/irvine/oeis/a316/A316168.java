package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolvn at 2021-08-09 22:50

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A316168 Decimal expansion of the middle x such that 1/x + 1/(x+2) + 1/(x+4) = 2, negated.
 * Equation; 1/x+1/(x+2)+1/(x+4)-(2)
 * @author Georg Fischer
 */
public class A316168 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316168() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.TWO))).add(CR.ONE.divide(x.add(CR.FOUR))).subtract(CR.TWO);
      }
    }.inverseMonotone(CR.valueOf(-1.583), CR.valueOf(-1.521)).execute(CR.ZERO).negate());
  }
}
