package irvine.oeis.a316;
// Generated by gen_seq4.pl decsolvn at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A316259 Decimal expansion of the middle x such that 1/x + 1/(x+3) + 1/(x+4) = 3 (negated).
 * Equation; 1/x+1/(x+3)+1/(x+4)-(3)
 * @author Georg Fischer
 */
public class A316259 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A316259() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x).add(CR.ONE.divide(x.add(CR.THREE))).add(CR.ONE.divide(x.add(CR.FOUR))).subtract(CR.THREE);
      }
    }.inverseMonotone(CR.valueOf(-2.675), CR.valueOf(-2.571)).execute(CR.ZERO).negate());
  }
}
