package irvine.oeis.a318;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A318002 E.g.f.: 2*cosh(x) / (1 + sqrt(1 - 2*sinh(2*x))).
 * E.g.f.: 2*cosh(x) / (1 + sqrt(1 - 2*sinh(2*x)))
 * @author Georg Fischer
 */
public class A318002 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.monomial(Q.TWO, 0), RING.cosh(RING.x(), mN), mN), RING.add(RING.one(), RING.sqrt(RING.subtract(RING.one(), RING.multiply(RING.monomial(Q.TWO, 0), RING.sinh(RING.monomial(Q.TWO, 1), mN), mN)), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
