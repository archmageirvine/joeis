package irvine.oeis.a318;
// Generated by gen_seq4.pl sumdiv at 2023-04-05 22:33

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A318638 G.f.: Sum_{n&gt;=1} ( (3 + x^n)^n - 3^n ).
 * @author Georg Fischer
 */
public class A318638 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A318638() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> ((d - mN / d) >= 0) ? Binomial.binomial(d, mN / d).multiply(Z.THREE.pow(d - mN / d)) : Binomial.binomial(d, mN / d).divide(Z.THREE.pow(mN / d - d)));
  }
}
