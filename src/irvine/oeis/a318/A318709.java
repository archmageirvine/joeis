package irvine.oeis.a318;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A318709 Decimal expansion of the solution to x = sqrt(5 + sqrt(5 - sqrt(5 - sqrt(5 + x)))).
 * Equation; x-(sqrt(5+sqrt(5-sqrt(5-sqrt(5+x)))))
 * @author Georg Fischer
 */
public class A318709 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A318709() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return x.subtract(CR.FIVE.add(CR.FIVE.subtract(CR.FIVE.subtract(CR.FIVE.add(x).sqrt()).sqrt()).sqrt()).sqrt());
      }
    }.inverseMonotone(CR.valueOf(2.569), CR.valueOf(2.673)).execute(CR.ZERO));
  }
}
