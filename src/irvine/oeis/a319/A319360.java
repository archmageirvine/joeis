package irvine.oeis.a319;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A319360 Expansion of e.g.f. (1 + x)*exp(log(1 + x)^2/2).
 * E.g.f.: (1 + x)*exp(log(1 + x)^2/2)
 * @author Georg Fischer
 */
public class A319360 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.onePlusXToTheN(1), RING.exp(RING.series(RING.pow(RING.log(RING.onePlusXToTheN(1), mN), 2, mN), RING.monomial(Q.TWO, 0), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
