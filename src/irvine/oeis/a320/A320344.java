package irvine.oeis.a320;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A320344 Expansion of e.g.f. log(1 + x)/(1 - log(1 + x))^2.
 * E.g.f.: log(1 + x)/(1 - log(1 + x))^2
 * @author Georg Fischer
 */
public class A320344 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.log(RING.onePlusXToTheN(1), mN), RING.pow(RING.subtract(RING.one(), RING.log(RING.onePlusXToTheN(1), mN)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
