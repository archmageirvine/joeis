package irvine.oeis.a320;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A320352 Expansion of e.g.f. (exp(x) - 1)/(exp(x) - exp(2*x) + 1).
 * E.g.f.: (1 + sinh(x) - cosh(x))/(1 - 2*sinh(x))
 * @author Georg Fischer
 */
public class A320352 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.add(RING.one(), RING.sinh(RING.x(), mN)), RING.cosh(RING.x(), mN)), RING.subtract(RING.one(), RING.multiply(RING.monomial(Q.TWO, 0), RING.sinh(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
