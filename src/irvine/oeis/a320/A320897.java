package irvine.oeis.a320;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-25 11:10

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A320897 a(n) = Sum_{k=1..n} k^2 * tau(k)^2, where tau is A000005.
 * a(n) = sum(k=1,n,k^2*numdiv(k)^2);
 * @author Georg Fischer
 */
public class A320897 extends LambdaSequence {

  /** Construct the sequence. */
  public A320897() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Z.valueOf(k).multiply(k).multiply(Jaguar.factor(k).sigma0().square())));
  }
}
