package irvine.oeis.a321;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A321371 n + [n*r/s] + [n*t/s], where r = 1, s = cos(2*Pi/5), t = sec(2*Pi/5).
 * @author Georg Fischer
 */
public class A321371 extends FloorSequence {

  private final CR mR = CR.ONE;
  private final CR mS = CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos();
  private final CR mT = CR.ONE.divide(CR.TWO.divide(CR.FIVE).multiply(CR.PI).cos());

  /** Construct the sequence */
  public A321371() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mR).divide(mS).floor()).add(CR.valueOf(mN).multiply(mT).divide(mS).floor());
  }

}
