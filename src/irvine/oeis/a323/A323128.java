package irvine.oeis.a323;
// Generated by gen_seq4.pl 2026-09-06.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.function.Functions;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A323128 Number T(n,k) of colored set partitions of [n] where elements of subsets have distinct colors and exactly k colors are used; triangle T(n,k), n&gt;=0, 0&lt;=k&lt;=n, read by rows.
 * @author Georg Fischer
 */
public class A323128 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A323128() {
    super(0, 0, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(1, Functions.MIN.i(n, k), j -> Functions.FACTORIAL.z(k).divide(Functions.FACTORIAL.z(k - j)).multiply(Binomial.binomial(n - 1, j - 1)).multiply(get(n - j, k)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return Integers.SINGLETON.sum(0, k, i -> mB.get(n, k - i).multiply(Z.NEG_ONE.pow(i)).multiply(Binomial.binomial(k, i)));
  }
}
