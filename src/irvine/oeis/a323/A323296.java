package irvine.oeis.a323;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A323296 Number of 3-uniform hypergraphs spanning n labeled vertices where no two edges have exactly one vertex in common.
 * E.g.f.: exp(-x^2/2 - x^3/3 + 5*x^4/24 + x^2*exp(x)/2)
 * @author Georg Fischer
 */
public class A323296 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.add(RING.subtract(RING.subtract(RING.zero(), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN)), RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(Q.THREE, 0), mN)), RING.series(RING.monomial(Q.FIVE, 4), RING.monomial(new Q(24), 0), mN)), RING.series(RING.multiply(RING.pow(RING.x(), 2, mN), RING.exp(RING.x(), mN), mN), RING.monomial(Q.TWO, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
