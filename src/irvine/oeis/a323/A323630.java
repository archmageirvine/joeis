package irvine.oeis.a323;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A323630 Expansion of e.g.f. exp(log(1 - x)^2/2)/(1 - x).
 * E.g.f.: exp(log(1 - x)^2/2)/(1 - x)
 * @author Georg Fischer
 */
public class A323630 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.series(RING.pow(RING.log(RING.oneMinusXToTheN(1), mN), 2, mN), RING.monomial(new Q(2), 0), mN), mN), RING.oneMinusXToTheN(1), mN).coeff(mN).multiply(mF).toZ();
  }
}
