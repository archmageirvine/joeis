package irvine.oeis.a323;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A323773 Decimal expansion of the root of the equation (1-2*r)^(4*r-1) * (1-r)^(1-2*r) = r^(2*r).
 * Equation; (1-2*x)^(4*x-1)*(1-x)^(1-2*x)-(x^(2*x))
 * @author Georg Fischer
 */
public class A323773 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A323773() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(CR.TWO.multiply(x)).pow(CR.FOUR.multiply(x).subtract(CR.ONE)).multiply(CR.ONE.subtract(x).pow(CR.ONE.subtract(CR.TWO.multiply(x)))).subtract(x.pow(CR.TWO.multiply(x)));
      }
    }.inverseMonotone(CR.valueOf(0.359), CR.valueOf(0.374)).execute(CR.ZERO));
  }
}
