package irvine.oeis.a323;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A323777 Decimal expansion of the root of the equation (1-2*r)^(3-4*r) = (1-r)^(2-2*r) * r^(1-2*r).
 * Equation; (1-2*x)^(3-4*x)-((1-x)^(2-2*x)*x^(1-2*x))
 * @author Georg Fischer
 */
public class A323777 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A323777() {
    super(0, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(CR.TWO.multiply(x)).pow(CR.THREE.subtract(CR.FOUR.multiply(x))).subtract(CR.ONE.subtract(x).pow(CR.TWO.subtract(CR.TWO.multiply(x))).multiply(x.pow(CR.ONE.subtract(CR.TWO.multiply(x)))));
      }
    }.inverseMonotone(CR.valueOf(0.216), CR.valueOf(0.225)).execute(CR.ZERO));
  }
}
