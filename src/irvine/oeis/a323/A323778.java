package irvine.oeis.a323;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A323778 Decimal expansion of the root of the equation (1-r)^(2-2*r) * r^(2*r) = 1-2*r.
 * Equation; (1-x)^(2-2*x)*x^(2*x)-(1-2*x)
 * @author Georg Fischer
 */
public class A323778 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A323778() {
    super(0, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.subtract(x).pow(CR.TWO.subtract(CR.TWO.multiply(x))).multiply(x.pow(CR.TWO.multiply(x))).subtract(CR.ONE.subtract(CR.TWO.multiply(x)));
      }
    }.inverseMonotone(CR.valueOf(0.358), CR.valueOf(0.373)).execute(CR.ZERO));
  }
}
