package irvine.oeis.a324;
// Generated by gen_seq4.pl simbinom/simjagfa at 2023-03-17 21:33

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A324122 a(n) = sigma(n) - gcd(n*d(n), sigma(n)), where d(n) = number of divisors of n (A000005) and sigma(n) = sum of divisors of n (A000203).
 * @author Georg Fischer
 */
public class A324122 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A324122() {
    super(1);
  }

  @Override
  public Z next() {
    final FactorSequence fs = Jaguar.factor(++mN);
    return fs.sigma().subtract(fs.tau().multiply(mN).gcd(fs.sigma()));
  }
}
