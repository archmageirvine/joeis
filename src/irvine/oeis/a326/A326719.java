package irvine.oeis.a326;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A326719 a(n) = n! * [x^n] (x * tanh(x) * sech(x)) / 2.
 * E.g.f.: x*(exp(x) - exp(-x))/(exp(x) + exp(-x))^2
 * @author Georg Fischer
 */
public class A326719 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.x(), RING.subtract(RING.exp(RING.x(), mN), RING.exp(RING.negate(RING.x()), mN)), mN), RING.pow(RING.add(RING.exp(RING.x(), mN), RING.exp(RING.negate(RING.x()), mN)), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
