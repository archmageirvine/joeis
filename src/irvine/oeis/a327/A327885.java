package irvine.oeis.a327;
// Generated by gen_seq4.pl egfsi at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A327885 Number of set partitions of [n] such that at least one of the block sizes is 2.
 * E.g.f.: exp(exp(x)-1) - exp(exp(x)-1-x^2/2)
 * @author Georg Fischer
 */
public class A327885 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.exp(RING.subtract(RING.subtract(RING.exp(RING.x(), mN), RING.one()), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(Q.TWO, 0), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
