package irvine.oeis.a329;
// Generated by gen_seq4.pl decsolv at 2021-07-31 13:37

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCrFunction;
import irvine.oeis.cons.DecimalExpansionSequence;

/**
 * A329998 Decimal expansion of the solution of 1/sqrt(x-1) + 1/sqrt(x+1) = 1.
 * Equation; 1/sqrt(x-1)+1/sqrt(x+1)-(1)
 * @author Georg Fischer
 */
public class A329998 extends DecimalExpansionSequence {

  /** Construct the sequence. */
  public A329998() {
    super(1, new UnaryCrFunction() {
      @Override 
      public CR execute(final CR x) {
        return CR.ONE.divide(x.subtract(CR.ONE).sqrt()).add(CR.ONE.divide(x.add(CR.ONE).sqrt())).subtract(CR.ONE);
      }
    }.inverseMonotone(CR.valueOf(4.097), CR.valueOf(4.265)).execute(CR.ZERO));
  }
}
