package irvine.oeis.a330;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.factor.factor.Jaguar;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A330088 a(n) = Sum_{k=1..n} binomial(n,k) * sigma(k) * sigma(n - k + 1), where sigma = A000203.
 * a(n)=sum(k=1,n,binom(n,k)*sigma(k,1)*sigma(n-k+1,1))
 * @author Georg Fischer
 */
public class A330088 extends LambdaSequence {

  /** Construct the sequence. */
  public A330088() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Binomial.binomial(n, k).multiply(Jaguar.factor(k).sigma(1)).multiply(Jaguar.factor(n - k + 1).sigma(1))));
  }
}
