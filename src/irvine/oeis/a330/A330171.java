package irvine.oeis.a330;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A330171 a(n) = n + floor(ns/r) + floor(nt/r), where r = sqrt(2) - 1, s = sqrt(2), t = sqrt(2) + 1.
 * @author Georg Fischer
 */
public class A330171 extends FloorSequence {

  private final CR mR = CR.SQRT2.subtract(CR.ONE);
  private final CR mS = CR.SQRT2;
  private final CR mT = CR.SQRT2.add(CR.ONE);

  /** Construct the sequence */
  public A330171() {
    super(1);
  }

  protected Z evalCR(final long mN) {
    return Z.valueOf(mN).add(CR.valueOf(mN).multiply(mS).divide(mR).floor()).add(CR.valueOf(mN).multiply(mT).divide(mR).floor());
  }

}
