package irvine.oeis.a330;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A330173 a(n) = n + floor(ns/r) + floor(nt/r), where r = sqrt(2), s = sqrt(2) + 1, t = sqrt(2) + 2.
 * @author Georg Fischer
 */
public class A330173 extends FloorSequence {

  private final CR mR = CR.SQRT2;
  private final CR mS = CR.SQRT2.add(CR.ONE);
  private final CR mT = CR.SQRT2.add(CR.TWO);

  /** Construct the sequence */
  public A330173() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mS).divide(mR).floor()).add(CR.valueOf(n).multiply(mT).divide(mR).floor());
  }

}
