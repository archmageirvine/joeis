package irvine.oeis.a330;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A330176 a(n) = n + floor(nr/t) + floor(ns/t), where r = sqrt(5) - 2, s = sqrt(5) - 1, t = sqrt(5).
 * @author Georg Fischer
 */
public class A330176 extends FloorSequence {

  private final CR mR = CR.FIVE.sqrt().subtract(CR.TWO);
  private final CR mS = CR.FIVE.sqrt().subtract(CR.ONE);
  private final CR mT = CR.FIVE.sqrt();

  /** Construct the sequence. */
  public A330176() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mR).divide(mT).floor()).add(CR.valueOf(n).multiply(mS).divide(mT).floor());
  }

}
