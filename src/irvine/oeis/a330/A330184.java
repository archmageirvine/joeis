package irvine.oeis.a330;
// Generated by gen_seq4.pl floor at 2021-08-24 07:13

import irvine.math.cr.CR;
import irvine.math.z.Z;
import irvine.oeis.FloorSequence;
/**
 * A330184 a(n) = n + floor(nr/t) + floor(ns/t), where r = sqrt(2) - 1/2, s = sqrt(2), t = sqrt(2) + 1/2.
 * @author Georg Fischer
 */
public class A330184 extends FloorSequence {

  private final CR mR = CR.SQRT2.subtract(CR.HALF);
  private final CR mS = CR.SQRT2;
  private final CR mT = CR.SQRT2.add(CR.HALF);

  /** Construct the sequence */
  public A330184() {
    super(1);
  }

  protected Z evalCR(final long n) {
    return Z.valueOf(n).add(CR.valueOf(n).multiply(mR).divide(mT).floor()).add(CR.valueOf(n).multiply(mS).divide(mT).floor());
  }

}
