package irvine.oeis.a330;
// Generated by gen_seq4.pl decexpr at 2021-07-16 22:36

import irvine.math.cr.CR;
import irvine.math.cr.ComputableReals;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A330934 Decimal expansion of the area of a sofa that can be moved around a 90-degree turn both to the right and to the left in a hallway of unit width.
 * Formula: <code>(3+2sqrt(2))^(1/3)+(3-2*sqrt(2))^(1/3)-1+atan(((sqrt(2)+1)^(1/3)-(sqrt(2)-1)^(1/3))/2)</code>
 * @author Georg Fischer
 */
public class A330934 extends DecimalExpansionSequence {

  private static final ComputableReals REALS = ComputableReals.SINGLETON;

  /** Construct the sequence. */
  public A330934() {
    super(1, CR.THREE.add(CR.TWO.multiply(CR.TWO.sqrt())).pow(CR.ONE.divide(CR.THREE)).add(CR.THREE.subtract(CR.TWO.multiply(CR.TWO.sqrt())).pow(CR.ONE.divide(CR.THREE))).subtract(CR.ONE).add(REALS.atan(CR.TWO.sqrt().add(CR.ONE).pow(CR.ONE.divide(CR.THREE)).subtract(CR.TWO.sqrt().subtract(CR.ONE).pow(CR.ONE.divide(CR.THREE))).divide(CR.TWO))));
  }
}
