package irvine.oeis.a331;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A331339 E.g.f.: 1 / (1 + log(1 - x - x^2)).
 * E.g.f.: 1 / (1 + log(1 - x - x^2))
 * @author Georg Fischer
 */
public class A331339 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.add(RING.one(), RING.log(RING.subtract(RING.oneMinusXToTheN(1), RING.pow(RING.x(), 2, mN)), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
