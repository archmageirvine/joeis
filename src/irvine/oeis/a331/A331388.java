package irvine.oeis.a331;
// Generated by gen_seq4.pl sumsi/sumgcd at 2021-11-20 22:12

import irvine.math.LongUtils;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A331388 a(n) = Sum_{k=1..n} mu(gcd(n, k)) * k / gcd(n, k).
 * @author Georg Fischer
 */
public class A331388 implements Sequence {

  private int mN = 0;

  @Override
  public Z next() {
    ++mN;
    Z sum = Z.ZERO;
    for (int k = 1; k <= mN; ++k) {
      final long gd = LongUtils.gcd(mN, k);
      sum = sum.add(Z.valueOf(irvine.math.Mobius.mobius(gd)).multiply(k / gd));
    }
    return sum;
  }
}
