package irvine.oeis.a331;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A331607 E.g.f.: exp(1 / (1 - sin(x)) - 1).
 * E.g.f.: exp(1 / (1 - sin(x)) - 1)
 * @author Georg Fischer
 */
public class A331607 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.series(RING.one(), RING.subtract(RING.one(), RING.sin(RING.x(), mN)), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
