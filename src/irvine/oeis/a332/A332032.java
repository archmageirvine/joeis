package irvine.oeis.a332;
// Generated by gen_seq4.pl sumdiv at 2023-04-05 22:33

import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A332032 G.f.: Sum_{k&gt;=1} k! * x^(k^2) / (1 - x^(2*k)).
 * @author Georg Fischer
 */
public class A332032 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A332032() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> ((((mN + d) & 1) == 0) && (((mN + (mN / d)) & 1) == 0) && d <= mN / d) ? Functions.FACTORIAL.z(d) : Z.ZERO);
  }
}
