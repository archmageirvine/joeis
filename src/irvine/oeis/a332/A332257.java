package irvine.oeis.a332;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A332257 E.g.f.: (1 - sinh(x)) / (1 - 2*sinh(x)).
 * E.g.f.: (1 - sinh(x)) / (1 - 2*sinh(x))
 * @author Georg Fischer
 */
public class A332257 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.one(), RING.sinh(RING.x(), mN)), RING.subtract(RING.one(), RING.multiply(RING.monomial(Q.TWO, 0), RING.sinh(RING.x(), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
