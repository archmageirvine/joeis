package irvine.oeis.a333;
// Generated by gen_seq4.pl sumdiv3/sumdiv at 2023-04-11 14:46

import irvine.factor.factor.Jaguar;
import irvine.math.LongUtils;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A333557 a(n) = Sum_{d|n, gcd(d, n/d) = 1} uphi(d) * uphi(n/d), where uphi = unitary totient function (A047994).
 * @author Georg Fischer
 */
public class A333557 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A333557() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> (LongUtils.gcd(d, mN / d) == 1) ? (Z.valueOf(-2).pow(Jaguar.factor(mN / d).omega()).multiply(Z.ONE.shiftLeft(Jaguar.factor(d).omega()).multiply(d))) : Z.ZERO);
  }
}
