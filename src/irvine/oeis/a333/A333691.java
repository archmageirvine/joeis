package irvine.oeis.a333;
// Generated by gen_seq4.pl egfsi/egfsie at 2021-12-01 20:53

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A333691 Expansion of e.g.f. atan(1 - sqrt(1 - x^2)) (even powers only).
 * E.g.f.: atan(1 - sqrt(1 - x^2)), even powers
 * @author Georg Fischer
 */
public class A333691 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    while ((++mN & 1) == 1) {
      if (mN != 0) {
        mF = mF.multiply(mN);
      }
    }
    if (mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.atan(RING.subtract(RING.one(), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(2), mN), RING.monomial(Q.HALF, 0), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
