package irvine.oeis.a334;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A334191 a(n) = exp(1/3) * Sum_{k&gt;=0} (3*k + 1)^n / ((-3)^k * k!).
 * E.g.f.: exp(x + (1 - exp(3*x)) / 3)
 * @author Georg Fischer
 */
public class A334191 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.x(), RING.series(RING.subtract(RING.one(), RING.exp(RING.monomial(Q.THREE, 1), mN)), RING.monomial(Q.THREE, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
