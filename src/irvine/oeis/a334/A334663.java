package irvine.oeis.a334;
// Generated by gen_seq4.pl sumdiv3 at 2023-04-11 14:46

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A334663 a(n) = Sum_{d|n} gcd(sigma(d), pod(d)), where pod(k) is the product of the divisors of k (A007955).
 * @author Georg Fischer
 */
public class A334663 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A334663() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    Z sum = Z.ZERO;
    for (final Z dd : Jaguar.factor(mN).divisors()) {
      final int d = dd.intValue();
      final FactorSequence fsd = Jaguar.factor(d);
      sum = sum.add(fsd.sigma().gcd(fsd.pod()));
    }
    return sum;
  }
}
