package irvine.oeis.a334;
// Generated by gen_seq4.pl sumdiv3 at 2023-04-11 14:46

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A334784 a(n) = Sum_{d|n} lcm(tau(d), sigma(d)).
 * @author Georg Fischer
 */
public class A334784 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A334784() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    Z sum = Z.ZERO;
    for (final Z dd : Jaguar.factor(mN).divisors()) {
      final int d = dd.intValue();
      final FactorSequence fsd = Jaguar.factor(d);
      sum = sum.add(fsd.sigma0().lcm(fsd.sigma()));
    }
    return sum;
  }
}
