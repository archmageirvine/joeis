package irvine.oeis.a335;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A335595 E.g.f.: exp(-x * (2 + x)) / (1 - x)^2.
 * E.g.f.: exp(-x * (2 + x)) / (1 - x)^2
 * @author Georg Fischer
 */
public class A335595 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.subtract(RING.zero(), RING.multiply(RING.x(), RING.add(RING.monomial(Q.TWO, 0), RING.x()), mN)), mN), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
