package irvine.oeis.a336;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A336012 a(n) is the number of chains from {} to a top element in the poset of even sized subsets of {1,2,...,n} ordered by inclusion.
 * E.g.f.: 1/(2-cosh(x))*(1+x)
 * @author Georg Fischer
 */
public class A336012 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.series(RING.one(), RING.subtract(RING.monomial(Q.TWO, 0), RING.cosh(RING.x(), mN)), mN), RING.onePlusXToTheN(1), mN).coeff(mN).multiply(mF).toZ();
  }
}
