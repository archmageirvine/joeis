package irvine.oeis.a336;
// Generated by gen_seq4.pl decsolv at 2021-08-03 20:49

import irvine.math.cr.CR;
import irvine.math.cr.UnaryCRFunction;
import irvine.oeis.DecimalExpansionSequence;

/**
 * A336050 Decimal expansion of the solution c of s/c = Pi, where s = arclength and c = chord length on the unit circle.
 * Equation; x-Pi*sqrt(2-2*cos(x))
 * @author Georg Fischer
 */
public class A336050 extends DecimalExpansionSequence {

  /** Construct the sequence */
  public A336050() {
    super(1, new UnaryCRFunction() { 
      @Override 
      public CR execute(final CR x) {
        return x.subtract(CR.PI.multiply(CR.TWO.subtract(CR.TWO.multiply(x.cos())).sqrt()));
      }
    }.inverseMonotone(CR.valueOf(4.534), CR.valueOf(4.720)).execute(CR.ZERO).divide(CR.PI));
  }
}
