package irvine.oeis.a336;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A336183 a(n) = n^2 + (1/n) * Sum_{k=1..n-1} binomial(n,k) * k * a(k) * (n-k)^2.
 * E.g.f.: -log(1 - exp(x) * x * (1 + x))
 * @author Georg Fischer
 */
public class A336183 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.negate(RING.log(RING.subtract(RING.one(), RING.multiply(RING.multiply(RING.exp(RING.x(), mN), RING.x(), mN), RING.onePlusXToTheN(1), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
