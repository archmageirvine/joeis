package irvine.oeis.a337;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A337011 a(n) = 2^n * exp(-1/2) * Sum_{k&gt;=0} (k + 2)^n / (2^k * k!).
 * E.g.f.: exp(4*x + (exp(2*x) - 1) / 2)
 * @author Georg Fischer
 */
public class A337011 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.add(RING.monomial(Q.FOUR, 1), RING.series(RING.subtract(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.one()), RING.monomial(Q.TWO, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
