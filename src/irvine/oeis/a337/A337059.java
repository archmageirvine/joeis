package irvine.oeis.a337;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A337059 E.g.f.: 1 / (2 + x^3/6 - exp(x)).
 * E.g.f.: 1 / (2 + x^3/6 - exp(x))
 * @author Georg Fischer
 */
public class A337059 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.add(RING.monomial(Q.TWO, 0), RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(Q.SIX, 0), mN)), RING.exp(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
