package irvine.oeis.a337;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence0;

/**
 * A337552 a(0) = 1; a(n) = Sum_{k=1..n} binomial(n,k) * (3*k-2) * a(n-k).
 * E.g.f.: 1 / (exp(x) * (2 - 3*x) - 1)
 * @author Georg Fischer
 */
public class A337552 extends Sequence0 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.monomial(Q.TWO, 0), RING.monomial(Q.THREE, 1)), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
