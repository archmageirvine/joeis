package irvine.oeis.a337;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A337555 a(0) = 1; a(n) = (1/2) * Sum_{k=1..n} binomial(n,k) * (3^k-1) * a(n-k).
 * E.g.f.: 2 / (2 + exp(x) - exp(3*x))
 * @author Georg Fischer
 */
public class A337555 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.monomial(new Q(2), 0), RING.subtract(RING.add(RING.monomial(new Q(2), 0), RING.exp(RING.x(), mN)), RING.exp(RING.monomial(new Q(3), 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
