package irvine.oeis.a337;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A337932 Number of ways to write n as the sum of two deficient numbers (A005100).
 * a(n)=sum(i=1, floor(n/2), (1-sign(floor(sigma(n-i, 1)/(2*(n-i)))))*(1-sign(floor(sigma(i, 1)/(2*i)))))
 * @author Georg Fischer
 */
public class A337932 extends LambdaSequence {

  /** Construct the sequence. */
  public A337932() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 2, i -> Z.ONE.subtract(Jaguar.factor(n - i).sigma(1).divide(Z.valueOf(2 * n - 2 * i)).signum()).multiply(Z.ONE.subtract(Jaguar.factor(i).sigma(1).divide(Z.valueOf(2 * i)).signum()))));
  }
}
