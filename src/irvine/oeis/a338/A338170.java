package irvine.oeis.a338;
// Generated by gen_seq4.pl sumdiv3 at 2023-04-11 14:46

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A338170 a(n) is the number of divisors d of n such that tau(d) divides sigma(d).
 * @author Georg Fischer
 */
public class A338170 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A338170() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    Z sum = Z.ZERO;
    for (final Z dd : Jaguar.factor(mN).divisors()) {
      final int d = dd.intValue();
      final FactorSequence fsd = Jaguar.factor(d);
      sum = sum.add(fsd.sigma().mod(fsd.sigma0()).isZero() ? Z.ONE : Z.ZERO);
    }
    return sum;
  }
}
