package irvine.oeis.a338;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A338735 a(n) = Bell(n) + n - 2 (cf. A000110).
 * E.g.f.: exp(exp(x) - 1) + exp(x) * (x - 2) + 1
 * @author Georg Fischer
 */
public class A338735 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.add(RING.add(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.x(), RING.monomial(Q.TWO, 0)), mN)), RING.one()).coeff(mN).multiply(mF).toZ();
  }
}
