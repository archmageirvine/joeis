package irvine.oeis.a340;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A340283 Number of partitions of n into 3 parts such that the smallest part is relatively prime to each of the other two parts.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),floor(1/gcd(k,i))*floor(1/gcd(k,n-i-k))))
 * @author Georg Fischer
 */
public class A340283 extends LambdaSequence {

  /** Construct the sequence. */
  public A340283() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.ONE.divide(Z.valueOf(k).gcd(i)).multiply(Z.ONE.divide(Z.valueOf(k).gcd(n - i - k))))));
  }
}
