package irvine.oeis.a340;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A340286 Number of partitions of n into 3 parts such that at least two parts share a common factor &gt; 1.
 * a(n)=sum(k=1,floor(n/3),sum(i=k,floor((n-k)/2),1-floor(1/gcd(k,i))*floor(1/gcd(k,n-i-k))*floor(1/gcd(i,n-i-k))))
 * @author Georg Fischer
 */
public class A340286 extends LambdaSequence {

  /** Construct the sequence. */
  public A340286() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 3, k -> Integers.SINGLETON.sum(k, (n - k) / 2, i -> Z.ONE.subtract(Z.ONE.divide(Z.valueOf(k).gcd(i)).multiply(Z.ONE.divide(Z.valueOf(k).gcd(n - i - k))).multiply(Z.ONE.divide(Z.valueOf(i).gcd(n - i - k)))))));
  }
}
