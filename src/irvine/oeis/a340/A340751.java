package irvine.oeis.a340;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A340751 Number of partitions of n into 4 parts such that both the smallest two parts and the largest two parts are relatively prime.
 * a(n)=sum(k=1,floor(n/4),sum(j=k,floor((n-k)/3),sum(i=j,floor((n-j-k)/2),floor(1/gcd(k,j))*floor(1/gcd(i,n-i-j-k)))))
 * @author Georg Fischer
 */
public class A340751 extends LambdaSequence {

  /** Construct the sequence. */
  public A340751() {
    super(0, n -> Integers.SINGLETON.sum(1, n / 4, k -> Integers.SINGLETON.sum(k, ((n - k) / 3), j -> Integers.SINGLETON.sum(j, ((n - j - k) / 2), i -> Z.ONE.divide(Z.valueOf(k).gcd(j)).multiply(Z.ONE.divide(Z.valueOf(i).gcd(n - i - j - k)))))));
  }
}
