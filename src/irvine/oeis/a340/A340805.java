package irvine.oeis.a340;
// Generated by gen_seq4.pl sumdiv2/sumdiv at 2023-04-10 10:08

import irvine.math.IntegerUtils;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A340805 a(n) is the number of solutions of the equation x*(y - 1) + (2*x - y - 1)*(x mod 2) = 2*n for 0 &lt; x &lt;= y.
 * @author Georg Fischer
 */
public class A340805 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A340805() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> Z.valueOf(((d <= (1 + IntegerUtils.sqrt(1 + 8 * mN)) / 4) ? 1 : 0) + ((d <= (IntegerUtils.sqrt(1 + 2 * mN) - 1) / 2) ? 1 : 0)));
  }
}
