package irvine.oeis.a340;
// Generated by gen_seq4.pl 2025-08-28.ack/lambdan at 2025-08-28 21:46

import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A340897 Number of sets in the geometry determined by the Hausdorff metric at each location between two sets defined by a complete bipartite graph K(6,n) (with n at least 3) missing two edges, where the two removed edges are not incident to the same vertex in the 6-point set and are also not incident to the same vertex in the other set.
 * @author Georg Fischer
 */
public class A340897 extends LambdaSequence {

  /** Construct the sequence. */
  public A340897() {
    super(3, n -> Z.valueOf(961).multiply(Z.valueOf(63).pow(n - 2)).subtract(Z.valueOf(1830).multiply(Z.valueOf(31).pow(n - 2))).add(Z.valueOf(1359).multiply(Z.valueOf(15).pow(n - 2))).subtract(Z.valueOf(484).multiply(Z.SEVEN.pow(n - 2))).add(Z.valueOf(79).multiply(Z.THREE.pow(n - 2))).subtract(4));
  }
}
