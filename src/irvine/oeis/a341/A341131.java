package irvine.oeis.a341;
// Generated by gen_seq4.pl 2026-08-17.ack/mlamfun

import irvine.math.MemoryFunctionInt3;
import irvine.math.function.Functions;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;
import irvine.oeis.DirectSequence;

/**
 * A341131 Number of partitions of n into 10 prime powers (including 1).
 * @author Georg Fischer
 */
public class A341131 extends AbstractSequence implements DirectSequence {

  private long mN;
  private int mColNo;

  /** Construct the sequence. */
  public A341131() {
    this(10, 10);
  }

  /**
   * Generic constructor with parameters.
   * @param offset first index
   * @param colNo column number
   */
  public A341131(final int offset, final int colNo) {
    super(offset);
    mN = offset - 1;
    mColNo = colNo;
  }

  protected final MemoryFunctionInt3<Z> mB = new MemoryFunctionInt3<>() {
    @Override
    protected Z compute(final int n, final int i, final int t) {
      return
        n == 0
          ? Z.valueOf(t == 0 ? 1 : 0)
          : (i < 1 || t < 1 ? Z.ZERO : get(n, i - 1, t).add(Functions.OMEGA.i(i) < 2 ? get(n - i, Functions.MIN.i(n - i, i), t - 1) : Z.ZERO));
    }
  };

  @Override
  public Z next() {
    return a(++mN);
  }

  @Override
  public Z a(final Z n) {
    return a(n.longValueExact());
  }

  @Override
  public Z a(final long n) {
    return mB.get(n, n, mColNo);
  }
}
