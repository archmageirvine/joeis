package irvine.oeis.a341;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A341586 E.g.f.: (exp(1 - exp(x)) - 1)^2 / 2.
 * E.g.f.: (exp(1 - exp(x)) - 1)^2 / 2
 * @author Georg Fischer
 */
public class A341586 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.pow(RING.subtract(RING.exp(RING.subtract(RING.one(), RING.exp(RING.x(), mN)), mN), RING.one()), 2, mN), RING.monomial(new Q(2), 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
