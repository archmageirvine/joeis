package irvine.oeis.a342;
// Generated by gen_seq4.pl sumsi/sumgcd at 2021-11-20 22:12

import irvine.math.LongUtils;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A342420 a(n) = Sum_{k=1..n} (n/gcd(k,n))^(n/gcd(k,n)).
 * @author Georg Fischer
 */
public class A342420 implements Sequence {

  private int mN = 0;

  @Override
  public Z next() {
    ++mN;
    Z sum = Z.ZERO;
    for (int k = 1; k <= mN; ++k) {
      final long gd = LongUtils.gcd(mN, k);
      sum = sum.add(Z.valueOf(mN / gd).pow(mN / gd));
    }
    return sum;
  }
}
