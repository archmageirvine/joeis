package irvine.oeis.a343;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A343673 a(0) = 1; a(n) = 3 * n * a(n-1) + Sum_{k=0..n-1} binomial(n,k) * a(k).
 * E.g.f.: 1 / (2 - 3*x - exp(x))
 * @author Georg Fischer
 */
public class A343673 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.subtract(RING.monomial(new Q(2), 0), RING.monomial(new Q(3), 1)), RING.exp(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
