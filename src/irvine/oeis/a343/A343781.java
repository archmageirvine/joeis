package irvine.oeis.a343;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-28 19:36

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A343781 a(n) = Sum_{k=1..floor(n/2)} sigma_k(n-k), where sigma_k(n) is the sum of the k-th powers of the divisors of n.
 * a(n)=sum(i=1,floor(n/2),sigma(n-i,i))
 * @author Georg Fischer
 */
public class A343781 extends LambdaSequence {

  /** Construct the sequence. */
  public A343781() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 2, i -> Jaguar.factor(n - i).sigma(i)));
  }
}
