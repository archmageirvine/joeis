package irvine.oeis.a343;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A343843 a(n) = Sum_{k=0..n} (-1)^k*binomial(n, k)*A000831(k).
 * E.g.f.: exp(x)*(1 - tan(x))/(1 + tan(x))
 * @author Georg Fischer
 */
public class A343843 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.one(), RING.tan(RING.x(), mN)), mN), RING.add(RING.one(), RING.tan(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
