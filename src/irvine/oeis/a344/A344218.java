package irvine.oeis.a344;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A344218 Expansion of e.g.f. log(1 + (1/(1-x)^5 - 1)/5).
 * E.g.f.: log(1 + (1/(1-x)^5 - 1)/5)
 * @author Georg Fischer
 */
public class A344218 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.log(RING.add(RING.one(), RING.series(RING.subtract(RING.series(RING.one(), RING.pow(RING.oneMinusXToTheN(1), 5, mN), mN), RING.one()), RING.monomial(Q.FIVE, 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
