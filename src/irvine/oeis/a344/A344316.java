package irvine.oeis.a344;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A344316 Number of primes appearing along the border of an n X n square array whose elements are the numbers from 1..n^2, listed in increasing order by rows.
 * a(n)=primepi(n)+primepi(n^2-1)-primepi(n*(n-1))+sum(k=1, n-2, primepi(n*k+1)-primepi(n*k))
 * @author Georg Fischer
 */
public class A344316 extends LambdaSequence {

  /** Construct the sequence. */
  public A344316() {
    super(1, n -> Puma.primePiZ(n).add(Puma.primePiZ((long) n * n - 1)).subtract(Puma.primePiZ((long) n * n - n * 1L)).add(Integers.SINGLETON.sum(1, n - 2, k -> Puma.primePiZ((long) n * k + 1).subtract(Puma.primePiZ((long) n * k)))));
  }
}
