package irvine.oeis.a344;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A344987 Number of Goldbach partitions of 2n into 2 primes where the smaller prime has an odd index and the larger prime has an even index.
 * a(n)=sum(k=1, n, mod(primepi(k), 2)*mod(primepi(2*n-k)+1, 2)*(primepi(k)-primepi(k-1))*(primepi(2*n-k)-primepi(2*n-k-1)))
 * @author Georg Fischer
 */
public class A344987 extends LambdaSequence {

  /** Construct the sequence. */
  public A344987() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Puma.primePiZ(k).mod(Z.TWO).multiply(Puma.primePiZ(2 * n - k).add(Z.ONE).mod(Z.TWO)).multiply(Puma.primePiZ(k).subtract(Puma.primePiZ(k - 1))).multiply(Puma.primePiZ(2 * n - k).subtract(Puma.primePiZ(2 * n - k - 1)))));
  }
}
