package irvine.oeis.a345;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-12 11:03

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A345039 Number of partitions of n into two composite parts that share a nontrivial divisor.
 * a(n)=sum(k=1, floor(n/2), (1-primepi(k)+primepi(k-1))*(1-primepi(n-k)+primepi(n-k-1))*(1-floor(1/gcd(k, n-k))))
 * @author Georg Fischer
 */
public class A345039 extends LambdaSequence {

  /** Construct the sequence. */
  public A345039() {
    super(1, n -> Integers.SINGLETON.sum(1, n / 2, k -> Z.ONE.subtract(Puma.primePiZ(k)).add(Puma.primePiZ(k - 1)).multiply(Z.ONE.subtract(Puma.primePiZ(n - k)).add(Puma.primePiZ(n - k - 1))).multiply(Z.ONE.subtract(Z.ONE.divide(Z.valueOf(k).gcd(n - k))))));
  }
}
