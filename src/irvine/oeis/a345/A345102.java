package irvine.oeis.a345;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A345102 a(n) = 1 + 3 * Sum_{k=0..n-1} binomial(n,k) * a(k) * a(n-k-1).
 * E.g.f.: exp(x) / sqrt(7 - 6 * exp(x))
 * @author Georg Fischer
 */
public class A345102 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.x(), mN), RING.sqrt(RING.subtract(RING.monomial(new Q(7), 0), RING.multiply(RING.monomial(new Q(6), 0), RING.exp(RING.x(), mN), mN)), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
