package irvine.oeis.a345;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A345132 Number of (n+2) X (n+2) symmetric matrices with nonnegative integer entries, trace 0, with n rows that sum to 2, and 2 rows that sum to 1.
 * E.g.f.: exp(x^2/4-x/2)/(1-x)^(3/2)
 * @author Georg Fischer
 */
public class A345132 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.subtract(RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(new Q(4), 0), mN), RING.series(RING.x(), RING.monomial(new Q(2), 0), mN)), mN), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.series(RING.monomial(new Q(3), 0), RING.monomial(new Q(2), 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
