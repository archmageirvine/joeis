package irvine.oeis.a345;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence1;

/**
 * A345887 Number of tilings of an n-cell circular array with rectangular tiles of any size, and where the number of possible colors of a tile is given by the largest cell covered.
 * E.g.f.: (exp(x)-x)/(x-1)^2 - exp(x)
 * @author Georg Fischer
 */
public class A345887 extends Sequence1 {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.series(RING.subtract(RING.exp(RING.x(), mN), RING.x()), RING.pow(RING.subtract(RING.x(), RING.one()), 2, mN), mN), RING.exp(RING.x(), mN)).coeff(mN).multiply(mF).toZ();
  }
}
