package irvine.oeis.a346;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A346432 a(0) = 1; a(n) = n! * Sum_{k=0..n-1} (n-k+1) * a(k) / k!.
 * E.g.f.: 1 / (2 - 1 / (1 - x)^2)
 * @author Georg Fischer
 */
public class A346432 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.monomial(new Q(2), 0), RING.series(RING.one(), RING.pow(RING.oneMinusXToTheN(1), 2, mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
