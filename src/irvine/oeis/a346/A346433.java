package irvine.oeis.a346;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A346433 E.g.f.: 1 / (2 - exp(2*(exp(x) - 1))).
 * E.g.f.: 1 / (2 - exp(2*(exp(x) - 1)))
 * @author Georg Fischer
 */
public class A346433 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.multiply(RING.monomial(new Q(2), 0), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
