package irvine.oeis.a346;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A346629 Number of n-digit positive integers that are the product of two integers ending with 2.
 * E.g.f.: (9*exp(10*x) - 9 + 110*x - 50*x^2)/200
 * @author Georg Fischer
 */
public class A346629 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = 0;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.subtract(RING.add(RING.subtract(RING.multiply(RING.monomial(Q.NINE, 0), RING.exp(RING.monomial(Q.TEN, 1), mN), mN), RING.monomial(Q.NINE, 0)), RING.monomial(new Q(110), 1)), RING.monomial(new Q(50), 2)), RING.monomial(new Q(200), 0), mN).coeff(mN).multiply(mF).toZ();
  }
}
