package irvine.oeis.a346;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A346750 E.g.f.: log( 1 + x^2 * exp(x) / 2 ).
 * E.g.f.: log( 1 + x^2 * exp(x) / 2 )
 * @author Georg Fischer
 */
public class A346750 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.log(RING.add(RING.one(), RING.series(RING.multiply(RING.pow(RING.x(), 2, mN), RING.exp(RING.x(), mN), mN), RING.monomial(new Q(2), 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
