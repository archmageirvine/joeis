package irvine.oeis.a346;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A346754 Expansion of e.g.f. -log( 1 - x^3 * exp(x) / 3! ).
 * E.g.f.: -log( 1 - x^3 * exp(x) / 6 )
 * @author Georg Fischer
 */
public class A346754 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A346754() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.negate(RING.log(RING.subtract(RING.one(), RING.series(RING.multiply(RING.pow(RING.x(), 3, n), RING.exp(RING.x(), n), n), RING.monomial(Q.SIX, 0), n)), n));
  }
}
