package irvine.oeis.a346;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A346844 E.g.f.: exp(exp(x) - 1) * (exp(x) - 1)^5 / 5!.
 * E.g.f.: exp(exp(x) - 1) * (exp(x) - 1)^5 / 120
 * @author Georg Fischer
 */
public class A346844 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A346844() {
    super(5);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.series(RING.multiply(RING.exp(RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), RING.pow(RING.subtract(RING.exp(RING.x(), mN), RING.one()), 5, mN), mN), RING.monomial(new Q(120), 0), mN);
  }
}
