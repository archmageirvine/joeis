package irvine.oeis.a346;
// Generated by gen_seq4.pl egfu at 2021-12-06 15:00

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A346893 E.g.f.: 1 / (1 - x^5 * exp(x) / 5!).
 * E.g.f.: 1 / (1 - x^5 * exp(x) / 120)
 * @author Georg Fischer
 */
public class A346893 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A346893() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int mN) {
    return RING.series(RING.one(), RING.subtract(RING.one(), RING.series(RING.multiply(RING.pow(RING.x(), 5, mN), RING.exp(RING.x(), mN), mN), RING.monomial(new Q(120), 0), mN)), mN);
  }
}
