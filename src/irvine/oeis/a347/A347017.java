package irvine.oeis.a347;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A347017 a(n) = floor(2^(n-1)) - binomial(n,3) + binomial(n,2) - n + 1.
 * E.g.f.: exp(x)*(sinh(x) + 1 - x + x^2/2 - x^3/6)
 * @author Georg Fischer
 */
public class A347017 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.multiply(RING.exp(RING.x(), mN), RING.subtract(RING.add(RING.subtract(RING.add(RING.sinh(RING.x(), mN), RING.one()), RING.x()), RING.series(RING.pow(RING.x(), 2, mN), RING.monomial(new Q(2), 0), mN)), RING.series(RING.pow(RING.x(), 3, mN), RING.monomial(new Q(6), 0), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
