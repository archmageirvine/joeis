package irvine.oeis.a347;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A347072 E.g.f.: -log(1 - x) * (sec(x) + tan(x)).
 * E.g.f.: -log(1 - x) * (sec(x) + tan(x))
 * @author Georg Fischer
 */
public class A347072 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.zero(), RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.add(RING.sec(RING.x(), mN), RING.tan(RING.x(), mN)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
