package irvine.oeis.a347;
// Generated by gen_seq4.pl sumdiv at 2023-04-05 22:33

import irvine.math.IntegerUtils;
import irvine.math.LongUtils;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A347176 G.f.: Sum_{k&gt;=1} (-1)^(k+1) * k * x^(k^2) / (1 - x^(k^2)).
 * @author Georg Fischer
 */
public class A347176 extends AbstractSequence {

  private int mN = 0;

  /** Construct the sequence. */
  public A347176() {
    super(1);
  }

  @Override
  public Z next() {
    ++mN;
    return Integers.SINGLETON.sumdiv(mN, d -> LongUtils.isSquare(d) ? Z.valueOf((((((d & 1) + 1) & 1) == 0) ? 1 : -1) * IntegerUtils.sqrt(d)) : Z.ZERO);
  }
}
