package irvine.oeis.a347;
// Generated by gen_seq4.pl egfsi at 2021-12-01 15:37

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.Sequence;

/**
 * A347502 Number of dominating sets in the n-cycle complement graph.
 * E.g.f.: exp(2*x) + x^4/12 - exp(x)*(1 + 2*x)
 * @author Georg Fischer
 */
public class A347502 implements Sequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.add(RING.exp(RING.monomial(Q.TWO, 1), mN), RING.series(RING.pow(RING.x(), 4, mN), RING.monomial(new Q(12), 0), mN)), RING.multiply(RING.exp(RING.x(), mN), RING.add(RING.one(), RING.monomial(Q.TWO, 1)), mN)).coeff(mN).multiply(mF).toZ();
  }
}
