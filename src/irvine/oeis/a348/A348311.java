package irvine.oeis.a348;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A348311 a(n) = n! * Sum_{k=1..n} (-1)^k * (k-2) / (k-1)!.
 * E.g.f.: x * (1 + x) * exp(-x) / (1 - x)
 * @author Georg Fischer
 */
public class A348311 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A348311() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.multiply(RING.multiply(RING.x(), RING.onePlusXToTheN(1), mN), RING.exp(RING.subtract(RING.zero(), RING.x()), mN), mN), RING.oneMinusXToTheN(1), mN);
  }
}
