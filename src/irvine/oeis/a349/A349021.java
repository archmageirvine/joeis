package irvine.oeis.a349;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A349021 G.f. satisfies A(x) = 1/(1 - x/(1 - x*A(x))^2)^4.
 * @author Georg Fischer
 */
public class A349021 extends LambdaSequence {

  /** Construct the sequence. */
  public A349021() {
    super(0, n -> {
      final int s = 2;
      final int t = 4;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(t * n - (t - 1) * (k - 1), k).multiply(Binomial.binomial(n + (s - 1) * k - 1, n - k)).divide(n - k + 1));
    });
  }
}
