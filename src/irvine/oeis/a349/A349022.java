package irvine.oeis.a349;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A349022 G.f. satisfies A(x) = 1/(1 - x/(1 - x*A(x))^3)^4.
 * @author Georg Fischer
 */
public class A349022 extends LambdaSequence {

  /** Construct the sequence. */
  public A349022() {
    super(0, n -> {
      final int s = 3;
      final int t = 4;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial((long) t * n - (long) (t - 1) * (k - 1), k).multiply(Binomial.binomial(n + (long) (s - 1) * k - 1, n - k)).divide(n - k + 1));
    });
  }
}
