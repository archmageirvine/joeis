package irvine.oeis.a349;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A349089 a(n) = n! * Sum_{k=0..floor((n-1)/4)} 1 / (4*k+1)!.
 * E.g.f.: (sin(x) + sinh(x)) / (2*(1 - x))
 * @author Georg Fischer
 */
public class A349089 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A349089() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.add(RING.sin(RING.x(), mN), RING.sinh(RING.x(), mN)), RING.multiply(RING.monomial(Q.TWO, 0), RING.oneMinusXToTheN(1), mN), mN);
  }
}
