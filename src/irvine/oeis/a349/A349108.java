package irvine.oeis.a349;
// Generated by gen_seq4.pl 2026-08-11.ack/perman01 at 2026-08-11 22:53

import irvine.math.group.MatrixField;
import irvine.math.matrix.LambdaMatrix;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

import java.util.function.BiFunction;


/**
 * A349108 a(n) is the permanent of the n X n matrix A(n) that is defined as A[i,j,n] = (n mod 2) + abs((n + 1)/2 - i) + abs((n + 1)/2 - j).
 * @author Georg Fischer
 */
public class A349108 extends AbstractSequence {

  private long mN = 0;
  private BiFunction<Long, Long, Q> mLambda;

  /** Construct the sequence */
  public A349108() {
    super(0);
    mN = -1;
    mLambda = (i, j) -> new Q(mN & 1).add(new Q(mN + 1, 2).subtract(i).abs()).add(new Q(mN + 1, 2).subtract(j).abs());
  }

  @Override
  public Z next() {
    if (++mN == 0) {
      return Z.ONE;
    }
    final LambdaMatrix<Q> mat = new LambdaMatrix<Q>(mN, mN, Q.ZERO, mLambda, 1);
    return new MatrixField<Q>(mN, Rationals.SINGLETON).permanent(mat).toZ();
  }
}
