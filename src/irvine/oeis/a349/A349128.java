package irvine.oeis.a349;
// Generated by gen_seq4.pl multman/mult at 2023-10-02 19:24

import irvine.factor.prime.Puma;
import irvine.math.z.Z;
import irvine.oeis.MultiplicativeSequence;

/**
 * A349128 a(n) = phi(A064989(n)), where A064989 is multiplicative with a(2^e) = 1 and a(p^e) = prevprime(p)^e for odd primes p, and phi is Euler totient function.
 * Multiplicative with: a(2^e) = 1, and a(p^e) = (q - 1)*q^(e-1), where q = prevprime(p).
 * @author Georg Fischer
 */
public class A349128 extends MultiplicativeSequence {

  /** Construct the sequence. */
  public A349128() {
    super(1, (p, e) -> {
      if (is2(p)) {
        return Z.ONE;
      } else {
        final Z q = Puma.prevPrimeZ(p);
        return q.subtract(1).multiply(q.pow(e - 1));
      }
    });
  }
}
