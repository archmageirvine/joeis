package irvine.oeis.a349;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-27 21:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A349255 G.f. A(x) satisfies A(x) = 1 / ((1 + x) * (1 - 2 * x * A(x)^2)).
 * a(n)=sum(k=0,n,(-1)^(n-k) * binom(n+k,n-k) * 2^k * binom(3*k,k) / (2*k+1))
 * @author Georg Fischer
 */
public class A349255 extends LambdaSequence {

  /** Construct the sequence. */
  public A349255() {
    super(0, n -> Integers.SINGLETON.sum(0, n, k -> Z.NEG_ONE.pow(n - k).multiply(Binomial.binomial(n + k, n - k)).multiply(Z.ONE.shiftLeft(k)).multiply(Binomial.binomial(3 * k, k)).divide(Z.valueOf(2 * k + 1))));
  }
}
