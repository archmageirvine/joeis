package irvine.oeis.a350;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-13 11:40

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A350102 Number of self-measuring subsets of the initial segment of the natural numbers strictly below n. Number of subsets S of [n] with S = distset(S).
 * a(n) = 2 + sum(k=1,n-1,floor((n - 1)/k))
 * @author Georg Fischer
 */
public class A350102 extends LambdaSequence {

  /** Construct the sequence. */
  public A350102() {
    super(0, n -> (n == 0) ? Z.ONE : Z.TWO.add(Integers.SINGLETON.sum(1, n - 1, k -> Z.valueOf(n - 1).divide(k))));
  }
}
