package irvine.oeis.a350;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-27 20:43

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A350128 a(n) = Sum_{k=1..n} k^n * floor(n/k)^2.
 * a(n) = sum(k=1,n,2*k*sigma(k,n-1)-sigma(k,n));
 * @author Georg Fischer
 */
public class A350128 extends LambdaSequence {

  /** Construct the sequence. */
  public A350128() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Z.TWO.multiply(k).multiply(Jaguar.factor(k).sigma(n - 1)).subtract(Jaguar.factor(k).sigma(n))));
  }
}
