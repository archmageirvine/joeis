package irvine.oeis.a350;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.function.Functions;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A350719 a(n) = Sum_{k=0..n} k! * 2^k * k^n * Stirling1(n,k).
 * a(n) = sum(k=0,n,k!*2^k*k^n*stirl(n,k,1));
 * @author Georg Fischer
 */
public class A350719 extends LambdaSequence {

  /** Construct the sequence. */
  public A350719() {
    super(0, n -> Integers.SINGLETON.sum(0, n, k -> {
      return Functions.FACTORIAL.z(k).multiply(Z.ONE.shiftLeft(k)).multiply(Z.valueOf(k).pow(n)).multiply(Functions.STIRLING1.z((long) n, (long) k));
    }));
  }
}
