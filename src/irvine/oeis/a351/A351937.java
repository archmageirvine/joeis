package irvine.oeis.a351;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A351937 Expansion of e.g.f. exp( (sinh(x) + x*cosh(x)) / 2 ).
 * E.g.f.: exp( (sinh(x) + x*cosh(x)) / 2 )
 * @author Georg Fischer
 */
public class A351937 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A351937() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.series(RING.add(RING.sinh(RING.x(), mN), RING.multiply(RING.x(), RING.cosh(RING.x(), mN), mN)), RING.monomial(Q.TWO, 0), mN), mN);
  }
}
