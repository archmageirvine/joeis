package irvine.oeis.a352;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A352069 Expansion of e.g.f. 1 / (1 + log(1 - 3*x) / 3).
 * E.g.f.: 1 / (1 + log(1 - 3*x) / 3)
 * @author Georg Fischer
 */
public class A352069 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A352069() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.add(RING.one(), RING.series(RING.log(RING.subtract(RING.one(), RING.monomial(new Q(3), 1)), mN), RING.monomial(new Q(3), 0), mN)), mN);
  }
}
