package irvine.oeis.a352;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A352070 Expansion of e.g.f. 1/(1 - log(1 + 3*x))^(1/3).
 * E.g.f.: 1/(1 - log(1 + 3*x))^(1/3)
 * @author Georg Fischer
 */
public class A352070 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A352070() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.log(RING.add(RING.one(), RING.monomial(Q.THREE, 1)), mN)), mN), RING.series(RING.one(), RING.monomial(Q.THREE, 0), mN), mN), mN), mN);
  }
}
