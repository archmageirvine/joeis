package irvine.oeis.a352;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A352429 a(0) = 1; a(n) = Sum_{k=0..floor((n-1)/4)} binomial(n,4*k+1) * a(n-4*k-1).
 * E.g.f.: 1 / (1 - (sin(x) + sinh(x)) / 2)
 * @author Georg Fischer
 */
public class A352429 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A352429() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.subtract(RING.one(), RING.series(RING.add(RING.sin(RING.x(), mN), RING.sinh(RING.x(), mN)), RING.monomial(Q.TWO, 0), mN)), mN);
  }
}
