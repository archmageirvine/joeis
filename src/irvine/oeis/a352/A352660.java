package irvine.oeis.a352;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A352660 a(n) = n! * Sum_{k=0..floor(n/4)} 1 / (4*k)!.
 * E.g.f.: (cos(x) + cosh(x)) / (2*(1 - x))
 * @author Georg Fischer
 */
public class A352660 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A352660() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.add(RING.cos(RING.x(), mN), RING.cosh(RING.x(), mN)), RING.multiply(RING.monomial(Q.TWO, 0), RING.oneMinusXToTheN(1), mN), mN);
  }
}
