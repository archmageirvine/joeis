package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354015 Expansion of e.g.f. 1/(1 - x)^(1 - log(1-x)).
 * E.g.f.: exp( -log(1-x) * (1 - log(1-x)) )
 * @author Georg Fischer
 */
public class A354015 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354015() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.subtract(RING.zero(), RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.subtract(RING.one(), RING.log(RING.oneMinusXToTheN(1), mN)), mN)), mN);
  }
}
