package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354242 Expansion of e.g.f. 1/sqrt(5 - 4 * exp(x)).
 * E.g.f.: 1/sqrt(5 - 4 * exp(x))
 * @author Georg Fischer
 */
public class A354242 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354242() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.pow(RING.subtract(RING.monomial(Q.FIVE, 0), RING.multiply(RING.monomial(Q.FOUR, 0), RING.exp(RING.x(), mN), mN)), Q.HALF, mN), mN);
  }
}
