package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354286 Expansion of e.g.f. 1/(1 - x)^(2/(1 + 2 * log(1-x))).
 * E.g.f.: 1/(1 - x)^(2/(1 + 2 * log(1-x)))
 * @author Georg Fischer
 */
public class A354286 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354286() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.oneMinusXToTheN(1), mN), RING.series(RING.monomial(Q.TWO, 0), RING.add(RING.one(), RING.multiply(RING.monomial(Q.TWO, 0), RING.log(RING.oneMinusXToTheN(1), mN), mN)), mN), mN), mN), mN);
  }
}
