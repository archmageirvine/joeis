package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354317 Expansion of e.g.f. exp(-log(1 + x)^2 / 2).
 * E.g.f.: 1/(1 + x)^(log(1 + x)/2)
 * @author Georg Fischer
 */
public class A354317 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354317() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.onePlusXToTheN(1), mN), RING.series(RING.log(RING.onePlusXToTheN(1), mN), RING.monomial(Q.TWO, 0), mN), mN), mN), mN);
  }
}
