package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354320 Expansion of e.g.f. 1/(1 - 4*x)^(x/8).
 * E.g.f.: 1/(1 - 4*x)^(x/8)
 * @author Georg Fischer
 */
public class A354320 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354320() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.monomial(Q.FOUR, 1)), mN), RING.series(RING.x(), RING.monomial(Q.EIGHT, 0), mN), mN), mN), mN);
  }
}
