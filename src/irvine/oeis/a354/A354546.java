package irvine.oeis.a354;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A354546 Expansion of e.g.f. exp(x)^( cos(x) - sin(x) ).
 * E.g.f.: exp(x)^( cos(x) - sin(x) )
 * @author Georg Fischer
 */
public class A354546 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A354546() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.multiply(RING.log(RING.exp(RING.x(), mN), mN), RING.subtract(RING.cos(RING.x(), mN), RING.sin(RING.x(), mN)), mN), mN);
  }
}
