package irvine.oeis.a355;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A355164 a(n) = exp(-1/3) * Sum_{k&gt;=0} (3*k + 2)^n / (3^k * k!).
 * E.g.f.: exp(2*x + (exp(3*x) - 1) / 3)
 * @author Georg Fischer
 */
public class A355164 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A355164() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.add(RING.monomial(Q.TWO, 1), RING.series(RING.subtract(RING.exp(RING.monomial(Q.THREE, 1), mN), RING.one()), RING.monomial(Q.THREE, 0), mN)), mN);
  }
}
