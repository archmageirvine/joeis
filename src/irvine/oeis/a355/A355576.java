package irvine.oeis.a355;
// Generated by gen_seq4.pl 2026-09-06.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A355576 Number A(n,k) of n-tuples (p_1, p_2, ..., p_n) of positive integers such that p_{i-1} &lt;= p_i &lt;= k^(i-1); square array A(n,k), n&gt;=0, k&gt;=0, read by antidiagonals.
 * @author Georg Fischer
 */
public class A355576 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A355576() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n == 0 ? Z.ONE : Integers.SINGLETON.sum(0, n - 1, j -> get(j, k).multiply(Z.NEG_ONE.pow(n - j)).multiply(Binomial.binomial(Z.valueOf(k).pow(j), n - j))).negate();
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
