package irvine.oeis.a356;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-27 20:43

import irvine.factor.factor.Jaguar;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A356046 a(n) = Sum_{k=1..n} sigma_n(k) * floor(n/k).
 * a(n) = sum(k=1,n,sumdiv(k,d,sigma(d,n)));
 * @author Georg Fischer
 */
public class A356046 extends LambdaSequence {

  /** Construct the sequence. */
  public A356046() {
    super(1, n -> Integers.SINGLETON.sum(1, n, k -> Integers.SINGLETON.sumdiv(k, d -> Jaguar.factor(d).sigma(n))));
  }
}
