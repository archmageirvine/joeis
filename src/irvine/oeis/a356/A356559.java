package irvine.oeis.a356;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A356559 a(n) = exp(-1) * n! * Sum_{k&gt;=0} Laguerre(n,k) / k!.
 * E.g.f.: exp(exp(-x/(1 - x)) - 1) / (1 - x)
 * @author Georg Fischer
 */
public class A356559 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A356559() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.series(RING.exp(RING.subtract(RING.exp(RING.subtract(RING.zero(), RING.series(RING.x(), RING.oneMinusXToTheN(1), mN)), mN), RING.one()), mN), RING.oneMinusXToTheN(1), mN);
  }
}
