package irvine.oeis.a357;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-08-22 13:29

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A357565 a(n) = 3*Sum_{k = 0..n} binomial(n+k-1,k)^2 + 2*Sum_{k = 0..n} binomial(n+k-1,k)^3.
 * a(n) = 3*sum(k = 0,n,binom(n+k-1,k)^2) + 2*sum(k = 0,n,binom(n+k-1,k)^3);
 * @author Georg Fischer
 */
public class A357565 extends LambdaSequence {

  /** Construct the sequence. */
  public A357565() {
    super(0, n -> (Z.THREE).multiply(Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(n + k - 1, k).pow(Z.TWO))).add(Z.TWO.multiply(Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(n + k - 1, k).pow(Z.THREE)))));
  }
}
