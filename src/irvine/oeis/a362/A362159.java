package irvine.oeis.a362;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A362159 Expansion of e.g.f. -exp(x * (1-9*x)^(1/3)).
 * E.g.f.: -exp(x * (1-9*x)^(1/3))
 * @author Georg Fischer
 */
public class A362159 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A362159() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.subtract(RING.zero(), RING.exp(RING.multiply(RING.x(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.monomial(new Q(9), 1)), mN), RING.series(RING.one(), RING.monomial(new Q(3), 0), mN), mN), mN), mN), mN));
  }
}
