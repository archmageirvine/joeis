package irvine.oeis.a362;
// Generated by gen_seq4.pl robots/egfu at 2023-05-07 23:33

import irvine.math.polynomial.Polynomial;
import irvine.math.q.Q;
import irvine.oeis.ExponentialGeneratingFunction;

/**
 * A362247 Expansion of e.g.f. exp(x * exp(x * (exp(x) - 1))).
 * E.g.f.: exp(x * exp(x * (exp(x) - 1)))
 * @author Georg Fischer
 */
public class A362247 extends ExponentialGeneratingFunction {

  /** Construct the sequence. */
  public A362247() {
    super(0);
  }

  @Override
  public Polynomial<Q> compute(final int n) {
    return RING.exp(RING.multiply(RING.x(), RING.exp(RING.multiply(RING.x(), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN), mN), mN);
  }
}
