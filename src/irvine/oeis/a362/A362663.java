package irvine.oeis.a362;
// Generated by gen_seq4.pl expr_gen/lambda at 2023-09-13 11:40

import irvine.factor.prime.Puma;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A362663 a(n) is the partial sum of b(n), which is defined to be the difference between the numbers of primes in (n^2, n^2 + n] and in (n^2 - n, n^2].
 * a(n) = sum(i=1,n,(primepi(i^2+i) + primepi(i^2-i) - 2*primepi(i^2)))
 * @author Georg Fischer
 */
public class A362663 extends LambdaSequence {

  /** Construct the sequence. */
  public A362663() {
    super(1, n -> Integers.SINGLETON.sum(1, n, i -> Puma.primePiZ(i * i + i).add(Puma.primePiZ(i * i - i)).subtract(Z.TWO.multiply(Puma.primePiZ(i * i)))));
  }
}
