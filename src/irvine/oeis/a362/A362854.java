package irvine.oeis.a362;
// Generated by gen_seq4.pl multman/mult at 2023-10-02 19:24

import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.MultiplicativeSequence;

/**
 * A362854 The sum of the divisors of n that are both bi-unitary and exponential.
 * Multiplicative with: a(p^e) = Sum_{d|e} p^d if e is odd, and (Sum_{d|e} p^d) - p^(e/2) if e is even.
 * @author Georg Fischer
 */
public class A362854 extends MultiplicativeSequence {

  /** Construct the sequence. */
  public A362854() {
    super(1, (p, e) -> Integers.SINGLETON.sumdiv(e, d -> p.pow(d)).subtract(((e & 1) == 1) ? Z.ZERO : p.pow(e / 2)));
  }
}
