package irvine.oeis.a364;
// Generated by gen_seq4.pl egfsi at 2023-09-04 07:31

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A364112 Expansion of e.g.f. 3*x/(exp(-3*x)+exp(-x)+exp(x)).
 * @author Georg Fischer
 */
public class A364112 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A364112() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.monomial(new Q(3), 1), RING.add(RING.add(RING.exp(RING.subtract(RING.zero(), RING.monomial(new Q(3), 1)), mN), RING.exp(RING.subtract(RING.zero(), RING.x()), mN)), RING.exp(RING.x(), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
