package irvine.oeis.a367;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A367233 G.f. satisfies A(x) = 1 + x*A(x)^3 / (1 - x*A(x))^3.
 * @author Georg Fischer
 */
public class A367233 extends LambdaSequence {

  /** Construct the sequence. */
  public A367233() {
    super(0, n -> {
      final int s = 3;
      final int t = 3;
      final int u = 1;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(t * k + u * (n - k) + 1, k).multiply(Binomial.binomial(n + (s - 1) * k - 1, n - k)).divide(t * k + u * (n - k) + 1));
    });
  }
}
