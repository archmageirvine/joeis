package irvine.oeis.a367;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A367281 G.f. satisfies A(x) = 1 + x*A(x)^2 / (1 - x*A(x)^3)^3.
 * @author Georg Fischer
 */
public class A367281 extends LambdaSequence {

  /** Construct the sequence. */
  public A367281() {
    super(0, n -> {
      final int s = 3;
      final int t = 2;
      final int u = 3;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial((long) t * k + (long) u * (n - k) + 1, k).multiply(Binomial.binomial(n + (long) (s - 1) * k - 1, n - k)).divide((long) t * k + (long) u * (n - k) + 1));
    });
  }
}
