package irvine.oeis.a367;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A367283 G.f. satisfies A(x) = 1 + x*A(x)^2 * (1 + x*A(x)^3)^2.
 * @author Georg Fischer
 */
public class A367283 extends LambdaSequence {

  /** Construct the sequence. */
  public A367283() {
    super(0, n -> {
      final int s = 2;
      final int t = 2;
      final int u = 3;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial(t * k + u * (n - k) + 1, k).multiply(Binomial.binomial(s * k, n - k)).divide(t * k + u * (n - k) + 1));
    });
  }
}
