package irvine.oeis.a367;
// Generated by gen_seq4.pl serexp/lambdan at 2024-01-22 15:40

import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.oeis.LambdaSequence;

/**
 * A367284 G.f. satisfies A(x) = 1 + x*A(x) * (1 + x*A(x)^3)^3.
 * @author Georg Fischer
 */
public class A367284 extends LambdaSequence {

  /** Construct the sequence. */
  public A367284() {
    super(0, n -> {
      final int s = 3;
      final int t = 1;
      final int u = 3;
      return Integers.SINGLETON.sum(0, n, k -> Binomial.binomial((long) t * k + (long) u * (n - k) + 1, k).multiply(Binomial.binomial((long) s * k, n - k)).divide((long) t * k + (long) u * (n - k) + 1));
    });
  }
}
