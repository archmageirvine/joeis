package irvine.oeis.a367;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A367423 Expansion of e.g.f. 1 / sqrt(1 + log(1 - 2*x)).
 * @author Georg Fischer
 */
public class A367423 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A367423() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.pow(RING.add(RING.one(), RING.log(RING.subtract(RING.one(), RING.monomial(Q.TWO, 1)), mN)), Q.HALF, mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
