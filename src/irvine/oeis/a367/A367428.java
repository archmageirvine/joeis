package irvine.oeis.a367;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A367428 Expansion of e.g.f. 1 / (1 - log(1 + 3*x))^(2/3).
 * @author Georg Fischer
 */
public class A367428 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A367428() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.one(), RING.exp(RING.multiply(RING.log(RING.subtract(RING.one(), RING.log(RING.add(RING.one(), RING.monomial(new Q(3), 1)), mN)), mN), RING.series(RING.monomial(new Q(2), 0), RING.monomial(new Q(3), 0), mN), mN), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
