package irvine.oeis.a367;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A367890 Expansion of e.g.f. exp(3*(exp(x) - 1 - x)).
 * @author Georg Fischer
 */
public class A367890 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A367890() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.multiply(RING.monomial(Q.THREE, 0), RING.subtract(RING.subtract(RING.exp(RING.x(), mN), RING.one()), RING.x()), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
