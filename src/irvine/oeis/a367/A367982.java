package irvine.oeis.a367;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A367982 Expansion of e.g.f. exp(-2*x) / (2 - exp(4*x)).
 * @author Georg Fischer
 */
public class A367982 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A367982() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.series(RING.exp(RING.subtract(RING.zero(), RING.monomial(new Q(2), 1)), mN), RING.subtract(RING.monomial(new Q(2), 0), RING.exp(RING.monomial(new Q(4), 1), mN)), mN).coeff(mN).multiply(mF).toZ();
  }
}
