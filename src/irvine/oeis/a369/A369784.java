package irvine.oeis.a369;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A369784 Expansion of e.g.f. exp( (exp(2*(exp(x)-1))-1)/2 ).
 * @author Georg Fischer
 */
public class A369784 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A369784() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.subtract(RING.exp(RING.multiply(RING.monomial(new Q(2), 0), RING.subtract(RING.exp(RING.x(), mN), RING.one()), mN), mN), RING.one()), RING.monomial(new Q(2), 0), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
