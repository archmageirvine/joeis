package irvine.oeis.a370;
// Generated by gen_seq4.pl egfsi at 2024-02-24 18:19

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A370326 E.g.f.: exp(Sum_{k&gt;=1} binomial(2*k,k) * x^k).
 * @author Georg Fischer
 */
public class A370326 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A370326() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.subtract(RING.series(RING.one(), RING.pow(RING.subtract(RING.one(), RING.monomial(Q.FOUR, 1)), Q.HALF, mN), mN), RING.one()), mN).coeff(mN).multiply(mF).toZ();
  }
}
