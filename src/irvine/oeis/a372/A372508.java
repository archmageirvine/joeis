package irvine.oeis.a372;
// Generated by gen_seq4.pl 2025-01-03.ack/egfsi at 2025-01-06 00:11

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A372508 Expansion of e.g.f. exp(sinh(x)) - exp(cosh(x) - 1).
 * @author Georg Fischer
 */
public class A372508 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A372508() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.subtract(RING.exp(RING.sinh(RING.x(), mN), mN), RING.exp(RING.subtract(RING.cosh(RING.x(), mN), RING.one()), mN)).coeff(mN).multiply(mF).toZ();
  }
}
