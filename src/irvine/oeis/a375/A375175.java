package irvine.oeis.a375;
// Generated by gen_seq4.pl 2025-01-03.ack/egfsi at 2025-01-06 00:11

import irvine.math.group.PolynomialRingField;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Z;
import irvine.oeis.AbstractSequence;

/**
 * A375175 Expansion of e.g.f. exp( (exp( (exp(4*x) - 1)/2 ) - 1)/2 ).
 * @author Georg Fischer
 */
public class A375175 extends AbstractSequence {

  private static final PolynomialRingField<Q> RING = new PolynomialRingField<>(Rationals.SINGLETON);
  private int mN = -1;
  private Z mF = Z.ONE;

  /** Construct the sequence. */
  public A375175() {
    super(0);
  }

  @Override
  public Z next() {
    if (++mN != 0) {
      mF = mF.multiply(mN);
    }
    return RING.exp(RING.series(RING.subtract(RING.exp(RING.series(RING.subtract(RING.exp(RING.monomial(Q.FOUR, 1), mN), RING.one()), RING.monomial(Q.TWO, 0), mN), mN), RING.one()), RING.monomial(Q.TWO, 0), mN), mN).coeff(mN).multiply(mF).toZ();
  }
}
