package irvine.oeis.a375;
// Generated by gen_seq4.pl 2026-06-23.ack/lambdan at 2026-06-23 23:37

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A375931 The product of the prime powers in the prime factorization of n that have an exponent that is equal to the maximum exponent in this factorization.
 * @author Georg Fischer
 */
public class A375931 extends LambdaSequence {

  /** Construct the sequence. */
  public A375931() {
    super(1, n -> {
      if (n == 1) {
        return Z.ONE;
      }
      final FactorSequence fs = Jaguar.factor(n);
      final long max = fs.maxExponent();
      return FactorUtils.iterate(n, Z.ONE, (x, p, e) -> x.multiply(e == max ? p.pow(e) : Z.ONE));
    });
  }
}
