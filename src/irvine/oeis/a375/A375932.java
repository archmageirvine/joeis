package irvine.oeis.a375;
// Generated by gen_seq4.pl 2026-06-30.ack/lambdan at 2026-06-30 21:58

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A375932 The largest unitary k-free divisor of n where k = A051903(n) is the maximum exponent in the prime factorization of n.
 * @author Georg Fischer
 */
public class A375932 extends LambdaSequence {

  /** Construct the sequence. */
  public A375932() {
    super(1, n -> {
      final FactorSequence fs = Jaguar.factor(n);
      final int me = fs.maxExponent();
      return FactorUtils.iterate(n, Z.ONE, (x, p, e) -> x.multiply(p.pow(e * (e < me ? 1 : 0))));
    });
  }
}
