package irvine.oeis.a375;
// Generated by gen_seq4.pl 2026-06-23.ack/lambdan at 2026-06-23 23:37

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A375933 The second-largest exponent in the prime factorization of n, or 0 if it does not exist.
 * @author Georg Fischer
 */
public class A375933 extends LambdaSequence {

  /** Construct the sequence. */
  public A375933() {
    super(1, n -> {
      final FactorSequence fs = Jaguar.factor(n);
      final long max = fs.maxExponent();
      return Z.valueOf(max == 1 ? 0 : FactorUtils.iterate(fs, 0L, (x, p, e) -> (e < max & e > x) ? e : x));
    });
  }
}
