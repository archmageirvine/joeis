package irvine.oeis.a376;
// Generated by gen_seq4.pl 2026-06-30.ack/lambdan at 2026-06-30 21:58

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A376140 The number of divisors of n whose prime factorization has maximum exponent that is smaller than the maximum exponent in the prime factorization of n.
 * @author Georg Fischer
 */
public class A376140 extends LambdaSequence {

  /** Construct the sequence. */
  public A376140() {
    super(1, n -> {
      final FactorSequence fs = Jaguar.factor(n);
      final int me = fs.maxExponent();
      return (n == 1) ? Z.ZERO : FactorUtils.iterate(fs, Z.ONE, (x, p, e) -> x.multiply(e + (e < me ? 1 : 0)));
    });
  }
}
