package irvine.oeis.a376;
// Generated by gen_seq4.pl 2026-06-23.ack/filnum at 2026-06-23 23:37

import irvine.factor.factor.Jaguar;
import irvine.factor.util.FactorSequence;
import irvine.factor.util.FactorUtils;
import irvine.oeis.FilterNumberSequence;

/**
 * A376251 Numbers that have a second-largest exponent in their prime factorization and it is smaller by 1 than the largest exponent.
 * @author Georg Fischer
 */
public class A376251 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A376251() {
    super(1, 1, k -> {
      final FactorSequence fs = Jaguar.factor(k);
      final long max = fs.maxExponent();
      final long second = FactorUtils.iterate(fs, 0L, (x, p, e) -> (e < max & e > x) ? e : x);
      return second > 0L && second == max - 1;
    });
  }
}
