package irvine.oeis.a380;
// Generated by gen_seq4.pl 2025-02-10.ack/lambdan at 2025-02-10 21:26

import irvine.math.function.Functions;
import irvine.math.q.Q;
import irvine.math.q.Rationals;
import irvine.math.z.Binomial;
import irvine.oeis.LambdaSequence;

/**
 * A380801 Expansion of e.g.f. ( (1/x) * Series_Reversion( x * exp(-x / (1 - x)^2) ) )^2.
 * @author Georg Fischer
 */
public class A380801 extends LambdaSequence {

  /** Construct the sequence. */
  public A380801() {
    super(0, n -> {
      final int q = 2;
      final int r = 1;
      final int s = 1;
      final int t = 2;
      final int u = 0;
      return Rationals.SINGLETON.sum(0, n, k -> new Q(r * n + (s - r) * k + q).pow(k - 1).multiply(Binomial.binomial((r * u + 1) * n + ((s - r) * u + t - 1) * k + q * u - 1, n - k)).divide(Functions.FACTORIAL.z(k))).multiply(Functions.FACTORIAL.z(n).multiply(q)).num();
    });
  }
}
