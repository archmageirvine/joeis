package irvine.oeis.a381;
// Generated by gen_seq4.pl 2026-06-19.ack/lambdan at 2026-06-19 23:35

import java.util.TreeSet;

import irvine.factor.util.FactorUtils;
import irvine.math.predicate.Predicates;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A381399 a(n) is the number of prime elements in the set of bases and exponents in the prime factorization of n.
 * @author Georg Fischer
 */
public class A381399 extends LambdaSequence {

  /** Construct the sequence. */
  public A381399() {
    super(1, n -> {
      final TreeSet<Z> ts = new TreeSet<>();
      return FactorUtils.iterate(n, Z.ZERO, (x, p, e) -> x.add(ts.add(p) ? 1 : 0).add(ts.add(Z.valueOf(e)) && Predicates.PRIME.is(e) ? 1 : 0));
    });
  }
}
