package irvine.oeis.a381;
// Generated by gen_seq4.pl 2026-06-19.ack/filnum at 2026-06-19 23:35

import java.util.TreeSet;

import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A381402 Numbers k such that the set P of bases and exponents in the prime factorization of k (including exponents = 1) contains all numbers from min(P) to max(P).
 * @author Georg Fischer
 */
public class A381402 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A381402() {
    super(1, 1, k -> {
      if (k <= 1) {
        return false;
      } else {
        final TreeSet<Z> ts = new TreeSet<>();
        final Z y = FactorUtils.iterate(k, Z.ZERO, (x, p, e) -> {
          ts.add(p);
          ts.add(Z.valueOf(e));
          return p;
        });
        return ts.last().subtract(ts.first()).add(1).equals(ts.size());
      }
    });
  }
}
