package irvine.oeis.a381;
// Generated by gen_seq4.pl 2026-08-27.ack/marrlon

import irvine.math.MemoryFunction4;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A381426
 * @author Georg Fischer
 */
public class A381426 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A381426() {
    super(0, 0, 0, -1);
  }

  final MemoryFunction4<Long, Z> mB = new MemoryFunction4<>() {
    @Override
    protected Z compute(final Long o, final Long u, final Long t, final Long k) {
      return
        u + o == 0 ? Z.ONE : (t == 1 ? get(u + o, 0L, 0L, k) : Z.ZERO).add(Integers.SINGLETON.sum(1, o, j -> Z.valueOf(k).pow(u + j - 1).multiply(get(o - j, u + j - 1, 1L, k))));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, 0L, 0L, k);
  }
}
