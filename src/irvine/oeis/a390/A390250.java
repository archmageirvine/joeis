package irvine.oeis.a390;
// Generated by gen_seq4.pl 2026-06-23.ack/filnum at 2026-06-23 23:37

import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.FilterNumberSequence;

/**
 * A390250 Numbers m&gt;1 such that e_1 * (p_1 - 1) = ... = e_s * (p_s - 1), where p_1^e_1 * ... * p_s^e_s is the prime factorization of m.
 * @author Georg Fischer
 */
public class A390250 extends FilterNumberSequence {

  /** Construct the sequence. */
  public A390250() {
    super(1, 1, k -> FactorUtils.iterate(k, Z.ZERO, (x, p, e) -> x.isZero() ? p.subtract(1).multiply(e) : (x.equals(p.subtract(1).multiply(e)) ? x : Z.NEG_ONE)).compareTo(Z.ZERO) > 0);
  }
}
