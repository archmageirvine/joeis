package irvine.oeis.a392;
// Generated by gen_seq4.pl 2026-06-23.ack/lambdan at 2026-06-23 23:37

import irvine.factor.util.FactorUtils;
import irvine.math.z.Z;
import irvine.oeis.LambdaSequence;

/**
 * A392065 a(n) = number of primes with unit exponent in the prime factorization of n, or 1 if no such primes exist.
 * @author Georg Fischer
 */
public class A392065 extends LambdaSequence {

  /** Construct the sequence. */
  public A392065() {
    super(1, n -> {
      if (n == 1) {
        return Z.ONE;
      }
      final long num = FactorUtils.iterate(n, 0L, (x, p, e) -> x + (e == 1 ? 1 : 0));
      return Z.valueOf(num == 0L ? 1L : num);
    });
  }
}
