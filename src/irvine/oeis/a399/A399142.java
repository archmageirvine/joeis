package irvine.oeis.a399;
// Generated by gen_seq4.pl 2026-08-27.ack/mtrilon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.BaseTriangle;
import irvine.oeis.triangle.DirectArray;

/**
 * A399142 Triangle read by rows: T(n,k) counts trees on [n] rooted at 1 together with a level assignment L:[n]-&gt;{0,...,k} that uses every level, has L(1)=0, and strictly increases from parent to child, 0&lt;=k&lt;=n-1.
 * @author Georg Fischer
 */
public class A399142 extends BaseTriangle implements DirectArray {

  /** Construct the sequence. */
  public A399142() {
    super(0, 1, 0);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        k == 0 ? Z.valueOf(n == 1 ? 1 : 0) : Integers.SINGLETON.sum(k, n - 1, j ->
          Z.valueOf(j).pow(n - j).multiply(Binomial.binomial(n - 1, j - 1)).multiply(get(j, k - 1)));
    }
  };

  @Override
  public Z triangleElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
