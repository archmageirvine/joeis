package irvine.oeis.a399;
// Generated by gen_seq4.pl 2026-09-08.ack/marrlon

import irvine.math.MemoryFunction2;
import irvine.math.z.Binomial;
import irvine.math.z.Integers;
import irvine.math.z.Z;
import irvine.oeis.triangle.DirectArray;
import irvine.oeis.triangle.UpperLeftTriangle;

/**
 * A399444
 * @author Georg Fischer
 */
public class A399444 extends UpperLeftTriangle implements DirectArray {

  /** Construct the sequence. */
  public A399444() {
    super(0, 0, 0, 1);
  }

  final MemoryFunction2<Long, Z> mB = new MemoryFunction2<>() {
    @Override
    protected Z compute(final Long n, final Long k) {
      return
        n < 0 || k < 0 ? Z.ZERO : (k == 0 ? Z.valueOf(n == 0 ? 1 : 0) : (n == 0 && k == 1 ? Z.ONE : (k == 1 ? Integers.SINGLETON.sum(1, n, i -> get(n - i, i)) : Integers.SINGLETON.sum(0, n, i -> get(n - i, k - 1).multiply(get(i, 1L)).multiply(Binomial.binomial(n + k, i + 1)).multiply(Binomial.binomial(n, i))))));
    }
  };

  @Override
  public Z matrixElement(final int n, final int k) {
    return a(n, k);
  }

  @Override
  public Z a(final long n, final long k) {
    return mB.get(n, k);
  }
}
